# 复盘总结（2026-08-12）

> 覆盖：第一期（目录章节增删）、第二期（本地漫画导入）、第三期（批量拆分粘连章节）、第四期（4.1~4.5）＋ 三轮手测反馈修复＋第五期（售后与收尾桶）。

## 1. 总体脉络

从“本地 TXT 章节级编辑”出发，按计划书逐期推进：一期打基础（单章插入/删除）→ 二期独立成线（本地漫画导入）→ 三期把一期的机制扩展成批量拆分＋中文数字＋事务化 → 四期 4.1 收尾（规则超集、首标题规范化、书源书籍常驻、off-by-one）。过程中用户手测三轮，每轮都形成“复现 → 定位 → 修复 → 单测/实测验证”的闭环。

## 2. 各期成果与状态

| 期 | 内容 | 状态 | 关键提交 |
| --- | --- | --- | --- |
| 一期 | 目录新增/删除章节（本地 TXT） | 完成；事务化、中文数字、拆分在三期补强 | 1725e4215 / b14a42b37 / 33f3bca28 |
| 二期 | 本地漫画导入（三层结构、整本/系列、预览、漫画阅读器、书架分类） | 完成 + 两轮反馈迭代 | 9026b68ca / 807dd804c / 6d1e6c459 / 407f0a5df / 96a058178 |
| 三期 | 批量拆分粘连章节、中文数字重写、目录编辑事务化、测试补齐 | 完成 + 两轮修复 | a66316548 / c8da1ff00 / 73964bece / a4e32a6bb / a991821ca / 92904954f |
| 四期 4.1 | 拆分规则超集、首标题规范化、书源书籍常驻、默认标题 off-by-one | 完成并实测 | 584c1036b |
| 四期 4.2~4.5 | 多选合并、转本地引导、漫画增强、工程补齐 | 未开始（计划书已备） | — |

## 3. 手测发现与修复记录（三轮闭环）

1. **拆分时原章节标题被顶掉**（用户：没拆出来）：本地 TXT 解析器给的章节正文从标题行之后开始，拆分器把正文内第一个标题（第2回）当首单元，覆盖了“第1章”。修复：`ChapterSplitter.split` 增加 `firstTitle` 补入，正文首行不是本章标题时先补上（提交 a991821ca）。
2. **拆完标题跳号**（用户：第三章后直接到第六章）：拆分复用了“手动新增章节”的级联 +N 重写，续章标题被 +2。修复：拆分对后续章节仅右移 `index`、**标题保留原文**（提交 92904954f）。
3. **书源章节名与拆分规则对不上**（用户反馈 1）：内部空格、大写/全角数字、卷集部篇话后缀、包裹符号、分卷复合标题等失配。四期 4.1 将拆分规则扩展为目录规则超集，并对首标题做编号规范化比较（第1章 == 第一章）（提交 584c1036b）。
4. **书源书籍分类“消失”**（用户反馈 2）：非回归，原实现为“有书源书才显示”；已按用户预期改为常驻显示（`BookGroupDao.show` 中 -13 无条件，模拟器实测位于最左）。

## 4. 工程质量

- **事务化**：`BookChapterDao.applyTocEdit`（@Transaction 接口默认方法），插入/删除/拆分整体原子提交，失败自动回滚；配套 androidTest（TocEditTransactionTest）。
- **测试**：JVM 单测 48 个（中文数字、拆分纯逻辑、超集规则、规范化）；androidTest 2 个（事务回滚、书架分类）编译就绪，待真机运行。
- **验证门槛**：每步 `compileAppDebugKotlin` + 单测 + `assembleDebug` 打包通过。
- **环境踩坑记录**：
  - 计划书里的 `compileLegadoDebugKotlin` 实际不存在，实为 `compileAppDebugKotlin`；
  - Debug 包 `applicationId` 为 `io.legado.app.debug`，启动需完整 Activity 类名；
  - APK 文件名带构建时间戳（`legado_app_3.26.xxxxxx.apk`），安装脚本必须动态取文件，否则静默安装失败（曾导致手测跑旧包）；
- 模拟器旧签名不一致需卸载重装；中文文件名经 adb 推送乱码，用 ASCII 镜像目录解决；
- uiautomator dump 偶发返回旧内容，需检查 dump 输出而非静默覆盖。
- 本机 GitHub 直连不通，需走代理：`git config --global http(s).proxy http://127.0.0.1:7897`（Clash Verge 混合端口，已配置）；
- 本仓库 fork：`origin` = tjf-github/legado-E，`upstream` = Luoyacheng/legado-E；工作分支 `codex/manga-import` 已推送到 origin 并跟踪。

## 5. 关键设计决策

1. **拆分与手动新增的语义分离**：拆出的单元标题来自原文，续章标题不重编号（只动位置），避免标题序断裂；
2. **规则集对齐**：拆分规则 = 默认目录规则的超集，消除“目录粘住但拆分识别不了”的死角；纯数字标题不纳入（防正文行误命中），不设开关；
3. **旧功能修复谨慎原则**：默认标题 off-by-one 先标记、确认预期编号（`anchor.index + 1`）后再修，不随其他改动顺手改；
4. **冗余收敛**：“竖排无间隙开关”与“图片间距可调”功能重叠，四期设计合并为间距档位（字段兼容）；拆分规则不暴露“纯数字标题/场”等过度开关；
5. **“其它”折叠分类**（讨论待定）：只收整理/状态类（未分组、更新失败），音频/视频保持一级；可配置收纳范围、空则隐藏（四期计划书 §4.6）。

## 6. 遗留与未完成

- 四期 4.2 多选合并冗余章节（复用 `applyTocEdit`/`collectShift`）；
- 四期 4.3 在线书“转本地”操作引导；
- 四期 4.4 漫画侧增强（相册导入、元数据识别、混合结构自动化、统一阅读设置面板、间距档位、大目录懒加载/分页）；
- 四期 4.5 工程补齐（构建脚本封装、`ReadConfig` 序列化测试、工作区卫生）；
- 真机回归：androidTest 2 用例 + 一/二/三期手测清单，用户执行；
- 工作区卫生：`build.log`、`watch-build.ps1`、`make_test_book.py`、`test_book.txt`、`test_book_split.txt`、`测试漫画素材/` 未跟踪待决；`modules/web` 三个生成文件有本地改动；
- 书架“其它”折叠分类（设计已记录，未立项）。

## 7. 提交清单（2026-08-12 起 15 个）

`a66316548` 事务化 · `cdffe5ad0` 文档修正 · `c8da1ff00` 工具层 · `73964bece` 拆分主流程 · `a4e32a6bb` 拆分 UI · `59e40883d` androidTest · `d8284148b` 三期完成标记 · `b163d8c3d` 三期复盘 · `a991821ca` 标题保留修复 · `cfff711a0` 手测记录 · `92904954f` 续章不改写 · `f45509eb7` 适配分析 · `2bde8ca7e` 四期计划书与复盘 · `584c1036b` 四期 4.1 · `94a285f83` 其它折叠建议。

## 8. 后续建议

1. 按推荐顺序继续 4.2 多选合并（复用现成事务机制，价值最高）；
2. 真机回归并行执行，重点覆盖拆分（含超集格式）、合并、漫画导入与书架分类；
3. 每批保持“编译 + 单测 + 打包”门槛，小步提交；
4. 漫画增强优先大目录性能，再排相册导入；
5. “其它”折叠按 4.6 约束再立项，不阻塞主流程。

## 9. 四期 4.2 完成与整体测试（2026-08-12 续）

### 完成

- 4.2 多选合并冗余章节（提交 cb586c085）：菜单项 + 多选对话框（标题可编辑）+ `mergeChapters` 主流程（单事务、续章仅 index 左移、书签/进度同步）。

### 整体测试结果（模拟器实测）

- **通过**：书源书籍标签最左常驻；合并对话框多选/改标题；第2回+第3回合并 → 目录 5→4 章、续章编号前移（序号连续）；合并内容含两章正文且可读；书架卡片状态正确；48 个单测、编译、`assembleDebug` 全通过。
- **待真机**：androidTest 2 用例（事务回滚、书架分类）；合并的书签/进度同步实测。

### 正确（做得对的）

1. 事务化统一（`applyTocEdit`）：拆分/合并/插入/删除共用一条原子提交路径，失败自动回滚；
2. “标题保留原文”语义贯穿拆分与合并，避免编号/标题错乱；
3. 先写覆盖文件再入库，避免“有章节无正文”的坏状态；
4. 每轮反馈都走“复现 → 定位 → 修复 → 单测/实测”闭环；
5. 环境坑（APK 时间戳文件名、调试包名、签名不一致、adb 中文乱码）记录在案，避免重复踩。

### 错误（过程中出现并被纠正）

1. 拆分初版丢失原章节标题（`firstTitle` 补入修复）；
2. 拆分续章 +N 重写导致标题跳号（改为仅 index 移位、标题保留）；
2b. 合并初版未重写续章标题，2、3 合并后仍为 4（序号断档）——已修复：合并是删除语义，续章标题 -removalCount 重写；与拆分（插入语义，不改写）区分；
2c. 合并后无法拆分（反馈）：初版合并正文为纯拼接、无标题边界，合并结果不可再拆分撤销——已修复：合并时在后续章节正文前插入其标题行作为边界，合并结果可再拆分还原（模拟器实测：合并第2回+第3回后拆分预览正确还原 2 章）；
2d. 排查中发现隐藏问题：`BookHelp.getContent` **覆盖文件优先**，而“重新导入/重新解析”不清理覆盖文件——旧编辑/旧拆分/旧合并写入的 `.nb` 残留会顶替新导入的同名章节正文（内容错乱、拆分失灵）。本次模拟器“仅识别到一章”即因此产生；建议四期补“重新导入时清理该书覆盖文件”或明确文档说明；
3. 安装时引用过期 APK 文件名导致静默装旧包，手测跑错版本；
4. `uiautomator dump` 静默失败取到旧内容，多次误判界面（应检查 dump 输出而非吞掉错误）；
5. PowerShell 传参中中文引号/换行导致 `git commit` 失败两次（改用无引号/多 `-m`）；
6. Kotlin `Char` 运算类型错误（`'０' + '0'`）等编译错误。

### 疏漏（未覆盖 / 未做）

1. 合并的书签/阅读进度同步未在模拟器实测（依赖 androidTest 真机）；
2. 合并跨章（非相邻，如 第2回+第4章）场景未在模拟器验证；
3. 合并/拆分后目录字数统计不立即刷新（`wordCount` 待阅读时更新）；
4. 超集规则对真实书源书未实测（模拟器无书源书，需用户手测）；
5. 大章节（长正文）拆分/合并性能未压测；
6. 四期 4.3 转本地引导、4.4 漫画增强、4.5 工程补齐未开始；
7. 真机回归、工作区卫生（未跟踪文件）、书架“其它”折叠分类未立项。

### 反馈 2 结论（点击中间右侧跳目录）

- 非缓存、非错误：阅读器的**点击区域设置**把“中间右侧”映射为“章节列表”（`clickActionMiddleRight=10`，偏好文件中已确认）；
- 修改路径：阅读设置 → 点击区域设置，把中右格改为“下一页/无操作”即可；
- 与代码无关，未改代码。

### 合并撤销设计取舍（2026-08-12 决定）

- 保留现状：合并插入标题边界（可拆分还原），不做“去除标题＋撤销栈”方案；
- 理由：合并需长按触发，误操作概率低；撤销栈与合并标题开关暂缓。

## 10. 今日总结（2026-08-12 日终，21 个提交）

### 完成

- **三期**：目录编辑事务化、中文数字标题重写、批量拆分粘连章节（预览/勾选/改名）、JVM 单测与 androidTest；
- **四期 4.1**：拆分规则超集（空格/大写/全角数字、卷集部篇话、包裹符号）、首标题规范化、书源书籍常驻、默认标题 off-by-one 修复；
- **四期 4.2**：多选合并冗余章节（对话框、单事务、续章编号前移、标题边界可拆分撤销）；
- **规划**：第四期计划书（4.1~4.5）、一至三期复盘记录、整体复盘、书架“其它”折叠产品建议。

### 手测反馈闭环（5 条）

1. 拆分丢原章节标题 → `firstTitle` 补入；
2. 拆分续章跳号（第3回后直接第6章）→ 续章仅 index 移位、标题保留；
3. 书源章节名与拆分规则不匹配 → 规则超集＋编号规范化；
4. 合并后序号断档（2、3 合并后仍为 4）→ 续章 -N 重写；
5. 合并后无法拆分撤销 → 合并插入标题边界（保留此设计）；点击中间右侧跳目录 → 点击区域设置，非错误。

### 重要发现

- **覆盖文件残留**：`BookHelp.getContent` 覆盖优先，重导/重解析不清 `.nb`，旧编辑残留会顶替新导入正文（模拟器“仅识别到一章”即此因）——已记录，建议后续修复或文档说明。

### 质量验证

- 48 个 JVM 单测通过；androidTest 2 用例编译就绪待真机；`assembleDebug` 多次通过；
- 模拟器实测：拆分/合并全流程、撤销、续章编号、书源书籍标签常驻均验证。

### 遗留

- 真机回归（androidTest＋手测清单）；四期 4.3~4.5；工作区卫生；
- 覆盖文件残留问题的修复；书架“其它”折叠分类（待定）。

### 明日建议

1. 四期 4.3 在线书“转本地”引导；
2. 覆盖文件残留问题：重导/重解析时清理该书覆盖文件（或明确行为文档）；
3. 真机回归并行，重点覆盖拆分（超集格式）、合并撤销、漫画导入与书架分类。

## 11. 今日总结（2026-08-13，第四期 4.3~4.5 + 4.4 全部推进，9 个提交）

### 完成

- **覆盖文件残留修复**（`32ba47aea`）：`BookHelp.getContent` 覆盖优先，本地书重新解析路径（详情页加载目录/目录规则变更/刷新目录 API/源文件外部修改）只删目录不清 `.nb`，旧编辑/拆分/合并残留会顶替新正文（复盘 §9 2d 遗留）。本次在 4 处重新解析路径统一 `clearCache`；**冷启动（文件未变）不清缓存**，保留用户目录编辑；
- **4.3 转本地引导**（`c21611d0f`）：书籍详情页菜单「转本地」（仅在线书，音频/视频除外）：说明引导（仅含已缓存章节）→ 选 txt/epub → 选导出目录（记忆上次路径）→ 复用 `ExportBookService` 一键导出，提示书架本地导入；
- **4.5 工程补齐**：`ReadConfig` GSON 序列化单测 4 例 + 单测 JVM `--add-opens java.base/java.time`（`3be247648`）；第四期预处理盘点文档（`c9b336d90`）；构建工具与测试素材收编、`build.log` 忽略（`627f0f896`，新增 `build.ps1` 统一构建）；
- **4.4 漫画增强**：
  - 扫描进度回调 + 系列模式 x/y 显示（4.4.6 进度部分）；
  - 封面识别（命名含 cover/封面 优先，其次首图）写入 `Book.coverUrl`（4.4.2）；
  - 混合结构建议角色：直接图片数 ≥ 章节图片总数时默认整本连看，行级切换保留（4.4.3）；
  - 相册导入（4.4.1，`e74b573db`）：`READ_MEDIA_IMAGES`（API33+）/存储读取（低版本），MediaStore 相册=书、整本连看单章节，`album://` 书目录回退入库章节；
  - 图片间距档位（4.4.5，`3a3af49f7`）：`imageGap` 0/小/中/大（0/8/16/32dp），`imageNoGap` 字段保留兼容（false 映射为中档），替换原无间隙开关；
  - 统一显示设置面板（4.4.4，`1fc16a90f`）：方向/吸附/间距/动画/灰度/墨水屏（含阈值）/隐藏标题/缩放/点击滚动，菜单分散项移除。

### 验证

- JVM 单测 **54 个全绿**（`testAppDebugUnitTest`，含新增 ReadConfig 5 例、扫描器 7 例）；
- `compileAppDebugKotlin` + `assembleDebug` 通过；
- 未跑模拟器/真机手测（本次为代码侧交付，手测清单见下）。

### 设计决策

1. 重新解析清覆盖缓存的分寸：只在“源文件外部修改/显式刷新/目录规则变更”时清，**冷启动保留用户编辑**；
2. 相册书用 `album://` 前缀 bookUrl + `scanChapters` 回退入库章节，不引入新表/新列，遵守“不加表不加列”原则；
3. 间距档位兼容映射收敛冗余：0 = 原“无间隙开启”，旧 `imageNoGap=false` 自动视为中档；
4. 显示设置统一面板按现有菜单能力迁移，灰度/墨水屏互斥逻辑保留。

### 设计决策（2026-08-13 补：漫画书架排序）

- 需求：漫画有阅读顺序，看完后位置不变（小说整本无此诉求）；
- 实现：系列/整本/相册导入时按批分配 `order`（系列内第1话 order 最小、置顶）；书架默认排序对漫画书按 `order` 升序（稳定），其他书仍按最近阅读；
- 验证：模拟器实测系列导入后 world1(-14)/world2(-13) 顺序正确；打开 world2 阅读返回后位置不变；
- 边界：用户显式选择其他排序方式（最近阅读/名称/综合等）时仍按用户选择，漫画稳定排序仅在默认排序生效。

### 遗留 / 待确认

- **4.4.6 未完整**：仅做了进度提示；“分批扫描 + 目录页章节懒加载”未做（大目录场景仍全量扫描）；
- **4.4.2 未完整**：封面识别已做；作者/系列推导仅系列分组沿用父目录名，作者仍手动编辑；
- **真机/模拟器手测未跑**，建议清单：转本地导出→导入全流程；相册导入（权限拒绝/同意、单书单章阅读）；扫描进度显示；封面/建议角色；间距档位（0/小/中/大生效）；显示设置面板各开关（方向切换、灰度/墨水屏互斥、阈值）；拆分/合并回归；
- **`modules/web` 3 个生成文件改动**（auto-imports.d.ts / components.d.ts / tsconfig.app.json 移除 baseUrl）：待用户确认回退或保留；
- **`app/build.gradle` 为本地专属（skip-worktree）**：`testOptions` 的 `--add-opens` 只在本机生效，换环境跑 ReadConfig 测试需自行加该配置（或给生产 GSON 注册 LocalDate 适配器）；
- androidTest 2 用例仍待真机；书架“其它”折叠分类未立项。

### 补记（2026-08-13 同日收尾）

- **GSON java.time 治本**（`ef0b4beec`）：注册 `LocalDate/LocalDateTime/LocalTime` 适配器，序列化 ISO 字符串、反序列化兼容旧对象格式；不再依赖反射 java.time 私有字段，本地 `--add-opens` 配置已删除（build.gradle 本地专属），任何环境跑测试均无需额外参数；新增 7 个用例（61 全绿）；
- **modules/web 清理**（`8ebab9d5f`）：两个生成 d.ts 实为 autocrlf 伪修改（内容无变化），新增 `.gitattributes` 固定 LF 消除噪音；`tsconfig.app.json` 的 baseUrl 本地改动按确认回退，工作区干净；
- **第五期预留**：新增《第五期计划书-售后与收尾桶》，作为手测反馈修复池 + 四期遗留承接 + 候选功能目录，明确不是终点、新任务可新开。

### 手测记录（2026-08-13 模拟器 API 36，代码侧驱动 uiautomator）

**通过项**

- 应用启动/书架/标签（书源书籍、全部、本地小说、本地漫画、分组）；
- 整本导入：`/sdcard/manga_test` → manga_test（2 章节 5 图，符合固定三层结构，深层子文件夹不展开）；
- 系列导入：world1/world2 成书、系列分组（group=2）正确；
- 封面识别：DB 中 manga_test/world1/world2 的 coverUrl 均写入首图/首章首图；
- 显示设置面板：图片间距档位（选“中”后跨会话持久化）、方向切换（横排时显示横滑吸附项）、灰度/墨水屏互斥 + 阈值条显隐；
- 菜单精简：漫画菜单只剩“显示设置”统一入口，分散开关移除；
- 相册导入：权限引导（rationale → 系统弹窗）正常；TestAlbum 识别、导入（album://Pictures/TestAlbum、首图封面、单章）、阅读无报错；
- 转本地负向：本地书详情菜单不含“转本地”；
- 拆分回归：第2回 拆出“第3回 夜宿山庙”，后续章节标题保留、末片段计数更新；
- 合并回归：第3回+第3章尾声 → 合并入第3回，第4章 后会有期 → 第3章 后会有期（续章 -1），总数 5→4。

**发现并修复（1 个真 bug）**

- 相册导入在 API 35+ NPE：MediaStore 的 `BUCKET_ID/BUCKET_DISPLAY_NAME` 对**非本应用**媒体返回 null（隐私限制），`scanAlbums` 处理行时 NPE，相册恒为空。修复：改用 `RELATIVE_PATH` 分组 + 空值兜底（`bff5be310`），实测通过。

**环境/已知项（非代码问题）**

- 权限弹窗自动化点击“Allow all”未真正授权（测试驱动问题），改用 `pm grant` 后通过；应用侧权限流程正常；
- 拆分/合并后目录字数不立即刷新（已知疏漏，待阅读时更新）；
- 4.4.6 扫描进度文本因目录小、扫描快，自动化未能捕捉；代码路径已实现；
- 转本地正向（在线书）因模拟器无书源未能实测，仅验证了菜单可见性逻辑（本地书隐藏）。

### 优化记录（2026-08-13 二次开发，稳定性优先）

- 拆分正文空行保真（`e5f304551`）：不再过滤空行，单元正文保留段落结构；新增 2 个单测；
- 拆分/合并/新增后字数即时刷新（`e5f304551`）：`ChapterShift` 增加 `newWordCount`，事务内更新；模拟器实测拆分后 1238/1244/1250 字即时显示；
- 相册分组抽纯函数 `groupAlbums` + 3 个单测（`a6da40241`），防 API 15+ 隐私类问题回归；
- 死代码/文档清理（`c1290963a`）：删除无引用的 `MangaEpaperDialog` 及其布局与字符串；第四期计划书移除重复段落并标注完成状态；
- 原项目缺漏记录（不修）：见第五期计划书 §4.5（TextFile System.gc、LocalBook 错误当正文、STORAGE 分组仅看版本）。

### 第五期收尾（2026-08-13 续，4 个提交）

#### 完成

- **命中来源标注**（`aff75a045`）：拆分预览为每个单元标注命中规则类型（第N章 / Chapter N / 包裹符）；
- **撤销上次拆分**（`a175592b1`）：拆分前备份原章正文，拆分后菜单提供“撤销上次拆分”，恢复原章、删除新增章节并回移后续章节；
- **外部变更提示**（`49fbe45d9`）：源文件外部修改触发重解析且本地书存在覆盖文件时，先弹窗确认是否丢弃本地编辑，避免静默清理；
- **拆分预览弱提示**（`708794282`）：检测同编号重复单元与编号跳号，预览顶部给出弱提示；
- **四期遗留决策**：作者不自动推导（明确不补）；大目录分批扫描/目录懒加载待真机反馈或明确不补；modules/web 治理已完成。

#### 验证

- JVM 单测全绿（本次新增 `hasDuplicateNumber` / `hasNumberGap` 2 例）；`compileAppDebugKotlin` 与 `assembleDebug` 通过；
- 工作区卫生：`git status` 干净，无未跟踪文件、`modules/web` 无无关改动。

#### 低优先项决策

- 文件写入与 DB 事务不同步：本期记录不修，后续如需再做补偿机制；
- 超长章节拆分进度：本期记录不修，`SPLIT_LIMIT=500` 已兜底，超长章节可分批处理。

#### 仍待用户侧

- 真机回归（androidTest 2 用例 + 一~四期手测清单）；
- 手测反馈闭环：转本地、相册导入、间距档位、显示设置、拆分/合并回归；
- 书架“其它”折叠分类、在线书转本地后的批量合并整理等候选功能另立任务。

## 12. 后续维护与真机反馈（2026-08-15 ~ 16）

### 完成（两批真机反馈）

1. **书架排序/相册页序/导入搜索**（提交 cffabd393，2026-08-15）：
   - 书架「按书名/按作者」由 `cnCompare` 字典序改为 `String.naturalCompare`（数字块按数值、非数字块按拼音 Collator），修复 1,10,11,2,3 顺序；
   - 相册导入 `groupAlbums` 按图片 `displayName` 自然排序入库，修复整本连看各页乱序（MediaStore 的 DATE_ADDED 序 ≠ 文件名序）；
   - 漫画导入预览列表加搜索栏，按书名实时过滤（全选/汇总/确认仍作用于全量）。
2. **分组选择 + 目录多选删除**（2026-08-16，随本轮提交）：
   - 漫画导入加「分组」选择器（未分组/已有分组/新建分组），覆盖三模式，重导保留旧分组；
   - 目录菜单新增「删除所选章节」（漫画可见），`TocViewModel.deleteChapters` 批量删除（只移除目录、保留文件、单事务）。

### 流程固化

- 新增《发布推送流程书.md》+ 项目级 `AGENTS.md`（多 agent 共读），固化：push main 自动发版、沙箱提权三件事、openssl 后端配方、发布合并 `-X theirs`；
- 文档治理：删除「构建优化/第三期/第四期」三份「预处理/状态盘点」工作稿，结论已并入计划书与复盘。

### 关键踩坑（新增）

- **git 推送配方**：本机 SChannel 后端报 `SEC_E_NO_CREDENTIALS`，仅提权不够；可靠写法 = `danger-full-access` + `-c http.sslBackend=openssl`（openssl 解 TLS，提权解 GCM 凭据助手被沙箱拦）。
- **发版不是手动触发**：push `main` 即触发 `Test Build` 自动发 beta；曾误判为 `release.yml` 的 `workflow_dispatch`，教训已写入 `AGENTS.md`（接发布任务先核对 workflow 的 `on:`）。

### 验证

- JVM 单测全绿（含新增 `StringExtensionsTest`、`MangaFolderScannerTest.groupAlbumsSortsImagesByName`）；
- `compileAppDebugKotlin` + `assembleDebug` + `assembleAppRelease` 均通过。

### 仍待用户侧

- 真机回归：书架自然排序、相册页序、导入搜索、导入选分组、目录多选删除；
- 版本号小时精度撞号、CI update-tag Node 20 两项待办（见维护计划书）。
