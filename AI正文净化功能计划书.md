# AI 正文净化功能计划书

> 状态：**阶段 A/B 已完成并验收，阶段 C 尚未开始（2026-09-05）**
> 来源：在线书源正文可能包含错字、防盗替换、异常断行与格式污染；现有正则替换净化难以处理需要上下文判断的问题。
> 实施原则：默认关闭、逐书开启、原文可回退、分块处理、处理即释放模型上下文、结果可清理、不修改书源与原始正文。
> 约束：不新增数据库表/列；第一版只处理在线文字章节，不处理漫画、图片正文、OCR、本地书与整本批量改写。

## 0. 第一版决策摘要

第一版只做一条最小、可回退的主链路：**当前在线文字章节 + Chat Completions 协议 + 用户手动逐书开启 + 处理完成后手动切换 AI 正文**。以下决策不得在实现中重新模糊化：

- 现有确定性净化返回的 `BookContent` 是 AI 输入边界；AI 只处理其纯文本 `textList`，再构造保留 `sameTitleRemoved` 与 `effectiveReplaceRules` 的候选 `BookContent` 传给 `ChapterProvider`，不写回章节原文缓存；
- 正文先在整章范围内把 URL、数字等保护项哨兵化，再分成严格覆盖输入且互不重叠的块；`context_only` 是独立只读字段，不属于后一块正文；模型不返回改写后的整块文本，只返回带原文锚点的局部编辑列表，本机校验后应用；
- 逐书开关可保存在 `Book.ReadConfig` 并随书架备份；正文外发同意是“本设备 + 当前服务地址”的本地授权，不得随书架备份恢复到另一设备；
- API Key 必须由 Android Keystore 包装的独立安全存储持有，不进入普通 Preferences、数据库、书架/设置备份、调试导出、日志、异常和缓存；密钥存储未验收不得发布；
- 处理期间阅读页始终显示原文；完成后只提示“AI 正文已就绪”，不自动重排当前页面。用户主动切换或重新进入章节时，才用完整 AI 结果重新排版；切换时明确从章首开始，第一版不承诺原文与 AI 文本间的字符位置映射；
- 第一版不做 Responses 协议、下一章自动预处理、token/费用汇总和 200 MB LRU 自动淘汰；这些进入后续增强，不得混入首个实现提交。

## 1. 产品目标

在 Legado 的在线文字阅读链路中增加一个可选的 AI 后处理层，用于：

1. 修复明显错别字、同音替换字和防盗文中的故意替换；
2. 整理异常断行、粘连段落、重复标点等正文格式问题；
3. 保留原意、人物、数字、专有名词、情节与章节结构，不润色、不续写、不摘要；
4. 在 AI 不可用、输出不可信或用户关闭功能时，无条件回退到现有正文链路。

成功标准不是“AI 返回了文本”，而是：用户能稳定阅读、能看见处理状态、能随时回到原文，且不会因切章、预加载、失败重试造成失控调用或重复计费。

## 2. 第一版范围

### 2.1 纳入

- OpenAI-compatible 的 Chat Completions HTTP 服务，通过独立 Provider 接口隔离具体协议；
- 自定义服务地址、模型名、API Key、超时和单块字符上限；
- 全局 AI 配置 + 每本在线文字书独立开关；
- 当前章按段落切块、串行处理、逐块校验；
- AI 结果独立文件缓存、缓存失效、手动重试和清理；
- 处理状态、失败原因与“查看原文/重试”入口；
- 单元测试、伪 Provider 集成测试和真机手测清单。

### 2.2 明确不纳入

- 不自动为全部书籍或全部章节开启；
- 不把 AI 配置塞进书源规则，不允许书源提供者指定服务地址、提示词或密钥；
- 不上传书名、作者、书源 URL、Cookie、登录信息等非必要数据；
- 不处理图片标签、漫画、OCR、音频、RSS 和本地 TXT/EPUB；
- 不做文学润色、翻译、续写、摘要或敏感内容改写；
- 不修改既有章节原文缓存，不覆盖导出内容；第一版导出、全文搜索与朗读仍走原链路，后续单独评估一致性；
- 不实现多供应商账号同步、云端历史记录或整本永久 AI 化。
- 不实现 Responses 协议、自动预处理下一章、token/费用汇总和自动 LRU 淘汰；Provider 接口保留后续扩展能力。

## 3. 用户交互

### 3.1 全局配置

在设置中新增“AI 正文净化（实验）”：

- 总开关，默认关闭；
- 服务地址、模型、API Key；
- “测试连接”只发送固定测试文本，不发送书籍内容；
- 请求超时，默认 45 秒；
- 单块上限，默认 6000 个 Unicode 字符，可在安全范围内调整；
- 仅非计费网络处理，默认开启；实现以 Android 网络能力判断为准，不把“连接到 Wi-Fi”误当成一定免费；
- 清理 AI 正文缓存；
- 隐私提示：开启后，当前章节正文会发送给用户配置的第三方 AI 服务。

非敏感配置可使用现有应用配置持久化；API Key 必须由独立 `AiKeyStore` 使用 Android Keystore 包装后存入应用私有、禁止备份的位置。密钥不得进入普通 Preferences、数据库、书架/设置备份与导入、调试导出、日志、异常、缓存键或缓存元数据。仓库当前没有可直接复用的 `security-crypto`/`EncryptedSharedPreferences` 封装，因此这是阶段 B 的阻塞决策和发布门禁，不得写成已有能力。Keystore 密钥失效或解密失败时清除不可用密文、关闭 AI 总开关并要求用户重新输入，不得回退明文保存。

服务地址默认只接受 HTTPS；允许用户显式确认本机/局域网 HTTP 地址，但认证头不得跟随跨主机重定向。测试连接与正文请求都必须限制响应体大小，并在错误分类前完成脱敏。

### 3.2 每本书开关

- 仅在线文字书显示“AI 正文净化”；
- 默认关闭，用户逐书明确开启；
- 使用 `Book.ReadConfig` 的可选字段只保存逐书开关，不新增数据库列；旧数据缺少字段时等价于关闭；
- 开启时弹出正文外发与可能产生费用的确认；确认记录绑定本设备和规范化后的服务地址，不存进 `ReadConfig`，切换服务地址、恢复备份或迁移设备后必须重新确认；
- 关闭后立即取消本书未完成请求并显示原文，已生成缓存保留到用户清理或自然淘汰。

### 3.3 阅读页状态

当前章只出现简短状态，不阻塞基本阅读：

- “AI 处理中 2/5”；
- “AI 已处理”；
- “AI 失败，正在显示原文”；
- 操作：取消、重试、查看原文、重新处理本章。

首次实现不做逐字流式排版，也不在用户阅读中自动替换正文。当前章处理期间始终显示现有原文；整章结果确定后提示“AI 正文已就绪”。只有用户主动点“查看 AI 正文”或重新进入章节时，才把完整 AI 结果交给 `ChapterProvider` 重排，并明确从章首开始；“查看原文”同样从章首重新排版。这样不伪造原文位置与改写后位置之间并不存在的可靠映射。

## 4. 正文处理链路

```text
在线书源解析
  -> 既有原文缓存（保持不变）
  -> ContentProcessor 既有确定性处理
  -> AI 启用条件与缓存命中判断
  -> AiTextChunker 按段落切块
  -> AiTextProvider 串行请求
  -> AiOutputValidator 逐块校验
  -> 独立 AI 缓存
  -> ChapterProvider 排版显示
```

### 4.1 接入位置

- 不改 `WebBook.getContentAwait` 的书源解析职责；
- 不在同步的 `ContentProcessor.getContent` 内发网络请求；
- 在 `ReadBook.contentLoadFinish` / `contentLoadFinishAwait` 完成既有净化后、调用 `ChapterProvider` 前接入异步协调器；
- 为避免两条完成路径行为漂移，实施时先抽出共享的“准备可排版正文”挂起函数，再由两处调用；
- 共享函数返回 `AiDisplayContent(original: BookContent, aiCandidate: BookContent?, selectedSource)`：默认把 `original` 传给 `ChapterProvider`；仅当用户已选择 AI、缓存为当前书/章/输入哈希/配置版本的完整结果时，才构造并传入候选 `BookContent`。候选只替换 `textList`，必须保留原对象的 `sameTitleRemoved` 与 `effectiveReplaceRules`；
- AI 候选不写回 `BookHelp` 章节缓存，不改变 `WebBook`、导出、搜索或朗读读取的原文；
- 第一版只接阅读页，缓存、导出、搜索、朗读等旁路不得被悄悄改变。

### 4.2 启用条件

同时满足以下条件才允许请求：

1. 全局总开关已开；
2. 本书开关已开，且用户已确认正文外发；
3. 是在线文字章节，且通过唯一谓词 `isAiPlainText(book, bookContent)`：`!book.isLocal && !book.isImage`，每个 `textList` 元素均不匹配 `AppPattern.imgPattern`、`AppPattern.useHtmlRegex`，也不匹配 HTML 标签模式 `</?[A-Za-z][A-Za-z0-9:-]*(?:\s+[^<>]*?)?\s*/?>`；任一命中即整章拒绝。标签模式要求 `<` 后立即出现 `/` 或合法标签名，因此 `a < b > c`、`1 < 2` 和孤立尖括号属于允许的普通文本；`<p>`、`<font color="red">`、`<br/>`、`<b>` 等属于拒绝的结构化文本。该谓词被启用判断、测试语料和处理器共同复用，不在不同模块各写一套；
4. Provider 配置完整且网络策略允许；
5. 当前输入不存在有效 AI 缓存；
6. 请求仍属于当前书和当前章，协程未取消。

任一条件不满足，直接走原正文，不弹连续错误、不暗中重试。

## 5. 分块与上下文策略

“丢弃处理过的东西”定义为：**上一块完整正文不再进入后续模型请求**。为了处理跨块断句，只保留一个有上限、不可输出的短尾部上下文。

### 5.1 切块规则

1. 先扫描整章 `BookContent.textList`，把 URL、连续数字/数值等保护片段替换为本次任务内不可猜测、可逆的哨兵，保存只在内存存在的映射；
2. 再把哨兵化文本确定性分成互不重叠的正文块；所有块按顺序拼接必须与哨兵化整章逐 code point 相同，恢复哨兵后必须与原输入逐 code point 相同；
3. 优先按空行和段落边界切分；
4. 单段过长时按句末标点切分；仍过长才按 Unicode code point 边界硬切，不得截断代理对、组合字符或完整哨兵；
5. 默认每块正文不超过 6000 个 Unicode code point；系统提示、JSON 外壳、`context_only` 与输出预算分别计算；
6. 后一块最多携带前一块末尾 200 个 code point 作为独立 `context_only`；它不属于后一块正文、编辑偏移与整章拼接；
7. 块按章节内序号串行处理，最多一个请求在途；
8. 每块完成并校验后写入临时结果，释放请求体、响应体和上一块主体；模型会话不得累积整章历史。

这里限制的是模型上下文与请求内存；阅读排版最终仍需得到完整章节文本。完整结果通过临时文件/缓存组装，不把已处理内容重复发送给模型。

### 5.2 请求契约

系统提示固定版本化，核心约束：

- 只纠正可以从上下文明确判断的错字、防盗替换和格式错误；
- 不润色、不扩写、不删减事实、不改变人名地名与数值；
- 不解释处理过程，不返回改写后的整块正文，也不得对 `context_only` 提交编辑；
- 只返回当前 `chunk_text` 的局部编辑列表。每项包含按 Unicode code point 计的 `start`（含）、`end`（不含）、`original`、`replacement`、`kind`；`kind` 仅允许 `typo`、`anti_theft`、`punctuation`、`whitespace`；
- `original` 必须逐字等于对应范围，编辑按 `start` 升序、互不重叠；模型不得触碰或构造哨兵。

第一版协议固定为 Chat Completions，并要求适配层归一化成结构化结果：`chunk_id`、`edits`、`warnings`、`finish_reason`。`temperature` 固定为 0 或服务允许的最低确定性值；`finish_reason` 为长度截断、响应体超限或 JSON 不完整时整块失败。每块最多接受 128 项编辑；Provider 必须为最坏编辑 JSON 和外壳预留输出预算，无法保证时缩小输入块。最终正文由本机从后向前应用已校验编辑，再恢复哨兵；模型没有直接生成整块正文的写入权。

## 6. 校验与回退

AI 输出必须先校验，校验失败不能直接进入阅读页。

### 6.1 硬校验

- `chunk_id` 与请求一致；
- JSON 完整、`finish_reason` 不是长度截断，编辑数不超过 128；空编辑列表是合法“无需修改”，不是失败；
- 不包含提示词、Markdown 围栏、解释性前后缀；
- 每项 `start/end` 在当前 `chunk_text` 范围内、按序且互不重叠，`original` 与该 code point 范围逐字一致；任何偏移歧义整块失败；
- 任一编辑不得覆盖、拆分、删除或构造哨兵；恢复后 URL、数字等保护项的值、数量与顺序逐字不变；
- `typo`/`anti_theft` 的 `original` 与 `replacement` 都非空、各自最多 12 个核心字符，且去除空白与标点后的核心字符数必须相等；因此不能用此类编辑删句、并句或整段重写；
- `punctuation`/`whitespace` 编辑的两侧都不得包含汉字、字母或数字，只能改空白与标点；异常断行、粘连段落和重复标点只能通过此类局部编辑完成；
- 分块身份测试中哨兵化原始块拼接等于哨兵化整章，恢复后等于原 `BookContent.textList`；应用编辑后再做一遍哨兵完整性与本地结构检查。

### 6.2 失败策略

- 任一块协议错误、超时或校验失败：停止本章后续请求，整章结果作废并继续显示原文；第一版不向用户呈现 AI/原文混合章节；
- 认证失败或限流同样立即停止本章，不自动重试；
- 不自动跨 Provider 切换，不无限重试；
- 用户离开当前书、关闭功能或切换配置：取消未完成请求，迟到响应不得落盘；
- 第一版只缓存并显示 `Completed` 整章；`Failed` 和取消结果不得成为可命中缓存；
- 所有异常最终都必须能显示原文，AI 故障不能导致白页或章节加载失败。

## 7. 缓存设计

### 7.1 原则

- 既有章节缓存继续保存书源解析得到的原文；
- AI 结果放在应用 `cacheDir/ai_text` 独立目录，不覆盖原文、不进入 Android/应用备份；系统可随时回收，回收后只导致重新处理；
- 不加数据库表/列；元数据使用与结果同目录的小型 JSON 文件；
- 先写同目录临时文件，只有全部块处理完且整章状态为 `Completed` 才原子替换正式结果与元数据；部分、取消或崩溃删除临时文件，不留下可命中条目。

### 7.2 缓存键

缓存身份至少包含：

```text
bookUrl 哈希
+ chapterUrl 哈希 + index
+ AI 输入正文 SHA-256
+ Prompt 版本
+ Provider 类型/服务地址哈希/模型名
+ 分块参数版本
+ 校验规则版本
+ 配置世代号
```

API Key 不进入缓存键和元数据。正文、现有替换规则、简繁转换、模型、提示词或分块规则变化，都会自然产生新键。手动“重新处理本章”只删除该章 AI 缓存，不动原文缓存。

### 7.3 清理与淘汰

- 提供按本书、按本章和全部清理；
- 第一版不实现自动 LRU；缓存目录由系统回收，用户可手动清理；
- `BookHelp.clearCache(book)` 与删除书籍必须联动删除该书 AI 缓存；“清理 AI 正文缓存”只删 AI 目录，不动原文缓存；
- 服务地址、模型、Prompt、分块或校验配置变化时递增配置世代；读取时只接受当前世代和当前输入哈希，旧条目可以延后清理但永不降级命中。

## 8. 并发、取消与费用控制

- 全 App AI 请求并发数固定为 1；
- 切章、离开本书、关闭功能或配置变化立即取消旧任务；已经发出的 HTTP 请求即使返回，也必须在每次写临时块、提交正式缓存和刷新 UI 前重新核对任务令牌、书/章、输入哈希与配置世代；
- 同一缓存键只允许一个任务，阅读刷新不得重复请求；
- 认证失败和限流不自动重试；普通网络瞬断每块最多重试 1 次，并采用短退避；
- 设置页显示本次/本章请求块数和发送字符数，不记录 token 汇总、不估算费用；
- 日志只记请求 ID、块号、耗时、状态和长度，不记录正文、API Key、完整响应或认证头。

## 9. 模块边界（拟新增）

建议放在 `app/src/main/java/io/legado/app/help/ai/`，保持阅读模型只负责编排：

- `AiTextProvider`：供应商无关的挂起接口与错误分类；
- `OpenAiCompatibleProvider`：第一版 HTTP 适配；
- `AiTextChunker`：纯函数切块与短尾上下文；
- `AiPromptBuilder`：版本化编辑协议、整章保护项哨兵化与内存映射；
- `AiOutputValidator`：纯函数校验与回退原因；
- `AiChapterCache`：独立文件缓存、原子写与淘汰；
- `AiChapterProcessor`：串行状态机、去重、取消和进度；
- `AiTextConfig`：全局配置读取与脱敏；
- `AiKeyStore`：Android Keystore 包装、禁止备份的密文存储、失效清除与重新输入状态；
- `AiDisplayContent`：持有原始/完整候选 `BookContent` 与当前选择；候选只替换 `textList` 并保留原元数据，是传给 `ChapterProvider` 的唯一显示源；
- `AiProcessResult`：`Original` / `Completed` / `Failed`；第一版不暴露 AI/原文混合的 `Partial` 结果。

阅读页只依赖 `AiChapterProcessor` 与 `AiDisplayContent`，不得直接拼 HTTP JSON。Provider 不得依赖 Activity/Fragment，切块器、校验器和缓存键生成器必须可在 JVM 单测中独立运行。两条 `contentLoadFinish*` 的共享函数抽取必须先作为独立、无行为变化的提交完成并回归，再接 AI 协调器。

## 10. 实施阶段

### 阶段 A：契约与反例测试

- 建立真实错误样本的脱敏小语料：错字、防盗替换、异常断行、正常文本、专名/数字、超长段落；
- 先写切块、上下文不重复、校验、缓存键和失败回退测试；
- 用 Fake Provider 验证串行、取消、去重和部分失败；
- 固化并共用 `isAiPlainText` 谓词；阶段 A 的固定反例中，`<img>`、`<usehtml>`、`<p>`、`<font color="red">`、`<br/>`、`<b>` 必须判“拒绝”，`a < b > c`、`1 < 2`、孤立 `<`/`>` 和普通纯文本必须判“允许”；URL、跨边界长数字等保护项必须先在整章哨兵化再切块。

完成条件：不接真实服务也能证明状态机不会重复、乱序、吞文或阻断原文。

### 阶段 B：Provider 与安全配置

> 状态：`accepted`（2026-09-05）。实现与验证证据见《AI正文净化-阶段B-DSH实施报告.md》；未接阅读页、未使用真实密钥或真实正文。

- 实现 Provider 接口和 Chat Completions 适配；
- 完成超时、错误分类、响应上限、日志脱敏；
- 完成密钥本机安全存储和“测试连接”；
- 配置变更时取消旧任务并使旧缓存失效。

完成条件：测试连接不发送书籍内容；书架备份与设置备份/导出产物、日志、异常和缓存元数据中均检索不到测试密钥字面量；Keystore 失效能安全关闭功能并要求重录。

### 阶段 C：分块、校验与缓存

- 实现段落优先切块、短尾上下文、串行处理；
- 实现局部编辑列表硬校验、本机应用、任一块失败时整章回退和调用熔断；
- 实现只缓存 `Completed` 整章、原子写和明确清理联动。

完成条件：超长章节能在固定上下文上限内处理；中途取消/崩溃不会留下可命中的伪完整缓存。

### 阶段 D：阅读链路与 UI

- 抽出两条 `contentLoadFinish*` 共用的挂起准备函数；
- 接入当前章处理、状态提示、原文切换和手动重试；
- 加全局开关、逐书开关、首次外发确认；
- 接入“AI 正文已就绪”、用户主动切换显示源和从章首重排；不做后台自动换文和下一章预处理。

完成条件：关闭功能时行为与当前版本一致；失败时可立即阅读原文；切章与返回不会产生迟到覆盖。

### 阶段 E：回归与发布前验收

- JVM 单测、编译、全量单测；
- 真机覆盖正常网络、断网、慢响应、认证失败、限流、取消、切章、旋转/退后台、缓存命中与清理；
- 检查阅读位置、段落、图片章节、替换净化开关和原文缓存未回归；
- 发版前补跑 lint，并在更新日志明确“实验功能、正文外发、默认关闭”。

## 11. 验收标准

### 11.1 自动化验收

- 原始块顺序拼接与整章输入逐 code point 相同；不丢字、不重字，超长单段和 Unicode 边界有确定性退化规则；
- 后一块请求不包含上一块主体，只含上限内 `context_only`；
- Provider 始终串行；相同缓存键并发请求只执行一次；
- JSON 不完整、`finish_reason` 截断、编辑超过 128 项、范围越界/重叠、原文锚点不符、非法 `kind`、触碰哨兵、文本编辑核心字符数不等、单项文本编辑超过 12 个核心字符或 chunk ID 错误均整章回退原文；
- URL 或长数值跨候选块边界时仍作为完整哨兵不被切开，恢复后的值、数量与顺序与原文逐字一致；
- “整句删除/整段改写”反例必须因空 replacement、核心字符数不等或单项范围过大而回退；异常断行与重复标点正例只改空白/标点并通过；
- 任一块失败会停止后续调用并让整章继续显示原文；
- 取消、任一块失败或配置变化后没有可命中的 `Completed` 条目，也不刷新错误书籍/章节；
- 原文哈希、模型或 Prompt 版本变化会缓存失效；API Key 变化不会把密钥写入缓存键；
- AI 关闭、非在线文字书、`isAiPlainText` 判定失败的章节不调用 Provider；普通 `<`/`>` 数学文本不被误判为 HTML，URL/数字等哨兵值/数量/顺序不变；
- `BookHelp.clearCache(book)` 会清除该书 AI 缓存；单独清 AI 缓存不影响原文缓存；
- 保存测试密钥后，书架和设置备份/导出、日志、异常、缓存键和元数据均不包含密钥字面量；
- 现有替换净化仍先执行，关闭 AI 后输出与基线一致；
- `:app:compileAppDebugKotlin` 与 `:app:testAppDebugUnitTest` 通过；发版前 `:app:lintAppDebug` 通过。

### 11.2 真机验收

1. 普通章节开启后先读原文，处理完成只提示就绪；用户主动切换后从章首显示完整 AI 正文，过程中不自动跳页；
2. 防盗替换样本得到修复，正常专名、数字和情节不被改写；
3. 超长章节分多块处理，界面进度、顺序、边界和最终正文正确，无跨块吞字/重字；
4. 断网、认证失败、超时、切章、退后台均不白页、不串章、不无限重试；
5. 重进章节命中缓存，不重复产生调用；“重新处理”只刷新 AI 结果；
6. 查看原文、关闭本书开关、关闭总开关均立即生效；
7. 漫画、图片章节、本地书、导出、搜索和朗读保持现状；
8. 清理 AI 缓存后原始章节仍可正常阅读；清理书籍缓存和删除书籍不会遗留该书 AI 缓存；
9. 恢复书架备份后逐书开关可以恢复，但本设备未重新确认当前服务地址前绝不发送正文；
10. HTTP 本机/局域网地址必须二次确认，跨主机重定向不携带 API Key。

## 12. 风险与控制

| 风险 | 控制 |
| --- | --- |
| AI 改写原意或吞文 | 模型只提局部编辑、原文锚点校验、文本编辑等核心字符数且单项限长、空白/标点单独限域、整章失败回退 |
| 上下文有限 | 正文块严格分区 + 200 code point 独立只读尾部 + 不累积会话历史 |
| 重复计费 | 独立缓存 + 单飞去重 + 并发 1 + 第一版不预处理下一章 |
| 阅读卡顿/位置漂移 | 先显示原文，后台处理；完成只提示，用户主动从章首切换显示源 |
| 切章后串内容 | 任务绑定书/章/输入哈希，协程取消，落盘前再次核验身份 |
| 正文和密钥泄露 | 最小上传、按设备+地址确认、Keystore 包装、HTTPS 默认、禁止备份/日志/跨主机认证重定向 |
| 特殊格式被破坏 | 第一版只收纯文本 `BookContent`；图片/HTML 等结构化标记整章拒绝，URL/数字走请求内哨兵硬校验 |
| 缓存膨胀 | `cacheDir` 可由系统回收 + 按章/书/全部清理；第一版不自建 LRU |
| 与导出/搜索/朗读不一致 | 第一版明确仅阅读显示；后续依据使用反馈再统一语义 |

## 13. DSH 协作审查记录

- 2026-09-03，Codex 通过本机 DSH `0.1.1-rc.2` 建立独立只读审查会话 `session-14c5afc2-6311-4b1f-a564-efc6ef4529e2`；实际目标为 `deepseek-official / deepseek-v4-flash-vision-exp`。
- 发送范围只有本功能书和 6 条只读代码事实摘要；未发送密钥、签名材料、真实书源、用户正文或私人数据，未授权 DSH 使用工具、改文件、提交、推送或发布。
- DSH 给出 `not_ready`，主要指出显示源未拍板、分块拼接未完全定义、密钥/备份边界缺失、占位符策略矛盾、部分缓存与阅读位置语义不清及第一版范围过大。
- Codex 独立对照 `ReadBook.contentLoadFinish*`、`ContentProcessor`、`BookContent`、`ChapterProvider`、`Book.ReadConfig`、`BookHelp.clearCache` 与备份路径后采纳上述实质问题；同时纠正 DSH 对显示源和占位符生命周期的推断：`ChapterProvider` 直接接收 `BookContent`，特殊格式临时占位会在 `ContentProcessor` 返回前恢复。未采纳“自动剥离回显上下文”，因为合法正文可能重复相同短语，自动裁剪会制造新的吞文风险。
- DSH 二次复审确认首轮 5 个阻塞项已关闭，并继续发现“哨兵/切块顺序、纯文本谓词、15% 核心字符容差”3 个漏洞；据此将协议收紧为“整章先哨兵化、模型只提局部锚定编辑、本机校验并应用”，消除整句删除和整段重写入口。
- 第三次定向复审确认哨兵顺序与删除防护已关闭，只剩 HTML 正则会误拒 `a < b > c`；修正规则并加入固定正反例后，DSH 最终返回 `ready`，确认可从阶段 A 开始。

## 14. 开工前唯一入口

本计划书只完成需求与风险预处理，不代表已开始编码。正式开工时从“阶段 A：契约与反例测试”开始，先形成失败样本和 JVM 测试，再实现 Provider；不得先把真实 AI 请求直接接入阅读页。
