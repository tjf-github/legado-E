# 发布推送流程书（供各窗口 agent 共读）

> 目的：把「代码改动 → 发布 beta 更新包」的完整流程固化下来，任何窗口的 agent 照此执行即可，无需重复踩坑。
> 更新时间：2026-08-16（依据本次相册排序修复的实际发布过程整理）。

---

## 0. 一句话结论

- **开发分支**：`codex/manga-import`（日常改动提交在这里）。
- **发布分支**：`main`（push 到 `main` 即触发 CI 自动发版）。
- **发版通道**：App 内更新检查读 `https://api.github.com/repos/tjf-github/legado-E/releases/tags/beta`，即 **beta 预发布**。
- **触发方式**：`git push origin main` → GitHub Actions `Test Build`（`.github/workflows/test.yml`）自动构建 APK 并发布/更新 beta 预发布（tag `beta`，`prerelease: true`）。

---

## 1. 环境事实（先记牢，别用错）

| 项 | 值 |
| --- | --- |
| 工作目录 | `D:\vsproject\legado-E` |
| 远程 `origin` | `https://github.com/tjf-github/legado-E.git` |
| 远程 `upstream` | `https://github.com/Luoyacheng/legado-E.git` |
| 开发分支 | `codex/manga-import`（已跟踪 `origin/codex/manga-import`） |
| 发布分支 | `main` |
| Gradle 用户目录 | `D:\gradle_home`（build.ps1 里固定；`C:\Users\huixu\.gradle` 也存在） |
| 编译任务 | `:app:compileAppDebugKotlin` |
| 单测任务 | `:app:testAppDebugUnitTest`（JVM 单测，CI 发版门禁之一） |
| 打包任务 | `:app:assembleDebug`（本地）/ CI 用 `assembleAppRelease` |
| flavor | `app` |
| 版本号 | 时间戳 `3.YY.MMDDHH`（小时精度，同一小时内两次发版会撞 tag，见维护计划书待办） |

---

## 2. 沙箱限制与提权（⚠️ 最容易踩坑，务必先读）

默认沙箱是 **workspace-write**：只允许写 `D:\vsproject\legado-E` 工作区。以下操作会被拦截，**必须用 `sandbox_permissions: danger-full-access` 重试一次**：

| 被拦截的操作 | 现象 | 解决 |
| --- | --- | --- |
| 工作区内**删除/改名文件**（unlink） | `git checkout`/`git clean` 报 `unable to unlink ... Invalid argument`；`Remove-Item` 报 `Access denied` | 切分支、清理工作树、`git clean` 前先提权 `danger-full-access` |
| Gradle 写用户目录 | wrapper 报 `FileNotFoundException: ...\gradle-8.14.4-bin.zip.lck (拒绝访问)` | 跑任何 `gradlew` 命令都要提权 `danger-full-access` |
| Git 推送/拉取（走 HTTPS 凭据库） | `schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS` | `git push`/`fetch`/`ls-remote` 提权 `danger-full-access`；若提权后仍报 schannel 错，再加 `-c http.sslBackend=openssl` |

**注意点**：
- PowerShell 把 git 写到 stderr 的正常输出当成错误，会显示 `NativeCommandError` 和 `[exit code: 1]`。**不要只看 exit code**，要看实际输出行：出现 `xxxx..yyyy  main -> main` 或 `Switched to branch ...` 就是成功。
- 每次提权是一次性的（针对那一条命令），下一条需要再带 `sandbox_permissions`。
- **git 网络操作可靠配方（2026-08-16 实测）**：本机 SChannel 后端不可用，仅提权仍可能报 `SEC_E_NO_CREDENTIALS`。可靠写法 = 提权 `danger-full-access` **再加** `-c http.sslBackend=openssl`，例：`git -c http.sslBackend=openssl push origin main`。openssl 解决 TLS 握手，提权解决凭据助手（Git Credential Manager）启动被沙箱拦。

---

## 3. 普通改动提交（改 bug / 加功能后）

```powershell
cd D:\vsproject\legado-E
git status -sb                       # 确认在 codex/manga-import、工作区状态
git add <具体文件>                   # 或 git add app/src/... 指定目录
git commit -m "fix(manga): ..."      # 中文提交信息用简洁纯文本（见 §6 踩坑）
git push                             # 分支已跟踪 origin/codex/manga-import
```

> 提交信息规范沿用仓库习惯：`fix(...)` / `feat(...)` / `docs` / `chore` / `test` 等。

---

## 4. 发布流程（push 更新到 release）

### 4.1 前提检查

```powershell
git -C D:\vsproject\legado-E status -sb
git -C D:\vsproject\legado-E log --oneline codex/manga-import -5
```

确认：
- 开发分支上的改动已提交并 push 到 `origin/codex/manga-import`；
- 本地**完整门禁**已过：CI `Test Build` 的 build job 实际跑 `testAppDebugUnitTest` + `lintAppDebug` + `assembleAppRelease`；
  本地至少补跑 `:app:testAppDebugUnitTest` 与 `:app:lintAppDebug`（都需提权），别只跑 compile+test 就发版。

### 4.2 合并并推送（核心步骤）

```powershell
$root = "D:\vsproject\legado-E"

# 1) 同步本地 main 到最新 origin/main
git -C $root fetch origin main
git -C $root checkout main
git -C $root merge --ff-only origin/main

# 2) 把开发分支合并进 main；冲突一律取 codex 侧（-X theirs）
git -C $root merge codex/manga-import -X theirs -m "Merge branch codex/manga-import into main"

# 3) 推送 main → 触发 CI 发版
git -C $root push origin main
```

> `-X theirs` 的含义：`git merge codex/manga-import` 时「theirs」= codex 侧。合并 codex → main 时冲突文件（一般是 `MangaFolderScanner.kt`、`MangaFolderScannerTest.kt`、`维护计划书-后续修复与增强.md` 这类两边都改过的）取 codex 版本即可，因为 codex 是 main 的延续，内容更新。
>
> 合并前可用 `git merge-tree --write-tree origin/main codex/manga-import` 预览是否有冲突、冲突哪些文件。

### 4.3 验证

```powershell
git -C $root diff --stat codex/manga-import main          # 应为空（合并后两分支内容一致）
git -C $root ls-remote origin refs/heads/main             # 远程应指向新的 merge commit
```

然后到 GitHub 网页确认（`gh` CLI 本机未安装）：
1. **Actions** 页 → `Test Build` 是否绿（push 到 main 自动触发）。
2. **Releases** 页 → `beta` 预发布是否更新为新 APK（`legado_测试版_3.YY.MMDDHHMM_release.apk`）。
3. 手机 App 内「检查更新」应能看到新版本（读 `releases/tags/beta`）。

### 4.4 收尾

```powershell
git -C $root checkout codex/manga-import   # 切回开发分支，保持工作区在开发分支上
git -C $root status -sb                    # 应干净、与 origin/codex/manga-import 同步
```

---

## 5. beta 预发布 vs 正式版

CI（`test.yml` 的 `prerelease` job）依据**本次 push 的 commit 是否改了 `app/src/main/assets/updateLog.md`** 决定：

| 条件 | 结果 |
| --- | --- |
| `updateLog.md` **未改动** | 发布/更新 **beta 预发布**（tag `beta`，`prerelease: true`）——日常更新走这条 |
| `updateLog.md` **有改动**（顶部新增 `**20xx/xx/xx**` 条目） | 发布 **正式版**（tag = 版本号，`prerelease: false`） |

> 想发正式版：先往 `updateLog.md` 顶部加更新日志条目 → 提交合并到 main → push。需要时让 agent 代改。

---

## 6. 踩坑记录（别的 agent 直接照着规避）

1. **PowerShell 中文提交信息被拆散**：`git commit -m "…"` 里含中文引号 `“”`、`<` 等，参数会被拆成多个导致 `pathspec '100'` 报错。规避：用简洁纯 ASCII/纯文本，或写临时文件 `git commit -F .git\COMMIT_MSG_TMP.txt`。
2. **切分支时文件删不掉**：默认沙箱拦截工作区 unlink。切 `main`/`codex`、`git clean -fd` 都要先提权 `danger-full-access`。
3. **切分支失败后工作树会「半切换」**：目标分支 HEAD 已切过去，但旧分支独有文件残留成 `??` untracked，且 `release.yml` 等可能 `M`。恢复步骤：
   - 先备份本地专属文件（见下条）；
   - `git checkout -- .` 恢复已跟踪文件；
   - `git clean -fd` 清掉 untracked（codex 独有文件都能从 codex 分支找回）。
4. **本地专属文件（git 忽略、不在任何分支）务必先备份再 `git clean`**：
   - `app/release.keystore`（`*.keystore` 被忽略）
   - `keystore.properties`、`keystore-base64.txt`（被忽略）
   - `build.log`（被忽略，可丢弃）
   > 备份到 `C:\Users\huixu\AppData\Local\Temp\legado-release-backup\`，清理完再 `Copy-Item` 回来。
5. **Gradle 必须带 `GRADLE_USER_HOME`**：`$env:GRADLE_USER_HOME = "D:\gradle_home"`（build.ps1 已处理），且跑 gradlew 要提权。
6. **`git ls-files --others` 中文路径会带引号/八进制转义**：判断文件是否在 codex 分支用 `git -c core.quotepath=false ls-files --others --exclude-standard -z` 拿裸路径再 `git cat-file -e "codex/manga-import:$p"` 校验。
7. **CI 门禁比本地多 lint**：`test.yml` build job 跑 `testAppDebugUnitTest lintAppDebug assembleAppRelease`；发版前本地漏跑 lint，若 lint 失败 CI 直接红，要多等一轮构建。发版前提检查已含 lint（见 §4.1，2026-08-20 曾踩）。

---

## 7. 快速命令速查

```powershell
$root = "D:\vsproject\legado-E"
$env:GRADLE_USER_HOME = "D:\gradle_home"

# 本地编译 / 单测（均需提权 danger-full-access）
.\gradlew.bat -p $root :app:compileAppDebugKotlin --console=plain
.\gradlew.bat -p $root :app:testAppDebugUnitTest --tests "io.legado.app.model.localBook.MangaFolderScannerTest" --console=plain
.\gradlew.bat -p $root :app:lintAppDebug --console=plain   # 发版前补跑（CI 门禁含 lint）

# 看分支/状态/远端
git -C $root branch -a
git -C $root status -sb
git -C $root -c http.sslBackend=openssl ls-remote origin refs/heads/main refs/tags/beta   # 提权 + openssl

# 预览合并冲突（不动工作树）
git -C $root merge-tree --write-tree origin/main codex/manga-import
```
