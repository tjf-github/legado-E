# 优化预处理计划书

> 状态：**预处理阶段（2026-08-20）**
> 定位：个人维护版的长期优化候选池，不承担日常 bug 修复和用户反馈闭环。
> 前置结论：当前版本已完成真机回归，功能符合预期；在没有新的明确需求前，以稳定为第一目标。

## 1. 计划目的

本计划书用于在“功能暂时冻结”期间，集中记录和评估以下事项：

- 构建速度、CI 和发布流程优化；
- 测试覆盖、回归效率和可观测性优化；
- 代码重复、工程债务和可维护性优化；
- 只有经过测量后才值得做的性能优化；
- 后续可能需要但当前不应贸然实施的结构调整。

本文件只做预处理和排期，不代表其中事项都要执行。

## 2. 与维护计划书的边界

| 文档 | 负责内容 |
| --- | --- |
| 《维护计划书-后续修复与增强.md》 | 真机反馈、实际 bug、明确的新功能、用户侧行为问题 |
| 本文件 | 技术优化、工程债务、性能测量、重构候选、执行前置条件 |
| 《构建优化书.md》 | 既有的 Gradle/构建专项方案和历史执行记录，作为本文件的参考资料 |

判断标准：用户实际遇到的问题进入维护计划；没有用户症状、但可能降低长期成本的事项进入本文件。

## 3. 项目全面评估基线

> 本节是多 agent 接手前的共同上下文。除非“基线刷新条件”被触发，后续 agent 不必重新扫描整个项目。

### 3.1 基线身份

- 基线日期：2026-08-20；
- 基线提交：`bbc220c63`；
- 开发分支：`codex/manga-import`；发布分支：`main`；
- 项目定位：基于 Legado 的个人维护 fork，原上游功能维护已中断，当前以个人使用需求为最高优先级；
- 当前阶段：主要功能已经完成，最新书架分组内多选功能已通过用户真机验证，进入稳定维护和优化预处理阶段。

### 3.2 工程结构

| 部分 | 作用 | 当前判断 |
| --- | --- | --- |
| `app` | Android 主应用，包含 UI、数据库、阅读器、书源、导入和书架等核心功能 | 主体模块，约 879 个源码文件，其中 Kotlin/Java 约 868 个 |
| `modules:book` | EPUB/电子书相关底层能力 | 独立 Android library，约 130 个源码文件 |
| `modules:rhino` | JavaScript/Rhino 执行能力 | 独立 Android library，约 32 个源码文件 |
| `modules/web` | Web 前端资源和构建产物 | 不在 `settings.gradle` 的 Android module 列表中，由独立 Web workflow 构建/同步 |
| `app/src/main/assets` | 更新日志、Web 资源、规则及其他运行时资源 | 发布和运行时均有依赖，清理前必须确认引用 |
| `app/src/main/schemas` | Room schema 输出 | 数据库变更时必须同步评估迁移风险 |

主要构建事实：Android namespace 为 `io.legado.app`，minSdk 21、targetSdk 36，Java/Kotlin 使用 JDK 17，产品 flavor 为 `app`，Debug/Release 使用不同 application suffix。

### 3.3 功能版图

- **在线阅读**：书源、搜索、发现、章节目录、阅读器、换源、净化和更新；
- **本地阅读**：TXT/EPUB 导入与阅读，目录规则和本地文件处理；
- **本地漫画**：文件夹/相册导入，整本/系列预览，图片自然排序，搜索和分组，漫画阅读器；
- **目录编辑**：章节增删、批量拆分、合并、撤销拆分、目录多选删除和事务化更新；
- **书架管理**：多种书架布局、分组、自然排序、六种排序方式、全局反序、分组内多选删除/移组；
- **扩展能力**：RSS、音频、视频、Web API/Web 服务、主题和阅读配置等原有能力；
- **发布更新**：GitHub Actions 构建 Debug 测试与 Release APK，beta 通过 `beta` tag 提供更新检查。

### 3.4 数据与行为边界

- 书籍和章节主要由 Room 数据库管理；书籍分组采用正整数位掩码；
- 当前约束是不新增表、不新增列，除非未来明确立项并设计迁移方案；
- 漫画章节删除默认只移除目录记录、保留源文件；文件夹型漫画重新扫描可能恢复章节；
- 本地 TXT/EPUB 导入与阅读路径属于高敏感区域，优化不得顺带改变其语义；
- 删除书籍涉及数据库记录、缓存、封面、源文件和在线书源回调，属于高风险操作；
- 书架多选状态以 `bookUrl` 为键，不能改回依赖 `Book` 对象相等性；
- Fragment 内不能假设 `GroupSelectDialog` 能取得 Activity callback，分组页现有内联分组选择逻辑是有原因的。

### 3.5 验证与质量现状

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| JVM 单测 | 89 个通过 | 覆盖章节数字、拆分、排序、漫画扫描、GSON 时间类型等纯逻辑 |
| Android instrumentation tests | 8 个源码用例 | 包含 Room、迁移、HTTP、JS 等；本次未由 CI 自动执行 |
| Kotlin 编译 | 通过 | `:app:compileAppDebugKotlin` |
| lint | 0 errors | 约 926 条 warning、21 条 hint，多数为历史工程债务 |
| Debug 打包 | 通过 | 已生成 Debug APK |
| 最新功能真机验证 | 通过 | 书架分组内多选、删除、移组等效果符合预期 |
| Release CI | 已配置 | `testAppDebugUnitTest` + `lintAppDebug` + `assembleAppRelease` |

### 3.6 已知风险和工程债务

- 版本号本地兜底仍按小时生成，更新比较逻辑还存在精度不足的已知风险；
- CI 仍使用 `richardsimko/update-tag@v1`，存在 Node 20 弃用风险；
- `test.yml` 同时声明了 `push`、`pull_request`、`workflow_run` 和 `workflow_dispatch`，与部分项目文档中“不是 workflow_dispatch”的描述不完全一致；
- 书架多选在 style1/style2 存在重复 UI/业务编排，当前功能稳定但维护成本较高；
- 选择模式使用 `notifyDataSetChanged`，大书架列表可能存在不必要的刷新成本；
- Gradle 配置存在旧式 Groovy 赋值语法 warning；
- lint warning 数量较多，暂不等同于当前功能缺陷，应按风险分类处理；
- instrumentation tests 尚未纳入 CI 自动门禁，真机回归仍是个人手工验证。

### 3.7 多 agent 接手规则

每个 agent 开始任务前按以下顺序读取：

1. 本文件第 3 节，确认项目基线、边界和已知风险；
2. `AGENTS.md`，确认分支、构建、提交和本地专属文件规则；
3. 《维护计划书-后续修复与增强.md》，确认是否已有相同反馈或决策；
4. 与任务直接相关的计划书和模块代码注释；
5. `git status -sb` 与 `git log --oneline -3`，确认其他窗口是否已推进仓库。

任务分类：

- 新 bug、真机反馈、明确功能需求 → 维护计划书；
- 构建、CI、性能、测试和重构 → 本文件；
- 只做评估 → 不修改代码、不顺手重构；
- 需要改代码 → 先写复现测试或明确验证路径，再实施最小改动。

交接时至少说明：改动文件、行为变化、验证命令、真机情况、未解决风险和是否需要后续 agent 接手。

### 3.8 基线刷新条件

出现以下任一情况时，应重新评估本节，而不是继续沿用旧结论：

- 主分支或开发分支发生较大范围合并；
- 新增/删除 Android module、修改 Gradle、CI 或发布逻辑；
- 修改数据库 schema、导入链路、阅读器或书架核心逻辑；
- 连续新增多个功能或出现新的崩溃/数据异常；
- 当前基线与 HEAD 相差约 20 个以上提交；
- 用户明确要求“重新评估项目”。

未触发刷新条件时，agent 只需针对任务范围检查变更点和关联调用方。

## 4. 当前稳定基线

- 分支：`codex/manga-import`；工作区只保留本地未跟踪 `look.ps1`，不纳入项目改动；
- 最新主线能力：本地漫画导入、目录章节编辑、拆分/合并、书架分组、自然排序和反序、分组内多选操作；
- 最新书架多选功能已完成真机验证，效果符合预期；
- JVM 单测：89 个通过；
- `:app:compileAppDebugKotlin`：通过；
- `:app:lintAppDebug`：0 errors，仍有约 926 条历史/累计 warning；
- `:app:assembleAppDebug`：通过，可生成 Debug APK；
- 数据库约束：不新增表、不新增列；本地 TXT/EPUB 和阅读路径保持零侵入；
- 当前策略：不为“看起来更先进”而主动改变已经稳定的交互和数据语义。

## 5. 优化原则

1. 先测量，再优化；没有基线数据的性能判断不进入实施阶段。
2. 一次只处理一个主题，独立提交，保证可以单独回退。
3. 优先低风险、高收益、可验证的事项；高风险重构必须后置。
4. 优化不能改变已验证的阅读、导入、目录和书架行为。
5. 每项优化至少通过编译、JVM 单测和 Debug 打包；涉及 UI 或数据行为时补真机回归。
6. 个人项目不追求形式上的工程复杂度；如果收益无法体感或量化，可以不做。

## 6. 预处理阶段清单

### 6.1 先建立可比较的基线

后续做性能或构建优化前，记录以下数据，避免“优化后感觉更快”的主观判断：

- 无改动增量构建耗时；
- 修改一个 Kotlin 文件后的增量编译耗时；
- `assembleAppDebug` 干净构建耗时；
- 应用冷启动、打开书架、打开阅读器、打开漫画和滑动长目录的大致耗时；
- 大批量导入、目录拆分/合并、书架批量操作的完成时间和内存表现；
- APK 大小、安装后占用和升级成功情况。

### 6.2 先区分问题类型

- **用户可感知**：卡顿、崩溃、数据错误、升级失败，转入维护计划，优先处理；
- **构建/发布成本**：构建慢、CI 不稳定、版本识别不可靠，进入工程优化；
- **代码维护成本**：重复逻辑、难以测试、模块边界不清，先记录，不立即大改；
- **纯洁癖问题**：格式、旧 warning、个人偏好，除非顺手且低风险，否则暂缓。

## 7. 优化候选池

### P0：稳定性和可恢复性

- [ ] 给当前真机验证版本创建稳定标签或本地归档；
- [ ] 保存对应 APK、版本号、测试设备和测试结论；
- [ ] 确认签名文件、密钥配置和源码备份可恢复；
- [ ] 任何优化开始前保留一份可直接使用的稳定版本。

### P1：低风险工程优化

- [ ] 统一 CI 触发规则与项目文档：当前 workflow 同时存在 `push` 和 `workflow_dispatch`，需要明确是否保留手动触发；
- [ ] 替换 `richardsimko/update-tag@v1`，消除 Node 20 弃用风险；
- [ ] 处理版本号比较精度问题，避免同一时间窗口内版本相等导致漏更新；
- [ ] 分批处理 Gradle 8.14.4 报出的 Groovy 旧式赋值语法 warning；
- [ ] 为新增书架多选的纯逻辑补 JVM 测试，尤其是选择、全选、反选、分组位掩码和空选择；
- [ ] 评估 `BooksFragment` 与 `BookshelfFragment2` 中重复的多选/分组弹窗逻辑，只有在行为稳定后再抽共享组件。

### P2：有数据再做的优化

- [ ] 根据构建基线决定是否继续拆分模块；
- [ ] 根据真机卡顿记录检查书架列表刷新、漫画图片加载和大目录操作；
- [ ] 根据 lint warning 分类清理真正影响可访问性、国际化或运行安全的项目；
- [ ] 评估是否需要增加少量 instrumentation smoke test，并只覆盖高价值路径。

### P3：暂不建议主动启动

- [ ] 一次性拆分 `:app` 大模块；
- [ ] 改动数据库结构或迁移已有数据；
- [ ] 为了统一风格重写稳定的 UI；
- [ ] 没有实际性能数据时调整图片、阅读器或网络核心链路；
- [ ] 为个人维护引入复杂的 issue、发布和自动化体系。

## 8. 建议路线

| 阶段 | 内容 | 进入条件 |
| --- | --- | --- |
| 冻结期 | 建立稳定标签、保存 APK、记录真机基线 | 当前版本确认可长期使用 |
| 低风险期 | 处理 CI/版本号/测试补齐等工程问题 | 有明确收益且可独立验证 |
| 测量期 | 采集构建和真机性能数据 | 出现慢、卡、耗时等实际症状 |
| 结构期 | 共享组件抽取、模块拆分等重构 | 前述优化无法解决问题，且有回退方案 |

## 9. 单项优化验收模板

每项实际启动前补充以下内容：

- 问题/目标：
- 当前基线：
- 预期收益：
- 影响范围：
- 是否涉及数据库或阅读路径：
- 验证命令：
- 真机验证场景：
- 回退方式：
- 结论：保留 / 回退 / 暂缓

## 10. 当前决策

- 当前版本先作为稳定基线保存，不主动进行大规模重构；
- 新功能继续写入维护计划书，本文件只接收优化类事项；
- 没有用户症状或可量化收益的优化，默认暂缓；
- 后续若原项目恢复维护，本项目仍保持个人 fork 的独立演进，不以兼容上游为当前目标。
