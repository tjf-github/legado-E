# 第三期计划书：批量拆分粘连章节（含中文数字重写与工程保障）

> 状态：**首版开发完成（2026-08-12）**：编译、单测、Debug 打包全部通过；androidTest 已写好并编译通过，待真机回归执行
> 提交记录：工程保障 a66316548 / cdffe5ad0 · 工具层 c8da1ff00 · 主流程 73964bece · UI a4e32a6bb · 测试 59e40883d
> 定位：第三期 = 方向 A（批量拆分粘连章节）＋ 方向 C（中文数字标题重写）＋ 方向 F（工程质量补齐）
> 第四期预留：方向 B（多选合并）、方向 D（在线书转本地引导）、方向 E（漫画侧增强）
> 依赖：第一期（目录新增/删减章节）已交付，本计划基于其插入/重排/覆盖文件机制扩展

## 1. 背景与目标

### 背景

- 粘连章节（一个 URL 对应多章）根因在书源/抓取层，一期已提供单章插入/删除能力，但用户仍需要把粘连的一章按标题规则**批量拆成多章**；
- 一期标题重写仅支持阿拉伯数字，中文数字标题（“第一百章”）会被跳过、只重排 index，拆分会放大该问题；
- 一期的插入/删除未事务化（顺序执行、无回滚），批量操作失败会留下半完成状态；构建基线当前中断，开发无法验证；
- 现状盘点与优先级依据见《第三期预处理-现状盘点与候选方向.md》。

### 目标（第三期）

1. 本地 TXT 书籍目录页支持**批量拆分粘连章节**：选择章节 → 按标题规则预览拆分结果 → 确认后拆成多章，后续章节级联重排；
2. 标题重写支持**中文数字**（“第N章/回/节”、“Chapter N”），拆分后自动重编号；
3. 消除对第三期影响最大的工程缺口：干净构建基线、目录编辑**事务化＋回滚**、核心逻辑**单测**。

## 2. 范围

### 本期包含

- 本地 TXT 书籍目录页“拆分此章节”入口（长按菜单，与一期新增/删除同组）；
- 拆分规则（首版固定标题模式，见 4.4）与拆分预览确认（章节清单、行级勾选/改名）；
- 执行拆分：批量插入 + 级联重排 + 覆盖文件写入/改名 + 书签/阅读进度同步；
- 中文数字标题重写（扩展 `ChapterNumberUtils` + 单测）；
- 工程保障（第 0 步，最高优先级）：
  - 干净构建基线（清缓存重编、现有单测通过）；
  - 一期 insert/delete 事务化与失败回滚；
  - 核心纯逻辑单测（中文数字、拆分）；
  - 记录旧功能疑似问题（新增章节默认标题编号），**本期不修改**（见 4.0.1）；
  - 工作区卫生（未跟踪文件收编/清理）。

### 本期不包含（第四期）

- 多选合并冗余章节（复用本期抽取的批量重排核心）；
- 在线书“转本地”操作引导；
- 漫画侧增强（单本相册导入、元数据识别、混合结构自动化、统一阅读设置面板、大目录懒加载/分页）；
- EPUB / MOBI / UMD / PDF 的章节编辑；
- 在线书（离线缓存态）的章节编辑。

## 3. 现状与依据（代码事实）

| 关注点 | 现状 | 位置 |
| --- | --- | --- |
| 单章插入/删除 | 已实现，顺序执行、**无事务无回滚** | `ui/book/toc/TocViewModel.kt:81 / 144` |
| 标题重写 | 仅阿拉伯数字（第N章/回/节、Chapter N）；中文数字返回 null | `help/book/ChapterNumberUtils.kt` |
| 正文读取 | 覆盖文件优先；无覆盖回退原文件 start/end 偏移 | `help/book/BookHelp.kt:421`、`model/localBook/TextFile.kt` |
| 正文写入/删除 | `saveText` / `delContent` 已具备 | `help/book/BookHelp.kt:177 / 447` |
| 书签同步 | `shiftChapter` / `delByChapterIndex` 已具备 | `data/dao/BookmarkDao.kt` |
| DAO 事务 | 均为纯接口、无 `@Transaction` | `BookChapterDao.kt`、`BookmarkDao.kt` |
| 目录 UI | 长按菜单（新增/删除）已具备，`isLocalTxt` 校验已具备 | `ui/book/toc/ChapterListFragment.kt:229` |
| 测试基建 | JVM 单测 + `androidTest`（MigrationTest 等）已存在 | `app/src/test`、`app/src/androidTest` |
| 构建基线 | `build.log` 最近一次失败（Gradle 缓存反序列化）；`.gradle_home/` 已清理并忽略 | 仓库根目录 |
| 工作区 | `build.log`、`watch-build.ps1`、`make_test_book.py`、`test_book.txt`、`测试漫画素材/` 未跟踪 | 仓库根目录 |

## 4. 功能设计

### 4.0 工程保障（第 0 步，最高优先级）

按对第三期的影响排序，**完成后再进入功能开发**：

1. **干净构建基线**（已完成 ✅）：清理 Gradle 缓存（`.gradle`、重建 `.gradle_home`）→ `.\gradlew.bat :app:compileAppDebugKotlin` 通过 → 现有单测（`ChapterNumberUtilsTest`、`MangaFolderScannerTest`）通过；注意：实际编译任务名为 `compileAppDebugKotlin`（计划书早期写法的 `compileLegadoDebugKotlin` 已废弃）；
2. **事务化改造**（已完成 ✅，提交 a66316548）：
   - `BookChapterDao` 新增 `applyTocEdit`：利用 Room 接口默认方法 + `@Transaction`（项目已有先例 `BookDao.replace`），把章节删除/重排/插入、书签删除/迁移、书籍元数据更新封装为单事务提交，失败自动回滚；
   - 插入/删除主流程改为“先准备正文文件（写入/删除/改名，失败仅告警），再单事务提交数据库变更”，不再逐条写库；
   - 受影响章节/书签清单在执行前收集在内存中（即事务的输入，兼作操作记录）；
3. **测试补齐**：
   - 纯 JVM（优先）：中文数字重写（进位/临界/混合）、拆分纯逻辑（标题定位、前言、空单元、上限）；
   - `androidTest`（功能之后补）：`BookDao` 分类查询、`Book.config` 序列化、DAO 事务流程；
4. **工作区卫生**：收编或清理未跟踪文件，确认 `modules/web` 三个生成文件的归属。

### 4.0.1 旧功能修复谨慎项（仅标记，本期不修改）

> ⚠️ **标记（2026-08-12）→ 已修复（2026-08-12 四期 4.1）**：一期 `insertChapter` 的空白标题回退编号疑似 off-by-one。
>
> 事实：新章节实际 `index = anchor.index + 1`，但两处兜底默认标题均为 `anchor.index + 2`：
> - `ChapterListFragment.showInsertChapterDialog`（预填，锚点标题无编号模式时）；
> - `TocViewModel.insertChapter`（用户清空标题时）。
>
> 触发路径：仅“锚点标题无编号模式（如“序章”“番外”）”或“用户清空标题输入框”两条冷路径；正常“第N章”锚点由 `nextNumberTitle` 正确预填。
>
> **处理决定（2026-08-12）**：经确认预期编号为新章节实际 `index = anchor.index + 1`，已修复（`insertChapter` 与对话框默认标题均改为 `anchor.index + 1`）。

### 4.1 交互流程

**入口**：目录页长按章节 → 菜单新增“拆分此章节”（仅本地 TXT，与一期新增/删除同一入口组）。

**拆分对话框**：

- 展示当前章节标题与规则说明（首版固定规则，见 4.4）；
- 显示预估拆分章节数；
- 确认后进入**预览清单**：每行一个新章节（标题、正文首行片段、字数），行级可勾选/取消（跳过误命中的拆分点）、可改名；
- 确认执行：批量写覆盖文件、插入章节、后续级联重排、刷新目录并 toast。

### 4.2 拆分主流程（split）

入参：`book`、目标章节 D（`index = i`）。

1. 读取 D 的有效正文：`BookHelp.getContent`（覆盖文件优先，无覆盖走 TextFile 偏移）；
2. 用标题规则定位全部标题位置，生成拆分单元列表（标题 + 正文片段）；首个标题之前的内容为前言（默认并入第一章，待确认项 1）；
3. 预览调整（勾选/改名）后确认；
4. 执行（DB 部分整体事务）：
   - 拆分单元 ≥ 2：D 改写为第一章（标题、正文覆盖文件重写），其余单元作为新章插入 `i+1`、`i+2`…；
   - 批量级联：D 之后所有章节 `index` 右移（拆分单元数 − 1），**标题保留原文不改写**（拆出的标题来自原文，续章不重编号，保证标题序连续），覆盖文件改名，书签同步；
5. 更新 `Book`：`totalChapterNum += 拆分单元数 − 1`；`durChapterIndex > i` 时右移保持当前阅读内容不变；
6. `ReadBook.onChapterListUpdated(book)` + 目录刷新。

### 4.3 边界规则

- 仅定位到 1 个标题（无可拆点）：提示并退出，不执行；
- 单次拆分单元数上限（默认 500，待确认项 3），超出提示分批；
- 正文超长：IO 线程执行 + 进度提示；
- 空正文/空单元：跳过并在预览中标记；
- 标题重复（同规则同标题）：保留原样，允许预览中改名。

### 4.4 标题规则（首版）

- 复用并扩展 `ChapterNumberUtils` 模式：
  - `第(\d+|[一二三四五六七八九十百千万零两]+)[章回节]`
  - `Chapter\s*(\d+)`（忽略大小写）
- 首版**不做自定义正则 UI**（避免正则复杂度与误配风险），固定模式 + 预览兜底（待确认项 2）。

### 4.5 中文数字重写（方向 C）

- 新增阿拉伯 ↔ 中文数字转换工具（1–9999，覆盖“一百零三”“两千三百”等）；
- `rewriteTitle` / `nextNumberTitle` 支持中文数字标题 +1/−1（“第一百章”→“第一百零一章”）；
- 阿拉伯数字与中文数字混排各自独立重写；
- 单测覆盖：进位（九十九→一百、九百九十九→一千）、临界（“第一章”−1 不重写）、零位（一百零九→一百一十）。

### 4.6 复用与扩展点

- 从 `insertChapter` / `deleteChapter` 抽取公共函数 `batchReindex(book, affected, delta)`：级联重排＋标题重写＋覆盖文件改名＋书签同步（**第四期 B 直接复用**）；
- 新增纯函数 `ChapterSplitter.split(content, rules): List<SplitUnit>`（无 Android 依赖，可 JVM 单测）。

## 5. 技术方案

### 5.1 数据层改动

- `BookChapterDao` / `BookmarkDao` 改造为 abstract class（或新增聚合 DAO），新增 `@Transaction` 方法（重排＋书签＋删除一体）；
- 既有查询方法保持不变，**不加表、不加列、不改约束**。

### 5.2 业务逻辑

- 新增 `help/book/ChapterSplitter.kt`（拆分纯逻辑 + 中文数字转换依赖 `ChapterNumberUtils`）；
- `TocViewModel` 新增 `splitChapter(book, chapter)`；`insertChapter` / `deleteChapter` 重构复用 `batchReindex`；
- `ChapterNumberUtils` 扩展中文数字转换。

### 5.3 UI 层

- `ChapterListFragment`：长按菜单新增“拆分此章节”；
- 拆分对话框（规则说明 + 预估）；
- 预览清单对话框/页面（勾选、改名、确认、进度）。

### 5.4 预计改动文件

| 文件 | 改动 |
| --- | --- |
| `help/book/ChapterNumberUtils.kt` | 中文数字转换 |
| 新增 `help/book/ChapterSplitter.kt` | 拆分纯逻辑 |
| `ui/book/toc/TocViewModel.kt` | 抽取 `batchReindex` + 新增 `splitChapter` |
| `data/dao/BookChapterDao.kt` / `BookmarkDao.kt` | 事务化改造（abstract class + `@Transaction`） |
| `ui/book/toc/ChapterListFragment.kt` | 菜单 + 对话框 + 预览 |
| `res/values/strings.xml` | 文案 |
| 新增单测 | `ChapterNumberUtilsTest` 扩展、`ChapterSplitterTest` |

## 6. 边界与限制（产品层面明确）

1. 入口仅本地 TXT 显示；在线书（含离线缓存）不提供入口；
2. 拆分在“重新解析本地文件 / 重新导入”后会被重置（与一期正文编辑行为一致）；
3. 首版固定标题规则，不提供自定义正则；
4. 单次拆分数量有上限（防呆）；
5. 中文数字支持范围 1–9999，超出范围的编号不重写（只重排 index）。

## 7. 风险与规避

| 风险 | 影响 | 规避 |
| --- | --- | --- |
| 事务未提交半完成 | 目录/书签错乱 | `@Transaction` + 内存备份双保险 |
| 中文数字转换错误 | 标题被改坏 | 纯函数 + 进位/临界单测 |
| 规则误命中（正文含“第X章”字样） | 拆错章 | 预览清单可勾选，确认前可见可改 |
| 覆盖文件改名失败 | 该章回退原文件正文 | 不阻断 DB，日志告警（本地 TXT 数据仍在） |
| 大正文拆分卡顿 | 体验差 | IO 线程 + 进度提示 + 数量上限 |
| 阅读进度错位 | 跳章/重读 | 按 4.2 规则调整 `durChapterIndex/Title` 并通知阅读器 |
| 拆分后标题重复 | 目录混乱 | 预览中可见并允许改名 |

## 7.1 稳定性保障策略（沿用一、二期 7.1 原则）

1. **零侵入**：仅新增“拆分此章节”菜单与新增流程，不改动现有路径行为（目录点击、反转目录、目录规则、正文编辑、缓存下载等）；必须改动处只允许“新增调用点或抽取公共函数”，不改写原有逻辑；
2. **不迁移数据库**：不加表不加列不改约束，仅 DAO 事务化封装，无 Room 迁移风险；
3. **独立失败域**：新逻辑全部 IO 线程执行并捕获异常，失败仅 toast + 日志，不向 UI 抛异常；
4. **事务与回滚**：DB 变更事务化；执行前备份受影响章节/书签，失败自动回滚；文件改名失败仅告警；
5. **编译门槛**：每步 `.\gradlew.bat :app:compileAppDebugKotlin` 通过再继续；
6. **小步提交**：按“工程保障 → 工具/纯逻辑 → 主流程 → UI”分步提交，可独立回滚；
7. **回归测试**：第 8 节清单交付前全量执行。

## 8. 测试计划

1. 中文数字重写：进位/临界/混合/范围外（单测）；
2. 拆分纯逻辑：命中/未命中/前言并入/空单元/上限（单测）；
3. 拆分主流程：一章拆 3 章，后续章节仅 `index` 右移、标题保留（如 第1章/第2回/第3回/第4章/第5章 连续），覆盖文件内容一致；
4. 边界：第一章拆分、最后一章拆分、仅 1 个拆分点不执行；
5. 未编辑正文（无覆盖文件）拆分后内容不丢；
6. 事务失败回滚验证（中途抛错，目录/书签无半完成状态）；
7. 书签与阅读进度：拆分后归属正确、当前章不跳错；
8. 回归：一期新增/删除、反转目录、目录规则、正文编辑、二期本地漫画导入与阅读、书架分类。

## 9. 验收标准

- [ ] 本地 TXT 长按出现“拆分此章节”，在线书/EPUB 不显示；
- [ ] 中文数字标题重写正确（单测通过）；
- [ ] 拆分后目录、标题（**序号连续、后续章节不改写**）、正文、书签、进度全部正确（测试 3、4、5、7）；
- [ ] 未命中规则不执行且有提示；
- [ ] 事务化后失败不留半完成状态（测试 6）；
- [ ] 构建基线恢复：编译 + 现有单测通过；
- [ ] 回归项通过（测试 8）。

## 10. 工作量与排期

| 阶段 | 内容 | 估时 |
| --- | --- | --- |
| 0-工程保障 | 构建基线、事务化、回滚、默认标题修复、工作区卫生 | 1 天 |
| 1-工具层 | 中文数字转换、`ChapterSplitter` 纯逻辑 + 单测 | 1 天 |
| 2-主流程 | 抽取 `batchReindex`、`splitChapter` | 1~1.5 天 |
| 3-UI | 菜单 + 对话框 + 预览清单 | 1 天 |
| 4-自测与回归 | 边界、回滚、回归 | 0.5~1 天 |
| 合计 | | **4.5~5.5 天** |

> 说明：按熟悉 Kotlin 的开发实施估算；不熟悉时建议按 9~11 天排期，并保持每步编译验证。

## 11. 待确认项

> 建议均按推荐项执行，确认后进入开发。

1. 前言/引子（首个标题前的内容）：**并入第一章**（不丢内容），不另设“前言章”；
2. 首版固定标题规则、不做自定义正则 UI：**接受**，以预览清单兜底；
3. 单次拆分单元数上限：**500**；
4. 真机回归：功能开发期间由用户并行勾一、二期清单，交付前至少勾完二期 §8.1；
5. `androidTest`（`BookDao` 分类、`Book.config` 序列化）纳入本期：**纳入但排在功能之后**。

> 已标记不处理：一期新增章节默认标题编号疑似 off-by-one（见 4.0.1）——本期仅记录，不改动旧行为。

## 12. 四期预留

- 多选合并冗余章节（复用 `batchReindex`）；
- 在线书“转本地”操作引导；
- 漫画侧增强：单本相册导入（MediaStore）、元数据自动识别、混合结构自动化识别、统一漫画阅读设置面板、图片间距可调、大目录懒加载/分页。

## 13. 复盘（2026-08-12 手测前）

### 完成情况

- 交付：批量拆分粘连章节（预览勾选/改名）、中文数字标题重写、目录编辑事务化、单测与 androidTest 补齐；
- 验证：编译、36 个 JVM 单测、`assembleDebug` 打包全部通过；androidTest 编译就绪待真机执行。

### 过程中的问题与处理

1. 计划书编译任务名 `compileLegadoDebugKotlin` 实际不存在，已确认为 `compileAppDebugKotlin` 并修正文档；
2. 构建环境：`GRADLE_USER_HOME` 指向沙箱外目录需授权运行；`build.log` 的缓存损坏问题因缓存已清理未复现；
3. 事务化实现采用项目已有的接口默认方法 + `@Transaction` 先例（`BookDao.replace`），未改表结构，比计划书预写的 abstract class 方案侵入更小；
4. 中文数字转换规范化输出“二”（“两千三百”→“二千三百”），输入兼容“两/〇”；
5. 调试包 `applicationId` 为 `io.legado.app.debug`，启动命令需写完整 Activity 类名；模拟器旧签名不一致已卸载重装；
6. 中文文件名经 adb 推送乱码，漫画素材以 ASCII 镜像目录推送（`/sdcard/Download/manga/`）；
7. 遵守旧功能修复谨慎原则：默认标题 off-by-one 仅记录未改动（见 4.0.1）。

### 手测素材（已准备/核对）

- `test_book.txt`：正常五章书（负向场景：拆分时提示无可拆分）；
- `test_book_split.txt`：粘连章节书（第2回/第3回粘在第1章内，默认目录规则不识别“第N回”，可演示拆分正向路径）；
- `测试漫画素材/`（二期回归用）已镜像推送到模拟器 `/sdcard/Download/manga/`；
- 模拟器 `legado_avd` 已启动并安装最新 Debug 包，应用已打开。

### 遗留事项

- 真机回归：androidTest（事务回滚、书架分类）＋ 一、二期手动清单；
- 工作区卫生：`build.log`、`watch-build.ps1`、`make_test_book.py`、`test_book.txt`、`test_book_split.txt`、`测试漫画素材/` 均未跟踪，保留或清理待用户决定；
- 四期内容按 §12 另行立项。

## 14. 手测发现与修复（2026-08-12）

### 问题

本地 TXT 解析器的章节正文从**标题行之后**开始（start 偏移不含标题行），导致拆分器把正文内第一个标题当作首单元：

- 现象：对“第1章（内含第2回/第3回）”执行拆分，预览只出 2 个单元（第2回/第3回），确认后“第1章 初入江湖”被“第2回 山中怪影”顶掉，原章节标题丢失；
- 定位：`ChapterSplitter.split` 只基于正文内容识别标题，无法感知当前章节标题。

### 修复（提交 a991821ca）

- `ChapterSplitter.split` 新增 `firstTitle` 参数：正文首行不是本章标题时先补入（首行已是则不重复），保证原章节作为第一单元保留；
- `previewSplit` 传入章节标题；新增 3 组单测（补标题 / 不重复 / 全角空格前缀）。

### 模拟器验证（legado_avd，Debug 包）

- 导入 `test_book_split.txt`：目录解析为 3 章（第1章 3689字 含粘连的第2回/第3回、第4章、第5章）；
- 长按第1章 → 拆分此章节：预览正确显示 3 个单元（第1章/第2回/第3回）；
- 确认后目录变 5 章：第1章、第2回、第3回、第6章（原第4章 +2）、第7章（原第5章 +2）；
- 打开第2回阅读，正文正确（覆盖文件写入正常）；
- 其他：模拟器安装 Debug 包时需注意 APK 文件名带构建时间戳（如 `legado_app_3.26.081211.apk`），安装脚本应动态取文件，避免引用过期文件名。

### 第二轮反馈与修复（2026-08-12）

拆分后标题序错乱：拆出 第2回/第3回 后，后续章节被级联 +2 重写（第4章→第6章、第5章→第7章），目录变成 第1章/第2回/第3回/第6章/第7章。

- 原因：拆分复用了“手动新增章节”的级联重编号逻辑；但拆出的单元标题来自原文，后续章节标题本就连续，不应改写；
- 修复（提交见 git log）：`splitChapter` 对后续章节仅右移 `index`，标题保留原文；测试 3 与验收标准同步更新；
- 期望结果：第1章 初入江湖 / 第2回 山中怪影 / 第3回 夜宿山庙 / 第4章 尾声 / 第5章 后会有期。

## 15. 拆分规则适配分析（反馈 1：部分书源章节名与拆分规则对不上）

### 15.1 现状：拆分规则 vs 默认目录规则（启用集）差异

| 维度 | 默认目录规则（TOC） | 拆分规则 `ChapterSplitter` |
| --- | --- | --- |
| 内部空白 | 允许 `第\s{0,4}数字\s{0,4}章` | 不允许（`第` 后必须紧跟数字） |
| 数字字符 | `\d`、〇零一二两三四五六七八九十百千万、壹贰叁肆伍陆柒捌玖拾佰仟 | `\d`、零一两三四五六七八九十百千万〇（无大写壹贰…、无全角数字） |
| 后缀 | 章/节/卷/集/部/篇（规则2）；回/话/场（规则4，默认关） | 章/回/节 |
| 前置符号 | 规则14/16：`【第1章】` 等 | 无 |
| Chapter | 规则12：`Chapter\s{0,4}\d` | `Chapter\s*\d`（忽略大小写，一致） |
| 纯数字标题 | 规则8：`1、xxx` / `1. xxx` | 无 |

### 15.2 典型案例（按失配类型）

1. **内部空格类**：`第 3 章 风云`、`第3 章`、`第 3章`——TOC 规则能识别（通常不粘），但若因书源/规则原因粘进正文，拆分漏检；
2. **大写数字类**：`第叁章 风云`（壹贰叁肆伍陆柒捌玖拾佰仟）——TOC 可识别、拆分不识别；
3. **全角字符类**：`第１章`（全角数字）、`Ｃｈａｐｔｅｒ １`（全角字母）——TOC 与拆分都不识别，**必粘且拆分漏检**（日文/全角排版书常见）；
4. **后缀差异类**：`第1话`（日式，默认 TOC 不识别 → 粘住，拆分不支持 `话` → 漏检）；`第1卷`/`第一集`（TOC 识别，拆分不支持卷/集）；
5. **包裹符号类**：`【第1章 风云】`、`『第1章』`、`◆第1章`——TOC 规则14/16 部分识别，拆分不识别；
6. **分卷/复合标题类**：`第一卷 第一章 风云`、`卷一 风云`——拆分要求行首以 `第…章/回/节` 开头，`卷` 在前即漏检；
7. **纯数字序号类**：`1、风云`、`1. 风云`——TOC 规则8 识别，拆分不识别；
8. **首标题去重衍生问题**：解析出的章节名与正文行格式不一致（简繁 `第1章 風雨` vs `第1章 风雨`、标题规则带空格 `第 1 章` vs 正文 `第1章`、替换净化只改了标题没改正文）→ `firstTitle` 按 trim 全等比较失败 → 补入后拆出**两个同编号单元**（第1章 + 第一章）。

### 15.3 根因

拆分器只识别“行首 + 窄格式”的标题；而被 TOC 粘住的标题，往往恰好是拆分规则也没覆盖的格式（回/话/全角/包裹符号），形成漏检死角。二者规则集不对齐是根本原因。

### 15.4 建议升级方案（待确认后实施）

1. 拆分规则扩展为默认目录规则的**超集**（只影响行首整行，误命中风险可控）：
   - 允许内部空白：`第\s{0,4}数字\s{0,4}[章回节卷集部篇话]`；
   - 数字字符集补齐：壹贰叁肆伍陆柒捌玖拾佰仟、全角 ０-９；
   - 后缀补齐：章/回/节/卷/集/部/篇/话（`场` 可选）；
   - 行首允许包裹符号：`【〔「『［《（` 等（与 TOC 规则14 对齐）；
   - Chapter 兼容：空格/全角字母/可选 `.`、`、` 分隔符；
   - 纯数字标题 `1、xxx`：默认关闭（正文列举行易误命中），可选开启；
2. 首标题去重改为**编号片段规范化比较**：抽行首 `第N章/回…` 片段，去空格、全角转半角、数字归一后再比较，避免格式差异导致重复单元；
3. 全部变更补单测（每类案例一组），并以预览勾选兜底。

## 16. 书架“书源书籍”分类核对（反馈 2）

- 代码事实：`AppDatabase.onOpen` 插入 `IdNetBook(-13)` 行（`order=-11`、`show=1`，位于“全部”左侧）；`BookGroupDao.show` 中 -13 的条件是“存在非本地、非音频/视频的书”才显示；`BookGroup.getManageName` 已映射“书源书籍”文案；二期提交 96a058178 即此实现，**非回归**；
- 模拟器实测：DB 中该行存在；当前书架只有本地书 → 标签按条件隐藏；在数据库副本中插入一本 `type=text` 的非本地书后，查询结果 -13 出现在最左（order -11 < 全部 -10），与 UI 行为一致；
- 结论：功能已实现，“看不到”是因为书架上还没有书源书；**2026-08-12 已改为常驻显示**（`BookGroupDao.show` 中 -13 无条件显示），与其他特殊分类（音频/视频/更新失败）保持按需显示。

## 17. 阶段状态总结（2026-08-12）

- 三期主体完成：批量拆分（含两轮修复：原章节标题保留、续章标题不改写）、中文数字重写、目录编辑事务化、JVM 单测与 androidTest 补齐；Debug 包已安装模拟器，待手测继续；
- 手测反馈分析：拆分规则适配（§15）、书架“书源书籍”分类核对（§16）；
- 四期承接：拆分规则超集与首标题规范化（§15.4）、默认标题 off-by-one 评估（§4.0.1）、多选合并、在线书转本地引导、漫画侧增强、工程补齐与真机回归支持——详见《第四期计划书-合并与漫画增强.md》。
