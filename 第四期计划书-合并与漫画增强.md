# 第四期计划书：合并与漫画增强（含三期收尾补丁）

> 状态：**计划书草稿（2026-08-12），待确认后分阶段进入开发**
> 定位：承接第一、二、三期未实现部分，以及手测反馈产生的建议
> 前置：三期已交付（批量拆分、中文数字重写、目录编辑事务化、JVM 单测与 androidTest）；真机回归建议并行执行

## 1. 背景与目标

### 背景

- 三期拆分的标题规则与部分书源章节名格式不匹配（反馈 1，案例见第三期计划书 §15）：内部空格、大写/全角数字、卷集部篇话等后缀、包裹符号、分卷复合标题、纯数字序号；
- 书架“书源书籍”分类按需显示，用户期望常驻（反馈 2，见第三期计划书 §16）；
- 第一期的“多选合并冗余章节”、在线书“转本地”操作引导尚未实现；
- 第二期的漫画侧增强全部未做：单本相册导入、元数据识别、混合结构自动化、统一阅读设置面板、图片间距可调、大目录懒加载/分页；
- 三期遗留：默认标题 off-by-one 评估、androidTest 未在真机运行、`ReadConfig` 序列化测试、构建脚本封装、工作区卫生。

### 目标（第四期）

1. **三期收尾补丁**：拆分规则扩展为目录规则超集；首标题规范化去重；off-by-one 评估；书源书籍常驻选项（若确认）；
2. **多选合并冗余章节**（第一期预留 B）；
3. **在线书“转本地”操作引导**（第一期预留 D）；
4. **漫画侧增强**（第二期评估项 E）；
5. **工程补齐**：构建脚本封装、`ReadConfig` 序列化测试、工作区卫生、真机回归支持。

## 2. 范围

### 本期包含（建议按 4.1 → 4.2 → 4.3 分阶段交付，各自可独立回归）

- 4.1 三期收尾：拆分规则超集、首标题规范化去重、off-by-one 评估、书源书籍常驻（若确认）；
- 4.2 目录增强：多选合并冗余章节；
- 4.3 在线书“转本地”操作引导；
- 4.4 漫画侧增强：单本相册导入、元数据自动识别、混合结构自动化识别、统一漫画阅读设置面板、图片间距可调、大目录懒加载/分页；
- 4.5 工程补齐：构建脚本封装、`ReadConfig` 序列化测试、工作区卫生。

### 本期不包含

- 书源/在线阅读体系改动；
- 数据库表结构变更（维持“不加表不加列”原则）；
- 真机回归本体（用户执行，代码侧提供测试与清单）；
- 新一期功能（见 §12 五期预留）。

## 3. 现状与依据（已完成 vs 未完成）

| 项 | 状态 | 代码位置 |
| --- | --- | --- |
| 单章插入/删除 + 事务化 | 已完成 | `TocViewModel.insertChapter/deleteChapter`、`BookChapterDao.applyTocEdit` |
| 批量拆分粘连章节（预览/勾选/改名） | 已完成（规则需超集化） | `ChapterSplitter`、`TocViewModel.splitChapter/previewSplit`、`ChapterListFragment` |
| 中文数字标题重写 | 已完成 | `ChineseNumberUtils`、`ChapterNumberUtils` |
| 多选合并冗余章节 | **未实现** | 复用 `applyTocEdit` / `collectShift` 机制 |
| 在线书“转本地”引导 | **未实现** | 导出/导入已有基础 |
| 单本相册导入（MediaStore） | **未实现** | 需新入口；`ImageLoader` 已支持相册 URI |
| 元数据自动识别 | **未实现** | 现为文件夹名推导 |
| 混合结构自动化识别 | **未实现**（行级切换已兜底） | `MangaFolderScanner`、`ImportMangaActivity` |
| 统一漫画阅读设置面板 / 图片间距可调 | **未实现**（无间隙开关已有） | `Book.imageNoGap`、`ReadMangaActivity` 菜单 |
| 大目录懒加载/分页 | **未实现**（全量扫描） | `MangaFolderScanner.scan/scanChapters` |
| 拆分规则超集 / 首标题规范化 | **未实现**（三期反馈建议） | `ChapterSplitter.titleRegex`、`split` |
| 默认标题 off-by-one 评估 | 已标记未改 | 三期计划书 §4.0.1 |
| 书源书籍常驻选项 | 未定（按需显示中） | `BookGroupDao.show`（-13 条件） |
| 构建脚本封装 | 未定稿（`watch-build.ps1` 雏形存在） | 仓库根目录 |
| `ReadConfig` 序列化测试 | 未实现 | `Book.ReadConfig` |
| androidTest | 已写 2 个用例，未真机运行 | `app/src/androidTest/.../data/dao/` |
| 工作区卫生 | 未决 | `build.log`、`watch-build.ps1`、测试素材等未跟踪 |

## 4. 功能设计

### 4.1 三期收尾补丁（优先）

**4.1.1 拆分规则超集**（方案详见第三期计划书 §15.4）

- 允许内部空白：`第\s{0,4}数字\s{0,4}[章回节卷集部篇话]`；
- 数字字符集补齐：壹贰叁肆伍陆柒捌玖拾佰仟、全角 ０-９；
- 后缀补齐：章/回/节/卷/集/部/篇/话（`场` 可选）；
- 行首包裹符号：`【〔「『［《（` 等；
- Chapter 变体：空格/全角字母/可选 `.`、`、` 分隔符；
- 纯数字标题 `1、xxx`：**不纳入**（正文列举行易误命中，不设开关）；
- 每类案例补单测，预览勾选兜底。

> 已完成（2026-08-12）：内部空白/大写与全角数字/卷集部篇话后缀/包裹符号/Chapter 变体均已实现并补单测；首标题编号规范化比较（第1章 == 第一章）已实现。

**4.1.2 首标题规范化去重**

- 抽行首“第N章/回…”编号片段，去空格、全角转半角、数字归一后比较，避免标题格式差异导致重复单元（如“第1章 風雨”vs“第1章 风雨”）。

> 已完成（2026-08-12）。

**4.1.3 默认标题 off-by-one 评估**

- 一期 `insertChapter` 空白标题回退为 `anchor.index + 2`；先与用户确认预期编号约定（建议按新章节实际 index = `anchor.index + 1`），确认后再改，不随其他改动顺手改。

> 已完成（2026-08-12）：确认预期编号为 `anchor.index + 1`，`insertChapter` 与对话框默认标题均已修复。

**4.1.4 书源书籍常驻选项（若用户确认）**

- `BookGroupDao.show` 中 `-13` 条件改为无条件显示（与“全部”一致），一行 SQL 改动；确认后实施。

> 已完成（2026-08-12）：-13 已改为无条件显示。

### 4.2 多选合并冗余章节（B）

**入口**：目录页长按菜单扩展“多选”，或目录页顶部进入多选模式。

**流程**：

1. 多选 2 章及以上（相邻/跨章均可，按选中顺序合并）；
2. 合并对话框：合并后标题（默认第一章标题）、正文拼接方式（按选中顺序拼接）；
3. 执行：拼接正文写覆盖文件 → 删除被合并章节 → 后续章节仅 `index` 左移、标题保留原文（与拆分语义一致）→ 书签/阅读进度同步；
4. 复用 `applyTocEdit` 单事务提交；预览/确认在前。

**边界**：合并后总数小于 2 章时提示；书签只保留第一章的；阅读进度落在合并章内则位置保留。

> 已完成（2026-08-12，提交 cb586c085 + 修复）：目录长按菜单新增“合并章节”，对话框多选＋合并后标题编辑；`mergeChapters` 按 index 升序拼接正文写入第一章覆盖文件，删除其余选中章，后续章节 index 左移且标题数字 -removalCount 重写（保持序号连续），单事务提交；书签/阅读进度同步。
> 语义区分：合并是**删除**语义（续章编号前移补缺，如 2、3 合并后续章 4→3）；拆分是**插入**语义（续章标题保留原文，不重写）。
> 模拟器实测：第2回+第3回合并后目录 5→4 章（第1章/第2回/第3章/第4章，序号连续），合并内容含两章正文、可读；**合并结果可再拆分还原**（合并时后续章节前插入其标题行作为边界，撤销误合并用）。
>
> **设计决定（2026-08-12）**：保留“合并插入标题边界、误合并用拆分还原”的现状；曾评估“去除标题＋撤销上次合并（快照恢复）”方案，因合并需长按触发、误操作概率低，暂缓实施；`撤销栈`、合并标题开关等扩展亦暂不做。
>
> **遗留问题（发现）**：`BookHelp.getContent` 覆盖文件优先，重新导入/重新解析不清覆盖文件——旧编辑/拆分/合并残留的 `.nb` 会顶替新导入正文，导致内容错乱与拆分失灵（模拟器已复现）。建议后续：重新导入/重新解析时清理该书覆盖文件，或在帮助文案中说明该行为。

### 4.3 在线书“转本地”操作引导（D）

- 在线书详情/目录页提供“转本地”入口：引导导出文件 → 导入本地（一期已明确路径）；
- 首版为引导 + 一键导出到本地目录，不改变在线阅读逻辑。

### 4.4 漫画侧增强（E）

**4.4.1 单本相册导入（MediaStore）**

- 相册扁平结构 → 单书单章；复用现有图片渲染与阅读器；
- 权限：相册读取（新权限申请流程）。

**4.4.2 元数据自动识别**

- 封面：取文件夹内首图或命名含 cover/封面 的图片；
- 作者/系列：从文件夹名/父目录名推导（不智能清洗）。

**4.4.3 混合结构自动化识别**

- 扫描时统计“直接含图 + 含子文件夹”的混合节点，预览默认给出建议角色，行级切换兜底保留。

**4.4.4 统一漫画阅读设置面板**

- 把竖排/横排、无间隙、缩放等整合为一个设置面板，替代分散菜单项；`imageNoGap` 等字段结构不变。

**4.4.5 图片间距可调（与无间隙开关合并，消除冗余）**

- 用一个“图片间距”选项（0=无间隙 / 小 / 中 / 大）取代竖排无间隙布尔开关：间距 0 即原“无间隙开启”；
- `imageNoGap` 字段保留兼容（映射为间距 0/非 0），UI 上不再暴露独立开关。

**4.4.6 大目录懒加载/分页**

- `MangaFolderScanner.scan/scanChapters` 分批扫描 + 进度提示；目录页章节懒加载。

### 4.5 工程补齐

- 构建脚本封装：固定 `ANDROID_PREFS_ROOT` 与 debug 密钥位置，输出路径动态取 APK（文件名带时间戳）；
- `ReadConfig`（含 `imageNoGap`）GSON 序列化单测；
- 工作区卫生：`build.log`、`watch-build.ps1`、`make_test_book.py`、`test_book.txt`、`test_book_split.txt`、`测试漫画素材/` 收编或清理，确认 `modules/web` 生成文件归属；
- 真机回归：提供 androidTest（事务回滚、书架分类）与手测清单，由用户执行。

### 4.6 书架分类“其它”折叠（产品建议，可选，待确认）

> 2026-08-12 讨论：类型/分类增多后标签条变长，考虑引入可折叠的“其它”类。

- **设计**：标签条最右侧放“其它 ▾”，收纳**低频/整理类**分类（网络未分组、本地未分组、更新失败），展开后仍可进入各子类；**音频/视频保持一级类型**（它们是用户的浏览入口，不应被折叠）；
- **收纳范围可配置**：用户在分组管理中可勾选哪些分类进“其它”，避免拍脑袋定死；
- **空则隐藏**：延续现有“条件显示”逻辑，无内容时“其它”不占位；
- **与现有机制关系**：分组管理仍可用；折叠只是视图收纳，不改 `book_groups` 数据结构；
- **风险**：折叠入口的发现性（需要明确的 ▾/箭头动画提示）；“类型/状态/整理”不同轴混放造成层级混淆——因此限制只收整理/状态类；
- **排期**：可选，与 4.2~4.4 不冲突；建议放 4.4 之后或五期，若要做可并入 4.4 的书架相关改动一起评估。
- 每类案例补单测，预览勾选兜底。

## 完成状态（2026-08-13 更新）

> 4.1~4.3、4.5 已完成代码侧实施；4.4 除下述两项外已完成（进度/封面/建议角色/相册导入/间距档位/统一面板）。
> 详见《复盘总结》§11（手测记录与提交清单）。
> 未做项：4.4.6 分批扫描与目录懒加载（仅进度提示）、4.4.2 作者推导（保持手动编辑）→ 转入第五期售后桶；
> 4.6 “其它”折叠分类未立项（候选）。

## 5. 技术方案

| 模块 | 改动 |
| --- | --- |
| `help/book/ChapterSplitter.kt` | 规则超集 + 首标题规范化 |
| `help/book/ChapterNumberUtils.kt` / `ChineseNumberUtils.kt` | 大写/全角数字支持（如需） |
| `ui/book/toc/TocViewModel.kt` | 多选合并主流程（复用 `applyTocEdit`/`collectShift`） |
| `ui/book/toc/ChapterListFragment.kt` | 多选模式与合并对话框 |
| `model/localBook/MangaFolderScanner.kt` | 懒加载/分页、混合结构统计 |
| 新增相册导入入口 + ViewModel | MediaStore 扫描与建书 |
| `BookDao`/`BookGroupDao` | 书源书籍常驻（如确认） |
| 构建脚本 / 测试 / 文档 | 工程补齐 |

## 6. 边界与限制

1. 合并与拆分均仅本地 TXT；在线书/EPUB 不提供入口；
2. 相册导入不保留层级（扁平）；
3. 元数据识别不保证准确，提供手动编辑入口（沿用现有编辑书籍信息）；
4. 超集规则放宽后可能误命中正文行——整行行首匹配 + 预览勾选兜底；
5. 真机回归不在代码侧完成。

## 7. 风险与规避

| 风险 | 影响 | 规避 |
| --- | --- | --- |
| 超集规则误命中 | 拆错章 | 整行行首匹配 + 预览勾选；每类案例单测 |
| 合并顺序/书签错乱 | 内容或书签错位 | 单事务 + 预览确认；书签只保留第一章 |
| 相册权限申请失败 | 无法导入 | 权限引导 + 失败提示 |
| 大目录扫描卡顿 | 导入/阅读卡死 | 懒加载/分页 + 进度提示 |
| 规则改动影响既有拆分 | 回归 | 全量单测 + 手测清单 |
| 旧功能修复（off-by-one） | 行为变更 | 先确认预期编号，小步提交可回滚 |

## 8. 测试计划

1. 超集规则：每类失配案例一组（空格/大写/全角/后缀/包裹/卷复合/纯数字开关）；
2. 首标题规范化：简繁/空格差异不产生重复单元；
3. 合并：相邻/跨章合并、正文顺序、书签/进度、后续标题保留；
4. 在线书转本地：导出→导入全流程；
5. 相册导入：单书单章、权限拒绝；
6. 元数据识别：封面/作者推导；
7. 混合结构：建议角色正确、行级切换兜底；
8. 大目录：千级文件夹扫描不卡死、有进度；
9. 阅读设置面板/间距：三档间距生效；
10. 回归：一/二/三期全部手测清单 + androidTest。

## 9. 验收标准

- [ ] 拆分规则超集与首标题规范化生效（单测覆盖每类案例）；
- [ ] 多选合并可用，目录/正文/书签/进度正确；
- [ ] 在线书“转本地”引导可完成导出→导入；
- [ ] 相册导入、元数据识别、混合结构建议可用；
- [ ] 大目录扫描有进度且不卡死；
- [ ] 阅读设置面板与间距选项生效；
- [ ] 工程补齐项完成（构建脚本、ReadConfig 测试、工作区卫生）；
- [ ] 一/二/三期回归通过。

## 10. 工作量与排期（熟悉 Kotlin 口径）

| 阶段 | 内容 | 估时 |
| --- | --- | --- |
| 4.1 三期收尾 | 超集规则、首标题规范化、off-by-one 评估、书源常驻 | 1~1.5 天 |
| 4.2 多选合并 | 多选模式 + 合并主流程 + 对话框 | 1.5~2 天 |
| 4.3 转本地引导 | 入口 + 引导 + 导出 | 0.5~1 天 |
| 4.4 漫画增强 | 相册导入 1.5~2 + 大目录 1 + 元数据 1 + 混合结构 0.5~1 + 设置面板/间距 1 | 5~6.5 天 |
| 4.5 工程补齐 | 构建脚本、ReadConfig 测试、工作区 | 1 天 |
| 回归 | 各阶段独立回归 + 最终全量 | 1 天 |
| 合计 | | **10~13 天**（建议拆 4.1/4.2/4.3 三批交付） |

> 说明：4.4 漫画增强体量最大，可独立拆分多个小任务按需排期；不熟悉 Kotlin 时按 2 倍排期。

## 11. 待确认项

1. 拆分规则超集：按 §4.1.1 实施（推荐）？纯数字标题 `1、xxx` 默认关是否接受？
2. 书源书籍分类：改为常驻显示（推荐按用户预期），还是保持按需显示？
3. 默认标题 off-by-one：预期编号确认为 `anchor.index + 1` 后再修？
4. 四期交付顺序：按 4.1 → 4.2 → 4.3 分批，还是先做漫画增强？
5. 真机回归：用户并行执行一/二/三期清单与 androidTest。

## 12. 五期预留（候选，不定稿）

- 目录批量改名/重排（拖拽排序）；
- 目录规则可视化编辑（拆分/合并规则界面化）；
- 漫画阅读设置的全局模板；
- 在线书“转本地”后的批量合并/整理。
