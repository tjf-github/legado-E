# 第二期计划书：本地漫画导入

> 状态：**首版实现完成 + 两轮用户反馈迭代**（导入 → 阅读体验 → 书架分类，编译通过、单测通过、调试签名已与本机一致），待真机回归（见第 8 节测试计划）。
> 与本仓库「第一期计划书-目录章节增删.md」相互独立，可先后或并行开发。

## 1. 背景与目标

### 背景

- 用户本地漫画以文件夹/相册形式存放，期望在阅读器内直接导入观看，无需借助外部工具整理；
- 非连载漫画一般没有“话”的概念：一本漫画 = 一个文件夹/相册，图片以 001、002… 命名平铺；
- 连载/同人漫画则存在“系列 → 多话”的结构。

### 目标

- 通过**文件路径（SAF 选文件夹）**导入本地漫画，自动识别固定三层结构；
- 支持两种形态：**多章节型**（第二层=书、第三层=章节）与**多本单书型**（第二层=系列分组、第三层=各自独立书）；
- 导入后书架生成独立书籍，点击章节后按图片自然排序整本/逐页观看；
- 不改动现有本地 TXT/EPUB 等导入与阅读逻辑。

## 2. 范围

### 本期包含

- 路径导入入口（SAF 目录选择，复用现有基础）；
- 固定三层结构扫描与建书（`Book` + `BookChapter`）；
- 两种导入模式：**整本导入**（所选文件夹=书，直接子文件夹=章节）、**系列导入**（所选文件夹=系列分组，直接子文件夹=各自独立书）；
- 图片自然排序（001、002…，处理字符串序 2 < 10 的问题）；
- 导入结果预览清单（行级操作：改名 / 跳过 / “当书-当章节”切换，兜底混合结构）；
- 阅读渲染复用文本阅读器图片模式（`<img>` 渲染管线）。

### 本期不包含

- 系统相册（MediaStore）导入：相册是扁平结构，保不住三层层级（后续可单独评估“单本相册导入”）；
- 可编辑文件树 / 层级重排；
- 新建文件夹、移动文件等物理整理（留给文件管理器，避免 SAF 移动文件的高成本与风险）；
- 混合结构自动识别（罕见场景，用预览行级切换或分两次导入兜底）；
- 在线漫画（书源）的任何改动；
- 数据库表结构变更。

## 3. 现状与依据（代码事实）

| 关注点 | 现状 | 位置 |
| --- | --- | --- |
| 图片加载 | 已支持网络 URL、`file://`、`content://` 相册 URI、本地文件路径 | `help/glide/ImageLoader.kt` |
| 目录选择 | SAF `OpenDocumentTree`、应用内 `FilePickerDialog`、持久化权限均已有 | `ui/file/HandleFileActivity.kt` |
| 本地导入白名单 | 仅 txt/epub/umd/pdf/mobi/azw3/azw，**不含图片** | `constant/AppPattern.kt:43` |
| 本地正文读取 | 默认走 `TextFile` 字节偏移 `start/end`，图片类型书籍需新增分支 | `model/localBook/LocalBook.kt` |
| 图片书籍类型 | `BookType.image`、`Book.isImage` 已存在；编辑书籍信息可改类型 | `help/book/BookExtensions.kt`、`ui/book/info/edit/BookInfoEditActivity.kt` |
| 图片渲染 | 文本阅读器已支持 `<img>` 渲染（`TextChapterLayout`/`ImageColumn`），`ReadBook` 对 `isImage` 默认滚动样式 | `ui/book/read/page/provider/TextChapterLayout.kt` |
| 漫画界面 | `ReadMangaActivity` 完整（webtoon/缩放/灰阶），当前仅对非本地图片书启用 | `utils/ContextExtensions.kt:73` |
| 卷概念 | `BookChapter.isVolume` 已存在，可表达“卷 → 章” | `data/entities/BookChapter.kt` |
| 书架分组 | `Book.group` 已存在，可表达系列归属 | `data/entities/Book.kt` |

## 4. 功能设计

### 4.1 固定三层结构

```
漫画（导入根目录，第一层）
└── A类 / 系列（第二层）
    └── A1、A2…（第三层，直接含图片 = 可读单元）
```

固定映射（不依赖可编辑树）：

| 层 | 角色 | 书架映射 |
| --- | --- | --- |
| 第一层 | 导入入口 | 不生成实体 |
| 第二层 | 系列/分类 | 书架分组（`Book.group`），或退化为书名前缀“系列·书名” |
| 第三层 | 直接含图片的文件夹 | 每本生成一本书；若为连载场景则作为该书的章节 |

### 4.2 两种导入模式

用户在发起导入时选择模式，**选层即定角色**：

- **整本导入**：所选文件夹=书；其直接子文件夹=章节；若没有子文件夹（直接全是图），整本作为一章连看；
  - 适用：`漫画/社团`（多章节型）、`漫画/异世界/异世界1`（单书）；
- **系列导入**：所选文件夹=系列分组；每个直接子文件夹=各自独立书；每本书按同样规则生成章节；
  - 适用：`漫画/异世界`（多本单书型）。

### 4.3 扫描规则

- 文件夹直接含图片 → 可读单元（书或章节，取决于模式）；
- 文件夹只含子文件夹 → 容器（系列或书）；
- 图片格式白名单：jpg / jpeg / png / webp / bmp / gif；
- 图片自然排序：按名称数字段排序（`001.jpg < 002.jpg < 010.jpg`），避免纯字符串序；
- 跳过无图文件夹、封面图/杂项文件夹（预览清单中可见并可勾选排除）。

### 4.4 建书与章节生成

- `Book`：`name` = 书文件夹名，`group` = 系列名，`type = image or local`，`bookUrl` = 文件夹 URI；
- `BookChapter`：`title` = 章节文件夹名（原样保留，**不自动重编号**）；整本连看时仅一个章节；
- 章节内记录图片列表（img src 数组），不依赖 `start/end` 字节偏移；
- 书与章节的元数据（封面、作者）首版从文件夹名推导，不做智能识别。

### 4.5 预览确认（轻量，非树）

扫描完成后展示**结果清单**，而不是可编辑树：

```
将导入 2 本书（系列：异世界）
  异世界1 —— 整本连看（38 张图）
  异世界2 —— 整本连看（45 张图）
```

清单行级操作：

- 改名（书 / 章节）；
- 勾选/取消导入（跳过杂物文件夹）；
- 极少数混合结构场景提供“当书 / 当章节”单行切换；
- 确认后批量建书入库。

### 4.6 阅读体验

- 首版复用文本阅读器的图片渲染（`<img>` + `ImageColumn`），`ReadBook` 对 `isImage` 书籍默认滚动样式，支持点击放大；
- 是否接入 `ReadMangaActivity` 漫画界面（webtoon 滚动、缩放、灰阶）列为**成型后评估项**（需放开 `isLocal` 限制，改动可控）。

## 5. 技术方案

### 5.1 新增/改动

| 文件/模块 | 内容 |
| --- | --- |
| 新增 `ImportLocalMangaViewModel` | 目录扫描（递归 + 自然排序 + 图片过滤）、建书/建章、预览数据 |
| 新增扫描工具（`MangaFolderScanner`） | 三层识别、容器/可读单元判定 |
| `LocalBook.getContent` | 新增 `isImage` 分支：按章节返回图片列表（img 标签文本或图片数据），不走 `TextFile` 偏移 |
| 导入入口 UI | 书架/文件选择器新增“导入本地漫画”，选择文件夹 + 选择模式 |
| 预览对话框/页面 | 结果清单 + 行级操作 |
| 渲染验证 | 确认 `content://`/`file://` 图片 src 在文本阅读器可正常加载（`ImageLoader` 已支持，需实测） |

### 5.2 稳定性约束（沿用第一期 7.1 原则）

- 零侵入：仅新增导入入口与 `LocalBook.getContent` 图片分支，不改现有 TXT/EPUB 路径；
- 不迁移数据库：不加表不加列；
- 独立失败域：扫描/建书异常捕获并提示，不影响书架其他功能；
- 编译门槛：每步 `.\gradlew.bat :app:compileLegadoDebugKotlin` 通过再继续；
- 小步提交：入口 → 扫描 → 建书 → 渲染验证 → 预览 UI 分步提交，可独立回滚；
- 交付前回归：本地 TXT / EPUB / PDF 导入与阅读、书架、目录页不受影响。

## 6. 边界与限制

1. 一个系列内部默认统一形态（全为多章节型或多本单书型）；**混合结构不自动识别**，用预览行级切换或分两次导入兜底；
2. 相册（MediaStore）不保留层级，本期不做相册导入；
3. 不做物理文件整理（新建/移动文件夹），章节是虚拟分组，不要求真实目录存在；
4. 超大型目录（上千文件夹/图片）需懒加载或分页扫描，避免卡顿；
5. 书与章节命名首版取文件夹名，不智能清洗。

## 7. 风险与规避

| 风险 | 影响 | 规避 |
| --- | --- | --- |
| 混合结构误判 | 书/章节角色错误 | 预览行级切换、分两次导入 |
| 字符串排序错误 | 页码乱序 | 自然排序算法 + 测试用例（2 < 10） |
| content:// 图片加载失败 | 相册 URI 无法显示 | 依赖已有 `ImageLoader` 分支，渲染阶段实测；失败兜底提示 |
| 大目录扫描慢 | 导入卡顿 | 懒加载/分页 + IO 线程 + 进度提示 |
| 重复导入 | 书架出现重复书 | 按文件夹 URI 查重提示 |
| 权限丢失 | 重启后读不到 | 沿用现有 `takePersistableUriPermission` 机制 |

## 8. 测试计划

1. 多章节型导入（`漫画/社团/第1话…`）：社团=1 本书，第1话=章节；
2. 多本单书型导入（`漫画/异世界/异世界1、异世界2`）：2 本独立书，系列进分组；
3. 整本连看（非连载单书）：一章、38 张图按序；
4. 自然排序：001、002、010 顺序正确；
5. 混合结构：预览行级切换后结果正确；
6. 空文件夹/无图文件夹：跳过且清单可见；
7. 重复导入：查重提示；
8. 大目录：扫描不卡死、有进度；
9. 阅读：点击章节后图片逐页显示，放大正常；
10. 回归：本地 TXT/EPUB/PDF 导入与阅读、书架、目录页正常。

### 8.1 真机手动测试清单（交付前逐项勾选）

- [ ] 入口：书架右上角菜单出现“导入本地漫画”，点击后弹出“整本导入 / 系列导入”两种模式；
- [ ] 整本导入（多章节型）：选择 `漫画/社团`（直接子文件夹=第1话…）→ 预览为 1 本书、直接子文件夹均为章节、图片数正确；
- [ ] 整本导入（单书连看）：选择直接含图片的文件夹 → 预览为 1 本书 1 个章节、图片按 001/002/010 自然排序；
- [ ] 系列导入：选择 `漫画/异世界`（含 异世界1、异世界2）→ 预览为 2 本独立书，确认后书架生成 2 本书且系列名进入书架分组；
- [ ] 自然排序：章节内 `001.jpg < 002.jpg < 010.jpg`，无字符串序错误；
- [ ] 空文件夹 / 无图文件夹：出现在清单中并默认未勾选，确认导入时被跳过；
- [ ] 重复导入：再次选择同一文件夹 → 行内标记“已在书架”，确认后重新导入且阅读进度重置；
- [ ] 预览行级操作：改名生效；勾选/取消生效；混合结构行可切换“整本连看 ↔ 子文件夹为章节”；
- [ ] 阅读：书架点击书籍可进入阅读，图片逐页/滚动显示正常，点击放大正常，上一章/下一章不跳错；
- [ ] 回归：本地 TXT / EPUB / PDF 导入与阅读、书架、目录页不受影响。

## 9. 验收标准

- [ ] 书架/文件选择器出现“导入本地漫画”入口；
- [ ] 整本导入与系列导入两种模式可用；
- [ ] 导入结果与预览清单一致，书/章节角色正确；
- [ ] 章节内图片按自然排序可读、可放大；
- [ ] 混合结构可经行级切换纠正；
- [ ] 回归项通过（测试 10）。

## 10. 工作量与排期

| 阶段 | 内容 | 估时 |
| --- | --- | --- |
| 导入入口 | 入口 + 模式选择 + 目录选择（基础已有） | 0.5 天 |
| 扫描与建书 | 三层识别、自然排序、建 Book/BookChapter、查重 | 1.5 天 |
| 正文供给与渲染 | `LocalBook.getContent` 图片分支、渲染实测 | 1 天 |
| 预览 UI | 结果清单 + 行级操作 | 1 天 |
| 自测与回归 | 边界、大目录、回归 | 0.5~1 天 |
| 合计 | | **4.5~5 天** |

> 说明：按熟悉 Kotlin 的开发实施估算；不熟悉 Kotlin 时建议按 8~10 天排期，并保持每步编译验证。

## 11. 成型后评估项（本次不定稿）

1. 是否接入 `ReadMangaActivity` 漫画界面（webtoon/缩放/灰阶）作为本地漫画默认阅读器；
2. 系列在书架上的呈现：书架分组（默认）还是书名前缀；
3. 是否增加“单本相册导入”（MediaStore，无层级）；
4. 书/章节元数据（封面、作者）的自动识别程度；
5. 混合结构是否需要更自动化的识别（当前以行级切换兜底）。

## 12. 用户反馈与落地（2026-08-11）

| 反馈 | 落地 |
| --- | --- |
| 竖排观看时图片上下不要有间隙（最好可设置） | 本地漫画接入漫画阅读界面（webtoon 竖排默认无间隙）；文本阅读器中图片书段间距归零，行距/段距设置仍可调 |
| 增加左右翻页的相册式模式 | 复用漫画界面“开启横向滚动”模式（整页吸附，左右翻页） |
| 本地小说与本地漫画分类分开 | 新增书架分类“本地漫画”（本地+图片类型）；原“本地”分类更名为“本地小说”，不再包含漫画 |
| 书架增加“书源书籍”分类（在“全部”左侧，无论是否缓存） | 新增书架分类“书源书籍”（非本地、非音频/视频的书，含已缓存在线书），排在最左侧 |
| 竖排无间隙改为开关 | 新增每本书的“竖排无间隙”设置（默认开启）：漫画界面菜单可切换，文本阅读器同步生效；关闭后图片间留间距 |

> 待真机验证：竖排无间隙开关、横向翻页、书架分类（本地漫画/书源书籍/本地小说）隔离，验证通过后勾选第 8.1 节清单。

## 13. 开发建议与后续规划（2026-08-11 评审）

### 13.1 产品建议

1. 「竖排无间隙」已做成每本书独立开关（默认开启）；后续可扩展“图片间距大小”可调，建议先稳定开关行为再增加参数；
2. 漫画阅读方式（竖排/横排）、无间隙、缩放等设置建议后续整合为统一的“漫画阅读设置”面板，避免菜单项过长；
3. 「书源书籍」与「网络未分组」职责不同（全量视图 vs 整理视图），保留并存；分类增多后建议考虑折叠“未分组”类；
4. 「本地小说」目前包含 epub/pdf/mobi 等非漫画本地文档，名称不完全贴切，可后续评估是否按格式继续细分。

### 13.2 工程建议（本轮复盘沉淀）

1. 构建签名：调试 APK 必须与本机 `~/.android/debug.keystore` 签名一致，否则覆盖安装报 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`；建议把构建命令封装为脚本，固定 `ANDROID_PREFS_ROOT` 与密钥位置，避免每次手工处理；
2. 构建产物文件名带时间戳（`legado_app_3.26.xxxxxx.apk`），交付安装命令前需重新确认实际文件名；
3. 真机回归（第 8.1 节）尚未完成，安装/推送依赖用户终端执行（开发环境 adb 受沙箱限制）；
4. 自动化测试覆盖不足：新增的路由/分类/渲染改动无单测，建议补充 `BookDao` 分类查询与 `ReadConfig` 序列化的测试；
5. 工作区卫生：`build.log`、`watch-build.ps1`、`测试漫画素材` 等未跟踪文件需定期清理或纳入管理；
6. 大目录扫描（上千文件夹/图片）的懒加载与分页仍未实现，导入大量素材前需验证性能。

## 14. 后期复盘与状态（2026-08-12）

### 已落地

- 首版 + 两轮用户反馈迭代全部完成（导入/阅读/书架分类）；
- 三期补充：`BookDao` 分类查询 androidTest（书源书籍/本地小说/本地漫画）；
- “书源书籍”分类核对：实现正常（`order=-11` 位于“全部”左侧，按需显示），详见第三期计划书 §16。

### 本期未实现（去向）

| 未实现项 | 去向 |
| --- | --- |
| 单本相册导入（MediaStore） | 四期 |
| 书/章节元数据自动识别 | 四期 |
| 混合结构自动化识别（预览行级切换已兜底） | 四期 |
| 统一漫画阅读设置面板 / 图片间距可调 | 四期 |
| 大目录懒加载/分页（当前全量扫描） | 四期 |
| 构建签名脚本封装（固定 `ANDROID_PREFS_ROOT` 与 debug 密钥） | 四期工程项 |
| `ReadConfig`（含 `imageNoGap`）序列化测试 | 四期工程项 |
| 工作区卫生（未跟踪文件收编/清理） | 四期工程项 |
| 真机回归（§8.1） | 用户并行执行 |
