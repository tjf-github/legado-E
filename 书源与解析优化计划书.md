# 书源与解析优化计划书

> 状态：**草案 v2 / 已纳入完整书源静态基线，待评审（2026-09-21）**
> 来源：《维护计划书-后续修复与增强.md》中的“书源优化”待办。
> 定位：围绕“具体书源质量”和“App 通用解析能力”两条独立主线，处理搜索、发现、详情、目录、正文、格式、替换、图片及请求稳定性问题。
> 约束：本文件是方案与排期，不代表已经授权实施；不新增数据库表/列，不改变本地 TXT/EPUB 导入与阅读语义，不触碰已封存的 AI 正文净化功能。

## 1. 计划目的

书源问题表面上常表现为“搜不到”“章节为空”“多出符号”“乱跳行”“图片不显示”，但根因可能分别位于网站响应、书源规则、通用解析器、格式化、净化替换或阅读排版层。如果不先定位层级，容易出现两类反效果：

- 用全局解析兼容一个书源的脏数据，导致其他书源行为改变；
- 只改书源规则掩盖 App 的通用解析缺陷，同类网站继续重复出错。

本计划的目标是建立一条可观测、可复现、可分层验收的处理路径：**先确认原始响应，再确认规则结果，最后确认格式化、替换和排版结果**。只有多个书源在同一规则语义下稳定复现的问题，才进入通用解析层修复。

## 2. 总体原则

1. **书源优先，解析兜底**：单个网站结构变化、选择器失效、请求头或登录问题，优先改对应书源；通用解析器只处理有明确跨源价值的问题。
2. **保真优先于“看起来干净”**：默认不猜测、不补字、不重写标点；无法安全判断时保留原文。
3. **结构先于字符串**：图片、链接、`<usehtml>` 和其他受支持结构应作为结构化片段保护，不能与普通正文一起被无边界正则误伤。
4. **兼容默认行为**：已有书源 JSON、已有替换规则和已缓存正文必须继续可用；新增严格行为优先通过检测、预览或显式开关引入。
5. **每层都可验收**：书源规则、通用解析、格式规范化、替换、图片和请求调度分别留测试与证据，不能用“最终页面看起来正常”代替分层判断。
6. **真实内容最小化**：测试优先使用脱敏后的最小复现样本；真实书源、Cookie、正文和日志不进入仓库，也不默认外发。
7. **书源集合是开放的**：§3.4 的 6139 个 URL 是**当前快照，不是封闭全集**——用户会新增书源，但**实际频率很低，可能长期不变**（截至 2026-09-21 已很久未新增）。因此本计划的所有产物（静态分群工具、规则修复、回归口径、诊断卡）必须对**尚未见到的书源**同样成立：工具可重复运行并自动纳入新增项，修复不针对已知集合硬编码，新增书源沿用同一套诊断与验收流程，不为其另开通道。本条只约束**设计形状**，不据此追加增量摄取、定期巡检或额外排期——低频新增下，一次性基线体检仍是划算且短期内不腐坏的投入。

## 3. 当前链路与已确认边界

### 3.1 正文处理链路

当前核心链路可概括为：

```text
网络原始响应
  -> AnalyzeRule（CSS/JSoup、XPath、JSONPath、正则、JS/WebJS）
  -> ContentRule.content 提取正文
  -> HtmlFormatter.formatKeepImg（逐页做块标签换行、去普通标签、保留并绝对化图片 URL）
  -> 获取并拼接正文下一页 / 副内容
  -> ContentRule.replaceRegex（对拼接后的全文执行书源自带替换）
  -> 保存原始正文缓存
  -> ContentProcessor（去重复标题、重新分段、简繁转换、用户净化替换）
  -> 空行折叠、段首缩进、图片/特殊 HTML 排版
  -> 阅读页
```

这意味着“多出符号、跳行、图片失效”不能只看最终结果，至少要比较以下四个快照：

| 快照 | 要回答的问题 |
| --- | --- |
| 原始响应 | 网站是否已经返回脏字符、异常 HTML 或错误页面 |
| 规则提取后 | CSS/XPath/JSONPath/JS 规则是否选多、选少或拼接错误 |
| 内置格式化后 | HTML 实体、块标签、空白和图片 URL 是否被正确转换 |
| 阅读处理后 | 重分段、简繁、净化替换、缩进和图片排版是否引入变化 |

### 3.2 两类“替换”必须分开

- **书源内替换**：`ContentRule.replaceRegex`，属于书源规则的一部分，在正文抓取与 HTML 格式化之后执行，适合处理该网站固定广告、尾注和特有标记。
- **用户净化替换**：`ReplaceRule` + `ContentProcessor`，按书名/书源范围匹配，在阅读处理阶段按顺序执行，支持普通字符串或正则、标题/正文作用域、排除范围和超时。

两者的职责不能混用：网站专属问题应留在书源内；跨站、由用户主动配置的阅读偏好才进入净化替换。验收时必须记录是哪一层生效，避免同一内容被重复清洗。

### 3.3 图片是正文结构，不是普通文本

当前正文中的图片以 `<img src="...">` 形式保留，图片地址还可能附带请求参数、引用章节 URL、请求头或解密逻辑。净化替换直接作用于整段字符串时，理论上既可以有意修改图片 URL，也可能无意删掉标签或把 `src` 变为空。因此图片优化的首要目标不是增加更多正则，而是先把“正文文字”和“结构化图片片段”区分开。

### 3.4 用户完整书源静态基线（2026-09-21）

用户提供本地压缩包《阅读pro书源.zip》。本轮只在本机静态读取书源 JSON，没有执行其中的 JS、没有发起网络请求、没有导入 App；压缩包中的书架、书签、阅读记录、搜索历史、配置与 MHT 等非书源数据均未纳入分析。

确认结果：

| 项 | 静态结果 | 计划含义 |
| --- | ---: | --- |
| `bookSource.json` 书源数 | 6139 | 作为当前权威全集 |
| 唯一 `bookSourceUrl` | 6139 | 当前全集内部没有 URL 重复 |
| 启用书源 | 4000 | 联网抽样优先从启用集选择 |
| 文本 / 音频 / 图片 / 文件 | 5805 / 109 / 188 / 37 | 自动化全量静态检查，联网验证按类型分层抽样 |
| 有正文规则 | 6004 | 另有 135 条需按类型判断是缺失、下载型或旧格式 |
| 有书源内 `replaceRegex` | 1635 | 替换兼容是高影响面，必须做静态检查与样本回归 |
| 有正文下一页规则 | 2030 | 分页循环、乱序和页边界格式应列为重点 |
| 正文规则使用 JS/WebJS | 1010 | 静态分析只能覆盖语法/结构，真实行为需受控执行 |
| 正文规则涉及图片属性 | 293 | 需覆盖懒加载、相对 URL 与属性兼容 |
| `imageStyle=TEXT` | 45（启用 21） | 图片字是现实但小众的特殊兼容路径，定为中优先级 |

压缩包内可解析的各书源 JSON 合集共有 16155 条引用，但合并去重后仍是上述 6139 个 URL，说明其他 JSON 合集主要是历史来源或重复打包。后续不按“合集文件”重复跑相同书源，而按 `bookSourceUrl`、规则结构指纹和最后更新时间归并。

安全边界也由此明确：至少 239 个书源的请求头字段包含 Cookie/Authorization/Token/Session 等敏感键名，790 个书源配置了登录入口或登录检测。原始压缩包、完整 Header、Cookie、登录数据与含真实内容的调试响应只能留在本机，不写入仓库、不进入代理提示或外部报告；仓库中只允许脱敏统计、合成夹具和用户明确批准的最小样本。

## 4. 问题分类与归属判定

### 4.1 具体书源问题

以下问题默认归属“书源本身”：

- 搜索 URL、请求方式、参数、编码、请求头、Cookie、登录或验证码发生变化；
- 搜索/发现/详情/目录/正文选择器因网站改版失效；
- 仅该站存在的广告、尾注、水印、章节分页或懒加载图片属性；
- 目录去重、章节 URL 补全、下一页规则或该站专用 JS 逻辑错误；
- 该站限流、并发率、重试节奏或响应格式特殊。

处理原则：以最小书源规则修正为主，不为单站增加全局分支；同一书源的搜索、详情、目录、正文必须作为一条完整链路回归，不能只验证当前出错环节。

### 4.2 App 通用解析问题

满足以下至少一项，才考虑进入通用解析层：

- 两个及以上独立书源可用最小样本复现；
- 现有规则帮助声明支持，但解析结果与声明不一致；
- 同一语义在 CSS/XPath/JSONPath/正则等解析方式间行为不一致；
- 问题来自通用 HTML/Unicode/URL 标准处理，而不是网站私有结构；
- 修复可以用离线夹具稳定证明，且旧行为兼容边界清楚。

### 4.3 格式问题

格式问题按来源拆成五类，不用一个“清理正则”包办：

1. **字符类**：HTML 实体、NBSP/全角空格、零宽字符、BOM、不可打印控制符、异常转义残留；
2. **换行类**：`CRLF/LF/CR` 混用、`br/p/div` 重复换行、多页拼接重复空行、每句一行或整章一行；
3. **标记类**：未闭合标签、脚本/样式残留、列表符号、网站复制水印、错误解码产生的符号；
4. **段落类**：标题重复、段首空格混乱、诗歌/聊天记录/代码片段被错误重分段；
5. **结构类**：图片、链接、注音、自定义 HTML 被当普通文字删改。

其中字符与换行可做保守的通用规范化；广告、水印和站点装饰仍由书源内替换处理；段落重排必须保留用户开关，不能强制全局启用。

## 5. 主线 A：具体书源治理

### A1. 建立书源诊断卡

每个待修书源使用同一张诊断卡，至少记录：

- 书源名称、脱敏标识、类型（文本/音频/图片/文件/视频）和规则版本时间；
- 失败环节：搜索、发现、详情、目录、正文、下一页、图片或登录；
- 复现关键词与章节仅保留脱敏摘要，不收录真实用户内容；
- HTTP 状态、最终 URL、响应类型、耗时、重试次数和错误类别；
- 原始响应特征、规则命中数量、最终输出数量；
- 修复前后差异及是否影响其他环节。

### A2. 书源规则质量清单

按下列顺序检查并优化：

1. **请求层**：URL、方法、参数编码、Header、CookieJar、登录状态、并发率、超时与跳转；
2. **搜索/发现**：列表范围、书名/作者归一化、详情 URL、封面和重复项；
3. **详情**：书名、作者、简介、分类、封面、最新章节与目录 URL；
4. **目录**：卷名、章节名、绝对 URL、顺序、去重、分页和更新时间；
5. **正文**：正文选择器、下一页、专属广告清理、图片懒加载属性、相对 URL 与解密；
6. **异常页**：登录页、验证码页、限流页、空响应不能被误当正文成功。

### A3. 书源质量等级

- **可用**：完整链路成功，正文/图片正确，无明显格式污染；
- **受限**：核心阅读可用，但登录、发现、分页或个别类型受限，限制已明确；
- **不稳定**：同一输入有明显间歇失败，需记录失败类别与频率；
- **失效**：核心链路不可用，不能仅凭一次网络超时判定，需区分规则失效、站点故障与网络问题。

现有“响应时间/失效分组”可继续保留，但后续校验结果应逐步从单一成功/失败扩展为“失败环节 + 错误类别”，避免把验证码、限流和选择器失效混为一类。

## 6. 主线 B：App 通用解析优化

### B0. 可观测性先行

在改语义前，先让书源调试能看见各阶段差异：

- 原始响应只显示必要片段和长度/类型摘要，敏感 Header、Cookie 和正文默认遮蔽；
- 显示规则链每一步的输入类型、命中数量、输出长度和耗时；
- 正文调试增加“提取后 / HTML 格式化后 / 书源替换后 / 阅读处理后”分段预览；
- 报错区分网络、HTTP、编码、规则语法、零命中、正文为空、下一页循环、图片 URL 为空和替换超时；
- 支持导出脱敏诊断摘要，不默认导出完整正文。

这一步的价值高于直接新增兼容：没有阶段快照时，格式与替换问题很容易被修错层。

### B1. 解析器一致性与容错

- 为 CSS/JSoup、XPath、JSONPath、正则、JS/WebJS 建立最小契约测试；
- 明确列表/字符串、空值、URL 补全、HTML 实体解码和替换语法的共同语义；
- 规则语法错误应指出具体规则阶段，不以空字符串静默吞掉；
- 保留现有宽松 JSON 兼容时给出调试提示，新建规则优先生成规范格式；
- 下一页增加去重、循环和最大页数防护，顺序拼接必须稳定；
- 并发抓取保持原页序，不因请求完成先后改变正文顺序。

### B2. 格式规范化

建议把格式处理拆成可测试的小步骤，而不是继续扩张一个大正则：

1. 统一换行输入为 `LF`，但先保留原始段落边界；
2. HTML 块标签只产生一个逻辑边界，连续 `br/p/div` 按规则折叠，避免重复跳行；
3. HTML 实体只解码一次，NBSP/全角空格按上下文处理，零宽字符使用明确白名单移除；
4. 清除标签时先保护图片和受支持的 `<usehtml>` 片段；
5. 多页拼接仅清理页边界产生的重复空行，不修改页内段落；
6. 最终阅读段落允许折叠纯空白行，但诗歌、列表、聊天记录、代码/特殊 HTML 不执行强制重分段；
7. 标点和特殊符号只处理可证明为编码/标记残留的集合，不做主观“美化”。

格式修复必须提供正反例：既证明目标脏格式被修复，也证明正常中文标点、英文标点、全角符号、诗歌空行和图片邻接关系未被改坏。

### B3. 替换系统优化

#### B3.1 第一优先级：可解释和可预览

- 编辑规则时可用一段本地样本文本预览“替换前/后”和命中次数；
- 显示本章实际生效规则、执行顺序、每条命中次数和耗时；
- 明确提示零长度匹配、可能跨段匹配、会命中图片/HTML 标签的规则；
- 规则报错或超时时保留原文，并指出具体规则，避免把错误堆栈直接替代正文；
- 支持临时禁用单条规则后刷新本章，便于二分定位冲突。

#### B3.2 第二优先级：保护结构化内容

在不改变现有 `ReplaceRule` 数据库结构的前提下，先采用内部令牌化：

```text
正文 -> 提取并编号保护片段（img/usehtml/受支持标签）
     -> 对普通文本执行默认净化替换
     -> 校验并还原保护片段
     -> 进入排版
```

默认规则只处理正文文字；确实需要改图片 URL 或结构标记的高级规则，继续走兼容路径并给出明显风险提示。这样既保留“替换图片地址/参数”的能力，也避免普通广告规则误删 `<img>`。

图片相关验收至少覆盖：

- `src`、`data-src`、`data-original`、`srcset` 与相对 URL；
- 图片 URL 自带逗号、花括号参数、查询串和转义字符；
- 文字与图片相邻、连续多图、图片前后空行；
- 替换有意修改 URL、无意跨越标签、把 URL 替换为空三种情况；
- 请求头、Referer、Cookie、图片解密和缓存键不因正文净化被丢失；
- 文本书内插图与纯图片书分别验收，不能互相代替。

#### B3.3 第三优先级：规则性能与冲突

- 继续保留单条正则超时，并补充预检测：空模式、尾随 `|`、零长度高频匹配、明显灾难性回溯；
- 缓存已编译正则，但缓存必须有上限并能随规则编辑失效；
- 对规则链统计总耗时和最慢规则，避免一条规则拖慢翻页；
- 记录规则顺序依赖：前一条改变后一条输入时，在预览中可见；
- 作用范围从“字符串包含”逐步补充可解释提示，防止书名或书源 URL 的意外部分匹配；是否改变匹配语义需另行评审，不能在本计划中直接切换。

#### B3.4 图片字替换专项（中优先级、特殊兜底）

这里的“图片替换”特指：某些文本书源只能把正文中的单字或短字串以小图片返回；当必须继续使用该书源且拿不到等价文本时，App 把这些图片嵌入文字行。它不是普通插图，也不是建议所有图片都转成文字样式。

当前实现会把 `<img>` 替换为占位字符参与断行，再用 `ImageColumn` 在对应字符格内绘制位图。此法能保证内容可见，但“格格不入”有明确的技术原因：

- 占位宽度主要来自字体中一个哨兵字符，而不是图片中文字的真实字宽；`text`/`TEXT` 两条路径的宽度语义也不完全一致；
- 位图按字符格宽度等比缩放后在整行高度内居中，没有按字体 baseline、ascent/descent 或实际墨迹边界对齐；
- 图片自带空白边、底色、灰度、抗锯齿和字重，切换字体、字号、粗细、前景色或夜间主题后不会自动融合；
- 图片字缺少真实 Unicode 语义，会影响复制、搜索、朗读、无障碍和准确字数统计。

因此采用三级修复路线，而不是直接放大/缩小所有图片：

1. **优先恢复真实文字**：若站点本质是字体混淆，优先使用已有 `queryTTF/replaceFont` 能力把字形映射回 Unicode；若图片带可靠 `alt/title/data-*` 映射，也在书源解析阶段还原文本。这是唯一能同时解决显示、复制、搜索、朗读和无障碍的方案。
2. **可确定映射时做书源级字典**：图片 URL、哈希或稳定编号能唯一映射到字符时，在对应书源规则中缓存映射并输出文字；映射不确定时失败关闭，不能猜字。
3. **确实只有位图时优化内联渲染**：保留现有图片字兜底，但按实际墨迹边界裁掉透明/纯色留白；以当前字体的 em 高度、baseline 和目标字宽缩放；单色字可选随正文颜色着色，彩色或置信度不足时保留原图；限制最大高度，禁止侵入相邻行，并缓存规范化结果。

不把通用 OCR 作为首选：逐字 OCR 成本高、易误字、影响隐私与离线能力，也无法为错误映射提供可靠回退。只有用户另行授权、存在稳定样本且能做逐字置信度/原图回退时，才作为独立实验评估。

专项验收矩阵：

- 浅色/深色主题，正文颜色变化；
- 3 个字号、正常/粗体、系统字体/自定义字体；
- 行距、字距、两端对齐、首行缩进、分页边界；
- 单个图片字、连续图片字、图片字夹在标点与英文之间；
- Android 8 与当前目标版本，普通翻页与滚动模式；
- 显示失败时保留原图片，不输出错误文字；若已恢复 Unicode，则复制、搜索、朗读和字数结果必须一致。

静态基线显示 45 个文本书源使用 `imageStyle=TEXT`，其中 21 个启用；32 个同时使用书源内替换，25 个正文规则还含 JS/WebJS。因此该专项定为**中优先级**，先从用户实际必须使用且能稳定复现的一个书源做样本，不阻塞书源全集的静态治理与格式主线。

### B4. 请求调度、缓存与失败语义

- 尊重书源并发率，区分搜索批量并发、目录请求、正文分页和图片下载；
- 只对幂等、可重试的网络失败重试，登录失效、验证码、规则零命中不盲重试；
- 取消翻页/切书后及时取消在途正文和图片任务，迟到结果不得覆盖当前章节；
- 缓存身份至少包含书源、书籍、章节和必要规则版本；书源规则改变后能明确刷新旧正文；
- 图片失败采用可重试占位和清晰错误，不把空 URL 当普通网络失败；
- 性能优化以测量为依据，不能用提高全局并发掩盖慢源或限流。

### B5. 书源编辑与调试体验

- 每个字段旁标出“输入类型、输出类型、可用变量和常见错误”；
- 一键从搜索调试贯穿到正文，保留每阶段选择的书籍/章节上下文；
- 规则保存前做静态检查，但警告与阻断分开，兼容旧规则；
- 提供“仅测试当前字段”和“完整链路测试”两种模式；
- 调试结果可复制最小复现，不包含 Cookie、令牌、真实书架数据和完整正文。

## 7. 分阶段实施顺序

### P0：样本与基线（不改功能）

交付物：

- 建立**可重复运行**的本地只读清单工具，对 `bookSource.json` 的当前快照（2026-09-21 为 6139 个唯一 URL）按启用状态、书源类型、规则结构指纹、JS/分页/替换/图片特征分群；工具不写死条数，书源新增后重跑即自动纳入；
- 对全部书源做离线静态检查；联网测试只从分群中选代表样本，避免把重复合集当成新书源反复请求；
- 收集 3—5 个脱敏最小内容样本，至少覆盖普通 HTML、JSON 接口、正文分页、文本内插图和纯图片书；图片字专项另取 1 个用户必须使用的目标源；
- 对每个问题填写书源诊断卡，确认归属层；
- 记录当前输出、耗时、命中数和失败类别；
- 冻结兼容样本：正常标点、诗歌空行、相邻图片、特殊 HTML、已有替换规则。

退出条件：每个准备实施的问题都能离线复现，或明确标记为“仅真实站点可复现”；没有样本的问题不进入通用层改动。

### P1：离线测试与阶段诊断

交付物：

- `AnalyzeRule`、`HtmlFormatter`、`ContentProcessor`、`ReplaceRule` 的 JVM 契约测试；
- 正文四阶段调试摘要；
- 统一错误分类与脱敏规则。

退出条件：不改变最终正文语义；既有构建与全量 JVM 单测通过；诊断信息能指出问题发生在哪一层。

### P2：先修具体书源

交付物：

- 按诊断卡逐个修复已确认书源；
- 每个源保留搜索→详情→目录→正文/图片的完整回归记录；
- 网站专属格式问题优先落在 `ContentRule.replaceRegex` 或该源规则中。

退出条件：目标源的复现样本通过，且没有为了单一源改变通用解析语义。

### P3：通用格式规范化

交付物：

- 换行、实体、不可见字符、HTML 边界和多页拼接的小步修复；
- 每项修复都有正例、反例和兼容样本；
- 格式化步骤职责拆清，避免同一字符在多层重复处理。

退出条件：目标格式问题全部通过；正常标点、段落、诗歌、图片和特殊 HTML 无回归。

### P4：替换与图片保护

交付物：

- 替换预览、命中次数、耗时和风险提示；
- 结构化片段保护及还原校验；
- 图片 URL 专项用例与失败语义；
- 超时/异常时保留原文，不以错误文本污染正文。
- 将图片字作为中优先级子任务：先判断能否恢复 Unicode；不能时再做基线、墨迹边界与主题适配，不与普通插图逻辑混改。

退出条件：普通净化规则不再误伤图片；高级图片 URL 替换仍兼容；规则冲突可定位；文本书和图片书真机通过。若本阶段纳入图片字专项，还需目标源在至少两种主题和三档字号下完成真机对比，且失败时保持原图可读。

### P5：请求与性能优化

交付物：

- 分环节耗时基线、慢规则/慢请求定位；
- 可控重试、取消、分页顺序与并发率验证；
- 缓存刷新边界说明。

退出条件：正确性无回退；在同样样本和网络条件下，P50/P95 耗时或失败率有可量化改善。

## 8. 测试矩阵与验收标准

### 8.1 自动化测试

| 维度 | 最小覆盖 |
| --- | --- |
| 响应类型 | HTML、JSON、纯文本、错误页 |
| 解析方式 | CSS/JSoup、XPath、JSONPath、正则、JS（可离线部分） |
| 正文 | 单页、多页、空正文、重复标题、特殊字符、混合换行 |
| 结构 | 纯文本、文本+图片、连续图片、`usehtml`、相对 URL |
| 替换 | 普通字符串、正则、捕获组、跨行、零命中、超时、规则链冲突 |
| 书源类型 | 文本、图片为必测；音频、文件、视频做不回归验证 |
| 兼容 | 现有书源 JSON 导入、现有替换规则、旧缓存正文 |

### 8.2 真机验收

- 目标书源完成搜索→详情→目录→打开正文/图片的完整链路；
- 连续翻阅至少 5 章，覆盖首章、中间章、多页章和含图章；
- 切章、刷新、换源、缓存后结果一致，不出现迟到内容覆盖；
- 开关“重新分段”“净化替换”分别验证，能明确判断是哪层造成格式变化；
- 替换规则预览与阅读页实际结果一致；
- 图片点击、预览、保存、刷新和失败重试行为正常；
- 调试/日志中不出现 Cookie、密钥或完整真实正文。

### 8.3 工程门禁

每个代码阶段至少执行：

```powershell
$env:GRADLE_USER_HOME = "D:\gradle_home"
.\gradlew.bat :app:compileAppDebugKotlin
.\gradlew.bat :app:testAppDebugUnitTest
```

发版候选另补：

```powershell
.\gradlew.bat :app:lintAppDebug
```

涉及图片、WebView、JS、请求头、Cookie、真机排版或并发的改动，不能只凭 JVM 单测宣称完成，必须补相应真机证据。

## 9. 衡量指标

不以“规则更多”作为成果，以以下指标判断：

- **正确性**：规则命中数、正文空结果率、目录/正文成功率、图片有效 URL 比例；
- **格式质量**：异常符号、重复空行、错误分段、标签残留的样本通过率；
- **稳定性**：同输入重复运行结果一致；分页无循环、丢页和乱序；
- **可解释性**：能从诊断信息定位到请求、规则、格式、替换或排版层；
- **性能**：搜索/目录/首屏正文的 P50/P95 耗时，最慢规则耗时，取消后的残留请求数；
- **兼容性**：旧书源、旧替换规则、文本书、图片书和本地书阅读路径无回归。

## 10. 风险与回退

| 风险 | 护栏 | 回退方式 |
| --- | --- | --- |
| 全局格式化误改正常正文 | 先离线样本、只做保守规范化、正反例成对 | 单项格式步骤可独立撤回 |
| 替换保护改变旧规则语义 | 默认兼容、预览告警、高级规则保留原路径 | 关闭保护路径恢复旧执行器 |
| 图片令牌还原失败 | 数量/校验和一致后才提交结果 | 整章保留原文与原图片标签 |
| 调试信息泄露内容或凭据 | 默认摘要、字段级脱敏、手动导出 | 关闭详细调试并清理临时数据 |
| 网络优化触发站点限流 | 尊重书源并发率、按错误分类重试 | 恢复原并发/重试策略 |
| 网站变化导致真实源测试波动 | 离线夹具是回归主证据，真实源是补充证据 | 标记站点状态，不误判解析回归 |

## 11. 预计涉及模块

| 模块 | 计划职责 |
| --- | --- |
| `model/analyzeRule/` | 规则解析一致性、错误定位、URL/列表语义 |
| `model/webBook/BookContent.kt` | 正文提取、分页拼接、书源内替换 |
| `utils/HtmlFormatter.kt` | HTML、换行、实体和图片标签保留 |
| `help/book/ContentProcessor.kt` | 阅读前格式处理、用户替换、段落输出 |
| `data/entities/ReplaceRule.kt` | 规则校验、超时与兼容语义 |
| `ui/book/source/debug/` | 分阶段预览、诊断摘要、完整链路调试 |
| `ui/replace/` | 替换预览、命中/耗时/风险提示 |
| `model/ImageProvider.kt`、`help/book/BookHelp.kt` | 图片 URL、下载、解密、缓存与错误语义 |
| `app/src/test/` | 离线样本、契约测试与回归矩阵 |

上表是影响面清单，不表示所有文件必须同时修改。实施时应按阶段拆小任务，先测试后改代码。

## 12. 建议优先级与首批切入点

建议顺序为：**P0 样本基线 → P1 阶段诊断与测试 → P2 具体书源 → P3 格式 → P4 替换/图片 → P5 性能**。

首批不建议直接重写解析器。最合适的切入点是：

1. 对当前快照（2026-09-21：6139 个唯一 URL）做离线静态体检和规则分群，先处理启用集，再覆盖音频/图片/文件等少数类型；体检脚本可重复运行，新增书源重跑即纳入；
2. 选取一个“多符号/异常字符”样本、一个“跳行”样本，以及一个必须继续使用的“图片字”目标源；
3. 为三者记录四阶段快照，判断问题分别来自书源、格式化、用户替换还是内联图片排版；
4. 先补 `HtmlFormatter` 与 `ContentProcessor` 离线回归测试；
5. 图片字先尝试恢复 Unicode，只有确实无文本映射时才优化位图基线与主题融合；
6. 如果普通替换会误伤图片标签，再实施结构片段保护；
7. 只有出现跨源共同证据，才调整 `AnalyzeRule` 或通用格式语义。

## 13. 启动前待确认

- 首批要处理的具体书源及可复现章节/页面；
- “多出符号”和“跳行”的实际截图或脱敏前后文本；
- 图片字专项的首个目标书源与复现章节；当前已明确它是必须使用特定资源时的特殊兜底，优先级为中等；
- 是否允许使用目标书源做联网真机测试；若允许，授权范围应限定到指定书源和脱敏样本；
- 首批范围是否只做 P0/P1 诊断，还是连同一个具体书源修复一起执行。

## 14. 与已封存的 AI 正文净化功能的关系

AI 正文净化功能自 2026-09-19 起按实验性质封存：发布与调优暂停，代码仍在主干并随构建编译（`AGENTS.md:36`）。本节只回答三件事——封存替本计划卸掉了什么、封存不解除什么、本计划能给将来的 AI 计划留下什么。

### 14.1 封存卸下的约束

封存的是 AI 侧，不是本计划。用户已裁定：**本计划按解析目标自身取舍，不考虑改动会不会波及 AI 侧**。旧稿中为保护 AI 侧状态而设的整套要求据此撤销，不再作为设计前提或验收条件：正文必须逐字符不变、段首缩进不得动、每项改动附前后 SHA-256 对照、本计划读数与封存时基线必须可比、各阶段附加 AI 侧交付物。由此确立三条：

- 本计划**不因** AI 缓存键、AI 缓存命中率、封存时基线的可比性而限制改动范围；
- 本计划**不产出**任何「对 AI 有影响 / 无影响」的判定，也不把它当作设计理由或验收结论——附不附对照读数都不产出。某项改动是否需要前后读数，只按本计划 §8 的验收标准判断；
- AI 若恢复，由 AI 侧**自行重新取基线**；本计划不承担保全或重建旧基线的责任。

**边界按「AI 专属代码」还是「共享代码」划，不按「谁读它」划。** AI 专属代码指 `help/ai/` 目录与 `ReadBook` 中的 AI 分支（14.2 第 2 条）。其余是共享代码：`AppPattern` 的 `imgPattern`/`useHtmlRegex`（`constant/AppPattern.kt:14`、`:17`）、`BookContent` 的数据形状（`help/book/BookContent.kt:5-14`）、`ContentProcessor` 的段落输出、`ReadBook` 的换章失效守卫——它们恰好也被 AI 读取，但归本计划可改面，不必为「AI 也在读」保留原语义。反之，删除或停用 AI 功能本身属于解封动作，不在本计划范围内。卷首第 6 行「不触碰已封存的 AI 正文净化功能」按 14.2 的四条理解，不含本节撤销的那些限制。

### 14.2 封存不解除的四件事

1. **不实现 AI 功能**：不接模型、不配 API Key、不做 AI 分块 / AI 校验 / AI 缓存 / AI UI；不新增任何为 AI 预留的字段、表、开关、接口或 TODO，也不把「将来 AI 需要」当作某处设计选择的理由——设计理由只能是书源、解析、格式、替换或排版本身。

2. **不动 AI 的代码与分支**：`app/src/main/java/io/legado/app/help/ai/`（20 个 Kotlin 文件）与 `app/src/test/java/io/legado/app/help/ai/`（22 个测试类）零改动，也不得为「适配」或「绕开」改其符号或可见性；`ReadBook` 中 AI 专属的部分同样不动——`AiPrepared`/`aiPrepared`（`:88-89`）、`activateCurrentAi`（`:568-592`）、`retryAi`（`:594-610`）、`prepareDisplayContent` 的 `buildScope`/`resolve` 调用（`:819`、`:828`）、`display.isAi` 的失效判断（`:866-875`、`:966-975`）。

   冻结的是这些分支的**行为**，不是它们的字面行。换章失效守卫 `aiEpoch`（`:73`，在 `durChapterIndex` setter `:77` 与 `releaseAndCancel()` `:1194` 自增）是整条阅读链路的通用守卫：`prepareDisplayContent` 与 `contentLoadFinish` 的各装载分支（`:823`、`:863`、`:901`、`:913`、`:963`、`:1002`、`:1015`）都用它判「章节是否已切换」，其中 `contentLoadFinish` 的六处是先判章节、再判 `display.isAi`。它正落在 §6 B4 第 3 条的取消语义上，属于**本计划可改面**：可改名、可重构其检查点，不必另立第二套守卫；同步更新 AI 分支中的引用属允许的机械改动，只要不改那些分支的判断语义。

3. **不改 AI 的编译与发布形态**：不用构建身份、R8 规则或 CI 配置隔离、剔除或重新打包 AI。`app/build.gradle:39-43` 的 `RELEASE_IDENTITY` 只改 `applicationIdSuffix`（`:101-102`），不参与源码剔除；ProGuard 规则与 CI 工作流当前对 `help/ai` 零命中，保持这一状态。封存是发布暂停，不是代码移除，改其编译形态本身就是解封动作。

4. **AI 测试不构成否决权**：§8.3 的 `:app:testAppDebugUnitTest` 会连同上面 22 个测试类一起执行。若本计划按解析目标所做的有意改动使其中某个 AI 测试失败，**以本计划的改动为准**——该失败不阻断本计划的推进，也不要求为它改回设计。处置：确认失败确由本计划的有意改动引起 → 就地记录（测试名、改动位置、为什么必须这样改）→ 继续推进。**不得**为恢复绿色去改 AI 代码或 AI 测试（那属于 14.2 第 2 条禁止的改动）。区分两种失败：来源于本计划有意改变语义的，按上述处置；来源于本计划自身实现缺陷、或波及 app 主源集编译与非 AI 测试的，按 §8.3 常规门禁正常修复。

> 术语不要混：§6 B4 的「缓存身份」指 `externalFiles/book_cache` 下的 `.nb` 正文缓存（`help/book/BookHelp.kt:60-61`、`data/entities/BookChapter.kt:192`），与 AI 缓存无关；§7 的「阶段 P0–P5」不是 AI 计划的阶段 A–E；「净化」指 `ReplaceRule` + `ContentProcessor`（§3.2）。同名术语各自定义，避免两类改动被接到一起。

### 14.3 参考价值：形状，不是桩

用户保留的诉求是「本计划可作为将来 AI 计划在解析方面的参考」。参考价值只能来自本计划按自己的目标做出来的形状，不能来自为 AI 预留的接口：

| 形状 | 本计划在哪产出 | 将来 AI 计划可直接复用什么 |
| --- | --- | --- |
| 链路分层，并说清哪一步产生正文文本 | §3.1；P3 段落与格式 | 定位 AI 输入口，判断改哪一层会改变模型输入 |
| 结构化片段「提取—编号—只在普通文本上处理—校验还原」 | §6 B2 第 4 条、§6 B3.2 | 分块与保护的还原契约（同构实现，不共享代码） |
| 四阶段可观测性：提取后 / 格式化后 / 替换后 / 阅读处理后 | §6 B0、P1 交付物 | 模型前后对照所需的读数口径与导出形状 |
| 失败分类，以及「超时或异常时保留原文」 | §6 B0、§6 B3.1 | 失败可解释性的分层方式，避免错误文本顶替正文 |
| 离线夹具 + 正反例成对的回归结构 | §8.1 测试矩阵、P0 冻结样本 | AI 侧回归的夹具组织与兼容边界写法 |

这五项都是本计划为自己产出的，AI 只是顺带受益。**参考价值体现在形状，不体现在桩**：不为 AI 新增任何预留物，也不为「将来好接入」而改变上面任一项的形状。

### 14.4 封存完整性与对外口径

- §13 第 4 条「是否允许使用目标书源做联网真机测试」的授权只覆盖书源抓取与真机验证，**不构成**恢复 AI 外发的授权：AI 外发仍以 AI 侧自己的设备同意为前提，本计划的任何授权都不能替代它。若某次会话把它解释成「可以顺便开 AI 验证」，按越界处理。§13 也无需为此新增待确认项——AI 边界不降级为每次施工都要重新确认的前提。
- `AGENTS.md` 已登记 AI 封存（`:36`）。其文档索引中原本把 AI 阶段 E 计划书标为「当前执行入口」的表述（`:37`）已于 2026-09-21 改为归档标注，并把当前推进计划指向本书。索引调整不改变封存状态：AI 工作的恢复仍需另行授权。
- 「本计划可供将来 AI 计划参考」这类表述**仅限本计划书内部**：不得写入 `README.md`、`app/src/main/assets/updateLog.md`、`api.md` 或发布说明，也不得作为「AI 已可用 / 已验收」的依据。这三个文件当前对本功能零命中（已核对），是需要保住的状态。
