# ai-stage-e-anchor-proof — 阶段E：ANCHOR 修复的真机固定字段取证

状态：`ready`

## 目标与非目标

- 目标（承接 `ai-stage-e-device` 的 provisional 缺口，补齐"只从固定日志字段读取结论"这一判定依据）：
  1. 在设备上**开启 `recordLog`**（我的 → 其他设置 → 记录日志），使 AI 诊断字段进入 App 日志；
  2. 对**能触发重复锚点编辑**的样本**强制重跑**（`重新处理本章`），**每样本前后各读一次 `ai_stats` 计数并取差值**，当场抄录；
  3. 从内置日志查看器导出 AI 固定字段（`ai-ok` / `ai-editkind detail=…` / `ai-fail code=… anchor=… chunk=N` / `ai-protocol detail=…`），据此判定：**是否触达组合锚点路径**、**是否仍出现 `ANCHOR ambiguous + changed`**；
  4. 结束后**恢复 `recordLog` 为关闭**（沿用项目既有约定），并复核。
- 非目标：
  - 不修改任何业务源码/测试/Prompt/缓存版本；
  - **不重新构建、不重新安装**（设备上已装的 `3.26.091915` 即已提交的 `6a7a2143a` 内容，无需重建）；
  - 不做发布判定（D2 另行，且必须在**生产分块 600** 下重测）；
  - 不更换 App 内 AI 目的地、模型或密钥；不卸载、不清数据。

## 外发授权（用户 2026-09-19 授权，逐项）

- 目的地 / 模型：**沿用 App 内已配置的 AI 服务与模型**（**名称不记录**）。
- 正文范围：**1–2 个样本**，优先选"开头存在重复标题/重复锚点"的样本（即上一任务样本 3）。
- 真实请求上限：**2 次**；每次前后必须读 `ai_stats` 取差值，**读不到即停止并写 `NOT RUN`**，不得凭推测推进。
- **分块上限的临时调整（controller 2026-09-19 补充授权，理由与代价都写明）**：本次取证前读到该样本的累计计数为 **请求 6 块 · 发送 4470 字符** → 在生产分块 600 下处理一整章约需 7 次请求，**必然突破"≤2 次"上限**（触发本任务书停止条件）。为在授权内取得固定字段证据，允许**临时**把分块上限改为 App 自带默认值 `6000`（4470 字符 → 1 块 → 1 次请求）；取证后**立即还原为 600** 并复核。
  **代价必须随结论一起写**：该证据成立于 **6000 分块**，**不是**生产配置；因此只能用于判断"锚点路径是否被触达、是否仍失败关闭"，**不得**用于发布判定（D2 必须在 600 下重测）。
- 证据边界（**上一任务 F2 的教训，本次为硬要求**）：只记固定脱敏字段、块数、字符数、状态文案与哈希。**书名、章节名、正文、响应体、异常 message、服务地址、密钥、模型名一律不得进入任何证据文件**；样本一律以「样本 A/B」编号。

## 实时基线

- 任务风险档：standard
- Git 仓库根：D:/vsproject/legado-E
- 分支：codex/manga-import
- Git HEAD：daab2ea440c056257075c17aee85ae5b86017cae
- 修前工作树摘要：clean
- 修前快照 SHA-256：d1ed1a8ca47a6759f2124c2ac98bcee0f7e1a6cb6aebcddbc7be82fa3344185b
- 当前 route_revision：1

## 复审冻结与轮次

- 最大复审轮次：2
- 复审冻结窗口：reviewer 启动至回执完成期间，授权文件、任务书与执行报告不得变化；runner 前后快照不一致时本轮报告失效。
- reviewer 可执行检查：只允许 `06-review-commands.json` 中冻结的参数数组；空数组表示 `NOT RUN`。
- 网络策略：本任务**需要**受控真实外发（范围以上一节为准），其它网络行为禁止。

### 验证对象修订（controller 2026-09-19，按复审 F1 的第二个选项）

复审指出：本报告作为主证据的 6 条 `code=ANCHOR anchor=ambiguous edit=changed chunk=1` 日志产生于 **16:06–16:12**，而本任务**首次触发在 16:2x**；本任务自身唯一一次请求跑在**临时 6000 分块**下且**成功**。因此：

- **本任务未在 600 分块下执行过任何一次请求** → 完成条件第 2 项（每样本前后 `ai_stats` 差值）**实质未达成**，一律写 `NOT RUN`，且**不得**据此下『该修复在 600 下被证伪』的肯定结论；
- 那 6 条日志的**验证对象口径**正式修订为：**任务窗口之外的既有观测**（含修复构建安装后、600 配置下、由非本任务触发）。报告必须以『既有观测（非本任务产生、未与 ai_stats 差值绑定）』标注，结论强度上限为**疑似**；
- 要升级为受控结论，必须在 **600 分块**下做一次受控重跑（该样本约需 7–8 次请求），**超出本任务 2 次上限**，须由用户另行授权后另立任务。

**证据抄录格式（按复审 F6 收窄）**：不得抄录 PID/TID；改用『时刻（到秒）+ 线程组编号』两列，并保留由线程组得出的分析结论。

## 角色与授权

- controller：dsh
- executor 可读 / 可改：读 `00-task.md`、`05-routing.md`、`14-executor-prompt.md`、`.agent-protocol/README.md`、`CURRENT.md`、`PROJECT.md`，以及上一任务 `../ai-stage-e-device/30-acceptance.md`（了解缺口口径）。
- reviewer 可读：`00-task.md`、`05-routing.md`、`06-review-commands.json`、`10-execution.md`、`14-reviewer-prompt.md`、`18-review-probes.md`。禁止读取凭据、`keystore*`、`look.ps1`、真实书源/正文、用户缓存、未脱敏日志。
- 沙箱提权：**需要**（`adb` 读写设备、`gradlew` 仅在被要求重建时才用）。本任务书预先一次授权：`adb` 对 `10CE5P1M1Z001P9` 的读写，档位 `danger-full-access`；一次一申请，不得批量扩大。
- **正式文件白名单：无。** 本任务**不得改动任何业务文件**（含 `app/build.gradle` 与 `AndroidManifest.xml`）；证据只写 `.agent-protocol/tasks/ai-stage-e-anchor-proof/**`。
- 允许产生的临时目录：系统临时目录、`D:\dsh-temp`；设备侧截图/dump 一律留在临时目录，脱敏后仅把固定字段抄入证据。
- 收尾前必须清理：`recordLog` 恢复关闭（复核）、设备侧临时文件、本任务产生的临时 dump。
- acceptor：dsh
- 外部目的地与模型：App 内已配置（**不记录名称**）。
- 明确禁止的数据和操作：凭据/Token、`keystore*`、`look.ps1`、**书名与章节名**、正文与响应体、模型名、卸载、清数据、提交、推送、切分支、发布、安装其它软件。

## 完成条件

- [ ] `recordLog` 开启并复核（保留开关状态证据）
- [ ] 每样本处理前后 `ai_stats` 计数**当场读取并给出差值**；读不到即 `NOT RUN` 并停止
- [ ] 至少 1 条 AI 固定字段日志被导出并抄录（若模型未提供上下文锚点，如实写"该样本未触达锚点路径"，不得推断）
- [ ] `recordLog` 已恢复关闭并复核
- [ ] 业务树零改动（`git status --porcelain` 仅协议文件；冻结命令覆盖）
- [ ] 未运行项一律写 `NOT RUN` 并说明缺什么才能判断

## 停止条件

- 需要超过 2 次请求，或需要更换目的地/模型；
- 任一 `ai_stats` 计数读不到（停止，不下结论）；
- 需要卸载、清数据或改动业务代码；
- 当前模式不满足任务要求的保证等级。

## 风险档规则

- `light`：只允许文档与说明性文件；`standard`：普通代码、启发式和配置行为，独立 reviewer、冻结快照、最多两轮；`high`：安全、权限、迁移、协议脚本、发布与高影响数据变更，需独立 acceptor。
