# ai-stage-e-anchor-proof — 最终验收

状态：`provisional`

- **acceptor 运行实例**：人类启动的 DSH 会话（与 controller/executor 同一实例）。独立复审为两个不同的 claude 会话：round 1 `eaaff58f-e8a4-4f27-be16-7d390141c3fd`、round 2 `53664ca5-d9b8-4564-b344-da61d952957c`。
- **mode / assurance / route_revision**：dsh-claude / standard / 1
- **实际 diff**：**业务代码零改动**。冻结命令 `git status --porcelain` 输出仅 ` M .agent-protocol/CURRENT.md`（协议文件；其中尾部多余空行已由 controller 清除），`git diff --check` **PASS**（重跑后）。
- **acceptor 现场复核（设备侧，无新外发）**：
  - `ps -A -o PID,ETIME,NAME`：**PID 18311 = io.legado.app.tjf.releaseS，且是设备上唯一的 legado 进程**（无共存包混淆）→ 与 `dumpsys` 的 `versionName=3.26.091915`、`lastUpdateTime=2026-09-19 15:51:51` 合并，**绑定成立**：本报告引用的 16:06–16:12 日志行**必然**由含修复的构建产生。
  - 全量 logcat 检索 `ai-ok` / `ai-protocol`：**0 条命中** → 报告『无证据表明组合锚点路径被有效利用』的**检索动作已补做**，结论为『已检索，未找到』（该构建对成功路径不留固定字段）。
  - 还原复核：分块上限 **600**、recordLog **checked=false**（均已当场 dump 核对）。
- **可见行为**：本任务唯一一次请求（6000 分块）→**AI 正文已就绪**；窗口外既有观测（600 分块）→ **6 条** `code=ANCHOR anchor=ambiguous edit=changed chunk=1`。
- **NOT RUN**：600 分块下的受控重跑（约需 7–8 次请求，超出本任务 2 次上限）、同身份 ai_stats 前后配对、D2 发布矩阵、缓存命中对照、提交/推送/发布。

## 复审 findings 逐项处置

**round 1**
- **F1（高｜核心结论依赖窗口外证据）**→ **已按复审给出的第二个选项处置**：任务书新增「验证对象修订」节（验证对象＝任务窗口之外的既有观测；结论强度上限为**疑似**；并明确『要升级为受控结论必须 600 下受控重跑，需另行授权』），执行报告同步重写。round 2 §三 已确认**逐条一致、无偏差**。
- **F2（高｜ai_stats 差值未达成）**→ **接受**：报告中该项已明确改写为 `NOT RUN` 并移除依赖它的推断。
- **F3（高｜6000 用错对象）**→ **接受**：6000 只兑现了『成功对照』，未兑现『失败固定字段』；已如实写入报告，并转为后续任务第 1 项。
- **F4（中｜日志窗口与 recordLog 绑定缺失）**→ **已闭合**：acceptor 复核——内置内存日志最早条目为 **16:19**（即 recordLog 开启时刻），而引用的日志行产生于 **16:06–16:12**，且内存日志内**无任何 AI 行** → 该批诊断**不经 recordLog 门控**、直接进 logcat，取证窗口合法。报告已补写该论证并说明改用 logcat 的理由。
- **F6（低｜抄录含 PID/TID）**→ **已改**：修订版仅保留『时刻 + 线程组甲乙』，并保留线程组分析结论。
- **F7（低｜探针 FAIL 与冻结核对不完整）**→ **已修**：`CURRENT.md` 尾部多余空行已由 controller 清除，探针**重跑 PASS**；`.review-snapshot.json` 的 `after` 缺失属 runner 侧限制，已在本文件与报告中标注。

**round 2**
- **F1（中｜把可避免的操作缺口写成固有约束）**→ **接受，且不再辩驳**：本次只读了『切换前（600 身份）』与『切换后结果』，**中间少读一次同身份基线**；『差值必然不可得』的说法**不成立**，正确表述是『本次未取得同身份配对（操作缺口）』。**未在本任务内修改报告**：复审轮次上限（standard = 2）已用满，复审后改动 10-execution.md 会破坏冻结快照并使 close 失败——故按协议转入**后续任务第 1 项**，并规定流程为『配置落定 → 读基线 → 触发 → 读结果 → 算差值』。
- **F2（中｜本任务成功运行未抄录固定字段）**→ **已闭合**：acceptor 现场全量检索 `ai-ok` / `ai-protocol` ＝ **0 条**（见上），即『已检索，未找到』；该结论不是『没去找』。
- **F3（中｜构建归属为超范围断言）**→ **已闭合**：acceptor 以 `ps` 证明 PID 18311 即唯一 legado 进程 `io.legado.app.tjf.releaseS`，其 `versionName=3.26.091915`（15:51 安装）→ 归属成立，不需要 PID 抄进证据。
- **F4（中｜3470 与 4470 差 1000 未解释）**→ **如实登记为未解释**：可能因书源目录刷新使同序号章节内容变化；该差值**不作为任何结论依据**，也不影响『6 条失败 ↔ 请求 6 块』的数量吻合。

## 对核心问题的权威口径（本文件为准）

> **证据高度指向『修复在生产分块 600 下未生效』，但本任务未做受控复现，故不写成已证伪。**

支撑：①6 条 `ANCHOR ambiguous + changed chunk=1` 已由 acceptor 绑定到**含修复的构建**（唯一 legado 进程 + 版本/安装时刻）；②当日同一时段 App 配置为**生产分块 600**；③该样本计数『请求 6 块』与 6 条失败数量吻合（每次失败停在第一块）；④同一样本在 6000 分块下本任务 1 次即 Ready → 失败强烈依赖分块粒度。

**未闭合的部分**：这 6 次**不是本任务触发**、**未与同身份 ai_stats 差值绑定**，触发方与运行参数（除分块上限外）无第三方记录；因此**不得**据此进入 D2 或发布。

## 最终判定

`provisional`

- **理由**：本任务把『固定字段证据』这一缺口**实质性推进了**（拿到 8 条固定字段、完成构建归属绑定、完成 recordLog 门控论证、业务树零改动、探针 PASS、设备侧全部还原），但完成条件第 2 项（同身份前后差值）**未达成**、核心问题**未受控复现**，且复审两轮均判『不能按通过验收』。故不给 `accepted`。
- **未达成/不可判项**：①600 分块下的受控复现（NOT RUN）；②同身份 ai_stats 配对（NOT RUN）；③『模型未给上下文』与『组合仍不唯一』的区分（现有日志无法区分）；④D2 发布矩阵。
- **唯一下一步**：**需用户就外发范围做一次新决策**——是否授权在 **600 分块**下对该样本做 1 次受控重跑（约 **7–8 次真实请求**，因每章在 600 下会被切成约 7 块）。授权则另立任务，按『配置落定 → 读基线 → 触发 → 读结果 → 算差值』取证，并同步检索 `ai-ok`/`ai-protocol`；不授权则以本文件的『证据高度指向』口径收尾，直接转入修复方案的重新决策。
- **提交 / 推送 / 发布状态**：**未提交、未推送、未合并、未发布**（协议证据文件另行决定是否入库）。发布判定维持 **review / NOT READY**。

## 后续任务（按优先级）

1. **600 分块受控复现（需新授权，约 7–8 次请求）**：同身份基线配对 + 窗口内固定字段；这是把口径从『高度指向』升级为『受控结论』的唯一途径。
2. **新增固定字段**区分『模型未提供上下文』与『组合仍不唯一』（代码改动，需另立任务与复审）。
3. **修复方案再决策**：若第 1 项确认失败，则锚点上下文方案在生产配置下失效，需在①强化 Prompt/schema 强制上下文、②改用不依赖模型上下文的策略（扩写 `original` / 段落对齐 + 偏移交叉校验）、③调整生产分块上限 之间做一次显式决策。
4. **D2 发布矩阵**：必须在 **600 分块**下全量执行后才可进入《发布推送流程书》§4。
