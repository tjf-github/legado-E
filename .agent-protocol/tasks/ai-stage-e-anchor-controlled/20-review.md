# 复审报告（第 2 轮 / 最大 2 轮）— ai-stage-e-anchor-controlled

## 一、结论

**有阻塞项（不建议本轮出具 PASS）。** 核心问题（"含修复的构建在生产分块 600 下是否仍以 `ANCHOR ambiguous + changed` 失败"）在现有证据下为 **无法判定**：唯一一次"通过"的观测缺决定性读数、其前置操作未经授权、且其与既有失败观测的"同一构建/同一条件"前提无任何证据。第八节的自更正方向正确，但**正文与冻结的 `05-routing.md` 仍以未证明事实作为"已确证"对外输出**，会被后续任务承接。

复审所处轮次为最后一轮。下述阻塞项**均无法在本任务白名单内闭合**（需重跑取证 / 新增固定字段 / 代码改动），故应由 acceptor 按"有阻塞项 + 核心结论无法判定"裁定，或另开任务。

---

## 二、发现

### F-A（高 / 阻塞）停止条件已被触发却继续执行；未证明项仍以"已确证"形式对外输出
- 位置：`00-task.md:52`（完成条件 2）、`00-task.md:61-63`（停止条件"任一 `ai_stats` 读数读不到（停止，不下结论）"）；`10-execution.md:21,34,41-43`；`05-routing.md:18,22`。
- 事实：`10-execution.md` 第八节 F1 已自认"『触发前』栏填的是状态文案而非 `ai_stats` 数值""『一次通过』与『6 块』均未获独立证明"。但正文 `:34`（结论 1）、`:21`（用量表）、`:41-43`（计账）、`:70`（建议）仍以既成事实陈述；`05-routing.md:18` 更把它写成"**已确证结论（可承接，不必重跑）**"。
- 具体失败场景：下一个任务读取 `05-routing.md:18` 的"不必重跑"，直接继承"间歇性失败"作为前提，跳过复跑 → 一个未证明的结论被固化进后续阶段 E 的结论链，且再也无人复测。
- 建议：在 `10-execution.md` 正文对应行就地加"未证明 / NOT RUN"标注（而非仅在末节追认）；由 controller 更正 `05-routing.md:18` 的"已确证"措辞为"待复核"。

### F-B（高）"间歇性"结论缺少构建/条件同一性证据
- 位置：`10-execution.md:34`（"既有观测（上一任务，含修复构建、600 配置）为 6 次…"）。
- 事实：全篇未记录任何构建同一性字段（versionCode、安装时间、APK 摘要等），也未记录两次观测的分块/超时读数原文。任务书要求的是"含修复的构建"这一次构建身份下的判定。
- 具体反例（会直接推翻结论）：若上一任务那 6 次失败发生在**引入上下文锚点修复之前**的构建（或该次安装后被覆盖升级），则"1 次成功 + 0 次失败"与"修复确定性生效"完全一致，"间歇性、取决于模型这一次输出"（`:35`）即为错误推断。
- 建议：补记构建同一性字段（versionCode + 安装时刻），二者不属任务书 `00-task.md:19` 的禁止项。

### F-C（高 / 中）唯一一次"通过"建立在计划外的设备状态改动之上，且构成因果混淆
- 位置：`10-execution.md:10`（自述"清理 AI 正文缓存"）、`00-task.md:13`（非目标：不改配置/缓存版本）、`00-task.md:48`（明确禁止：清数据）。
- 事实：报告自认首次触发无请求，随后执行了设置页"清理 AI 正文缓存"再重试，才出现状态迁移。该操作不在授权步骤内（`00-task.md:9-12` 只含 清 logcat / 开 recordLog / 读基线 / 触发 / 读结果 / 复原）。
- 双重问题：① 授权边界——"清理 AI 正文缓存"是否落于 `:48` 的"清数据"禁止项，应由 acceptor 明确裁定（executor 已上交 F6/F7）；② 因果混淆——**成功观测的初始条件与失败观测的初始条件不同**，因此即便成功成立，也不能与既有失败直接对比来支撑"间歇性"。
- 建议：acceptor 在 `30-acceptance.md` 中对 ① 给出明确结论；若判为越界，则本节的"通过"不得进入对外结论。

### F-D（中）recordLog 未开启，核心取证结果缺前提且辩护不可核实
- 位置：`00-task.md:9`（第 2 步：清空 logcat **并开启 recordLog**）；`10-execution.md:23`（"recordLog 全程为关闭"）、`:49`（自述偏差）、`:71`（F2 承认缺前提）。
- 事实：辩护理由是"已证明 logcat 不受其门控，见上一任务验收"，但该验收文件（`../ai-stage-e-anchor-proof/30-acceptance.md`）**不在 reviewer 白名单**，我无法核实。
- 具体失败场景：若 AI 固定字段实际受 recordLog 门控，则"窗口内 0 条固定字段"（`:22`）整体无效——本任务的取证目的即落空；此时 F2 的加限还不够，应是"该项取证 NOT RUN"。
- 建议：要么补一次开启 recordLog 的受控复跑，要么请 controller 把上一任务的证据一并冻结进白名单，使该辩护可被独立复核。

### F-E（中）内部数字自相矛盾，且复用被自己撤回语义的计数
- 位置：`10-execution.md:34`（"上一任务…为 **6 次** …失败"）／`:42`（"窗口外既有观测（非本任务）：6 次失败"）／`:43`（"上一任务 **1 块**"）。
- 事实：若那 6 次失败属实且每次都失败于 `chunk=1`（每次运行仅发 1 块），则上一任务至少消耗 6 块，与 `:43` 的"1 块"矛盾；若那 6 其实是界面"请求 6 块"的显示值，则结论 1 的前提正是第三节 `:27-30` 自己声明要警惕的语义误读。
- 建议：把"失败行数 / 请求块数 / 运行次数"三个量显式分开计数，并附原始行；否则结论 1 的量化前提（"6 次"）不成立。

### F-F（中）"失败方向是安全的"仅有 chunk=1 场景支撑
- 位置：`10-execution.md:36`（结论 3）。
- 事实：该断言引用的是既有 `chunk=1` 失败观测与一句 UI 文案（"AI 失败，正在显示原文"）。未见任何 chunk>1 失败时的行为证据，也未读源码确认整章回退语义。
- 具体反例：若失败发生在第 4 块（共 6 块），前 3 块可能已落盘，则结果是"部分块为 AI 文本、其余为原文"的混合态，而不是"失败即保留原文"。这是与任务书安全关注点直接相关的未验证边界。
- 建议：标注该结论的适用域为"已在 chunk=1 失败路径观察到"，或补 chunk>1 的失败观测（属下一任务范围）。

### F-G（中）完成条件 1 与 5 无任何原始回读证据
- 位置：`00-task.md:51`（"保留复核证据"）、`:55`（"recordLog 恢复关闭并复核；分块仍为 600"）；`10-execution.md:20,23`。
- 事实："分块 600 / 超时 120 / recordLog 关闭"全部是报告自述的数值，未附读数原文或截图指针。本任务第 1 轮已证明"自述读数"确实会出错（F1），因此这一项不能仅凭表格成立。
- 建议：附最小脱敏回读（数值行本身即可，不含设备/书源信息）。

### F-H（低-中）工作树一致性与冻结探针输出冲突，本轮快照有效性未获验证
- 位置：`18-review-probes.md:6-9,13-19`（`真实工作树前后状态：stable`；`git status --porcelain` 仅 1 行 ` M .agent-protocol/CURRENT.md`；`总结：PASS`）。
- 事实：本次复审会话开始时的环境快照显示的工作树为 ` M .agent-protocol/CURRENT.md`、` M AGENTS.md`、` M 维护计划书-后续修复与增强.md`，以及两条 `?? .agent-protocol/tasks/…`。两者不能同时成立（探针缺 `??` 行**正是上一轮 F6 已提的问题**，指向探针输出被截断）。
- 可能的良性解释：暂停期后处理"亮度反馈"（`05-routing.md:15`）改动了这两个非业务文档，且探针运行于证据目录生成之前。
- 限制：**我无法独立复核**——本会话未提供 shell 工具，唯一的执行授权是 `06-review-commands.json` 的两条 git 命令，故该两项均记 `NOT RUN`。按任务书"复审冻结与轮次"节，快照不一致时本轮报告失效，请 runner 在 review 窗口内前后各执行一次冻结命令并留存输出。若不成立，则完成条件 6（`00-task.md:56`）不能判 PASS。

### F-I（低）源码引用与自述互相矛盾
- 位置：`10-execution.md:27`（"源码可查：`AiReadingCoordinator.kt` …（:114）（:219、:236、:308）"）对 `:66`（F1："`AiReadingCoordinator` 的计数语义**本任务未验证**（未读源码复核）"）。
- 事实：同一报告一面以"源码可查"给出具体行号，一面声明本任务未读该源码，二者必有一处不实；若行号继承自上一任务，则可能已随工作树变化而失效。附带：该源码路径不在 `00-task.md:41` 列出的执行者可读清单内，此处是否属未授权读取，需 controller 明确。
- 建议：删除或标注为"引用上一任务、未在本任务复核（行号可能失效）"。

### F-J（低、低置信度）待裁定项的时序存疑
- 位置：`10-execution.md:87`（"F6 / F7 — 交由 acceptor 裁定（见 `30-acceptance.md`）"）。
- 事实：本任务的 `30-acceptance.md` 按流程在复审之后由 acceptor 产出；执行阶段引用它作为裁定出处，时序上异常（也可能指的是上一任务同名文件，路径书写有歧义）。我无权读取该文件，无法核实，仅供 acceptor 确认。

### 未发现问题的方面（已检查）
- **证据边界（`00-task.md:19`）**：逐行通读 `10-execution.md` 全文，**未发现**书名、章节名、正文、响应体、服务地址、密钥、模型名、PID/TID、设备序列号；出现的仅为时刻（到秒）、块数（6）、字符数（4470）、状态文案与动作名称，均在允许集内。
- **安全（注入 / 路径穿越 / XSS / SQLi / 凭据外泄 / 权限放大）**：本任务**零业务代码改动**、正式文件白名单为空（`00-task.md:44`），报告文本中未出现任何命令串、凭据或路径拼接逻辑，**未发现**可被利用面。检查范围仅限报告文本；设备侧 `adb` 参数未出现在报告中，无法复核（见第四节）。
- **简化 / 死代码**：不适用（无代码改动），**未发现**。
- **越界（业务代码 / 配置 / Prompt / 缓存版本）**：**未发现**代码或 App 配置改动；唯一的设备状态改动即 F-C 所指的 AI 缓存清理。

---

## 三、我实际执行过的核对动作

1. 读取并逐行核对白名单文件：`00-task.md`（66 行）、`05-routing.md`、`06-review-commands.json`、`10-execution.md`（87 行）、`14-reviewer-prompt.md`、`18-review-probes.md`。
2. 交叉比对：`00-task.md` 完成条件/停止条件 ↔ `10-execution.md` 第一~八节；`05-routing.md:18,22` ↔ `10-execution.md:34,37`；`18-review-probes.md` 探针输出 ↔ `10-execution.md:56`（完成条件 6）↔ 本会话环境工作树快照。
3. 内部一致性：对 `10-execution.md` 的 6 个量化值（触发前状态、触发后状态、6 块、4470 字符、0 条固定字段、600/120）逐个追溯其证据来源，除"0 条固定字段"外均为自述无原文。
4. 反例构造：F-B、F-C、F-D、F-E、F-F 各给出一个可使报告结论出错的具体场景（见上）。
5. **`06-review-commands.json` 的两条冻结命令（`git status --porcelain`、`git diff --check`）：`NOT RUN`** —— 本会话未提供 shell/Bash 工具，无法执行；仅有 `18-review-probes.md:11-31` 的 runner 执行记录可引用（其中 `no-business-tree-changes` 退出码 0、`git diff --check` 退出码 0，仅有 LF/CRLF 警告）。
6. 未使用 Glob / Grep / 目录遍历 / 子代理，未尝试写任何文件，未修改 `10-execution.md` 或 `30-acceptance.md`。

---

## 四、我没检查到的范围（诚实登记）

- **设备侧一切原始证据**：logcat 实际内容、`ai_stats` 读数、设置页读数、状态迁移本身，均无法复现或复核；本次复审的证据基础**全部是执行者叙述 + runner 冻结的 git 探针**。
- **应用源码**：`AiReadingCoordinator.kt`（计数自增/重置语义）、锚点匹配与整章回退逻辑、`ANCHOR ambiguous` 判定——不在白名单，故 `NOT RUN`；F-E、F-F 中涉及实现语义的部分均为"未复核"。
- **上一任务证据**：`../ai-stage-e-anchor-proof/30-acceptance.md` 不在 reviewer 白名单，故 F-D 的辩护、结论 1 所依赖的"6 次失败"原始行均未核实。
- **工作树**：无 shell，完成条件 6 的独立复核为 `NOT RUN`（F-H）。
- **外发合规**：无法证明本任务真实请求数 ≤ 8；实际上 F-A 已使该数不可验证。任务书 `06-review-commands.json:3` 的网络策略为 `deny-declared-not-enforced`（`18-review-probes.md:8` 自述 "网络强制：none"），且真实外发发生在设备侧、不在审查沙箱内——冻结命令集对任何外发主张**不具备约束力**。
- **`PROJECT.md` / `CURRENT.md`**：`00-task.md:42` 未将二者列入 reviewer 可读白名单，故**未读取**，构建/测试命令与敏感路径未核对。
- **第六节所列矩阵项**（D2 发布矩阵、缓存命中对照、断网/超时/认证/限流）：未跑，与我一致，无异议。

---

## 五、给 acceptor 的最小动作建议

1. 把 F-A 的"未证明项"从 `10-execution.md` 正文结论与 `05-routing.md:18` 中降级为"待复核"，避免被下游"不必重跑"承接。
2. 对 F-C（清 AI 缓存是否越界）给出明确裁定；若越界，"一次通过"不得进入对外结论。
3. 要求补记构建同一性字段（F-B）与最小脱敏回读（F-G）——这两项不需要重跑实验，只需下一次受控运行时一并采集。
4. 明确 `recordLog` 是否门控 AI 固定字段（F-D）；若门控为真，"窗口内 0 条固定字段"应改判为该项取证 `NOT RUN`。
5. F-H 由 runner 用冻结命令在 review 窗口前后各执行一次并留存输出，否则本轮快照有效性按任务书规则记为未验证。
