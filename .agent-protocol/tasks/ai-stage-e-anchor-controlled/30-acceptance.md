# ai-stage-e-anchor-controlled — 最终验收（acceptor 裁定）

状态：`provisional`

> **本任务无法执行 `close`，如实说明**：复审已运行 **3 轮**（`rounds/reviewer-r001` 为 stale、r002、当前各 1 轮），超过 standard 档上限 2。其中 r001 被判 stale 的原因是 **controller 在复审运行期间改动了业务树**（`维护计划书`/`AGENTS.md`），属我自己造成的作废。升级到 `high` 档需要独立 acceptor，dsh-claude 模式无法提供。故按协议『升档或转后续任务、如实留痕』处理：**证据完整保留在本目录，任务保持开启，`CURRENT.md` 指针仍指向它**，不代表通过。

- **acceptor 运行实例**：人类启动的 DSH 会话（与 controller/executor 同一实例）。独立复审为三个不同的 claude 会话（r001 stale `bdb55e4c…`、r002 `c099dc2e…`、当前 `9e045138…`）。
- **mode / assurance / route_revision**：dsh-claude / standard / 1
- **实际 diff**：业务代码零改动；冻结探针 `git status --porcelain` 仅协议文件、`git diff --check` PASS。
- **acceptor 亲自复核（设备侧，无新外发）**：App 进程 PID 18311 = `io.legado.app.tjf.releaseS` 且为设备上**唯一** legado 进程；分块上限 **600**（复核两次）；`recordLog` 全程**关闭**（本任务未开启）。

## 一、构建同一性（回应复审 F-B，acceptor 现场取证）

| 字段 | 值 |
| --- | --- |
| versionName | 3.26.091915 |
| lastUpdateTime | 2026-09-19 15:51:51（即含修复的共存包安装时刻） |
| firstInstallTime | 2026-09-06 20:31:38（未卸载重装） |
| APK SHA-256 | CB1FE9202B429922A49B11D6FBCF2418080AF647837C4CF3221548EF907A5CC6 |
| 运行进程 | PID 18311，启动于取证时刻前约 41 分钟（≈15:52） |

**结论**：既有观测中 16:06–16:12 的 6 条 `code=ANCHOR anchor=ambiguous edit=changed chunk=1` **必然由含修复的构建产生**。复审 F-B 提出的反例（『失败可能发生在引入修复之前的构建』）**被排除**。

## 二、两项越界裁定（复审 F-C / F-D）

- **F-C『清理 AI 正文缓存』**：裁定 **不属任务书禁止项『清数据』**——被判清的是可再生的派生缓存（AI 处理结果），不是用户数据、书源内容或配置；计划书 D2 亦把『清 AI 缓存』列为常规测试步骤。**但**该操作**不在本任务授权的步骤清单内**，属**计划外步骤**，且使该次『通过』的初始条件与既有失败观测不同 → 因此**该次『通过』不得与既有失败直接对比**，也就不能支撑『间歇性』或『已修复』。
- **F-D `recordLog` 未开启**：裁定 『窗口内 0 条固定字段』的强度**降级**为：『在 **logcat 通道**上按 `AiFailure|ai-ok|ai-protocol|ai-editkind|ANCHOR` 过滤命中 0 条』；其『logcat 不受 recordLog 门控』的辩护依据取自上一任务验收文件，而该文件**不在本任务 reviewer 白名单**，故该辩护**未获独立复核**，登记为未闭合项。

## 三、可宣称 / 不可宣称（本文件口径优先，覆盖执行报告与路由文件）

**可宣称**：
1. 修复代码本身已通过两轮独立复审并提交（`6a7a2143a`）；
2. 在**生产分块 600**、**含修复的构建**上，确实观测到 **6 次** `ANCHOR ambiguous + changed`（已绑定构建同一性）；
3. 失败方向是**整章失败关闭、保留原文**——但直接证据**仅**覆盖 `chunk=1` 场景；
4. 本任务另一次尝试后，状态由『未处理』变为『已就绪』（**状态迁移本身**可确证）。

**不可宣称**：
1. 『修复在生产配置下有效』或『无效』——**核心问题为无法判定**；
2. 『失败是间歇性的』——两次观测的初始条件不同（文本可能随目录刷新变化、且本次前置了清缓存），且『一次通过』本身缺决定性读数；
3. 『本次受控运行消耗 6 块 / 未越过 8 次上限』——缺少『清缓存后、触发前』的同身份读数，无法排除陈旧计数；
4. 任何以『已确证』形式出现的上述内容。`05-routing.md` 原有『已确证结论（可承接，不必重跑）』**已由 controller 更正为『无法判定（待复核）』**并加注『严禁承接的表述』。

## 四、未闭合 findings（全部转后续任务，不阻塞本功能封存）

- F-A（正文就地标注未完成；标注意义由本文件第三节覆盖）；F-D（辩护未获独立复核）；F-E/F-G（内部数字自洽与原始回读证据缺项）；F-F（『失败方向安全』的覆盖上限）；F-H（探针输出缺 `??` 行的成因：探针的 `git status` 带排除路径，任务目录不在其业务树快照内）；F-I/F-J（源码引用与待裁定项时序）。
- 上述均为**证据强度**问题，不涉及已发布代码的正确性——本轮**无代码改动**。

## 五、用户决定与封存（2026-09-19）

> 用户决定：**AI 正文净化按『实验性质』封存**，暂停发布验收与继续调优，D2 发布矩阵一并暂停，不再授权真机外发。

- 已记录位置：`维护计划书-后续修复与增强.md` §2 待办池首块（含证据位置、未排除解释、恢复时首选假设）；`AGENTS.md` 文档索引行同步标注『自 2026-09-19 起按实验性质封存、暂停发布』。
- **发布判定维持 review / NOT READY**；AI 功能**不得**作为已验收特性对外表述。
- **恢复时的第一步**（需新授权）：开启 `recordLog`，在 600 分块下做『配置落定 → 读基线 → 触发 → 读结果』的同身份配对；随后按《维护计划书》首块列出的 5 条假设逐一排除。
- **提交 / 推送 / 发布状态**：本任务证据与文档改动**尚未提交**（待本轮收尾统一入库）；**未推送、未发布**。

## 六、最终判定

`provisional`

- **理由**：本任务在原授权范围内完成了受控复现动作并留下完整证据，但**核心问题（修复在生产分块下是否失效）为无法判定**（缺决定性读数），两轮有效复审均判『有阻塞项』；且本任务**无法执行 `close`**（复审 3 轮 > standard 档上限 2）。故不给 `accepted`，按 `provisional` 记录并封存。
- **唯一未决问题**：含修复的构建在生产分块 600 下是否仍会以 `ANCHOR ambiguous + changed` 失败——需新授权后按『同身份前后读数 + recordLog 开启』重取证。
- **发布状态**：AI 功能按用户决定**封存为实验性质**，**不得发布**；发布判定维持 review / NOT READY。
