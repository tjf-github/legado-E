# ai-stage-e-device — 复审报告（reviewer，独立会话）

## 结论

**有阻塞项（不能按"通过"验收）。**

两条阻塞项均来自"证据存在性"而非"报告不诚实"：

1. **核心目标无任何证据**：本任务要判断真机 `ANCHOR ambiguous + changed` 是否已被上下文锚点消解、能否取得"无需修改"缓存命中。执行报告自述该判断**未验证**（`10-execution.md:50`）。我确认工作树内所有 reviewer 可读证据中**都不存在**能支撑该判断的固定字段证据（`recordLog` 关闭）。此项在本轮属于"无证据可判"，不能计为通过。
2. **"≤3 次请求"（完成条件 3 的后半句，`00-task.md:63`；硬上限见 `00-task.md:22`）无法成立**：执行报告自述样本 2 的计数器未当场读取，只能确定 **≥3 块**（`10-execution.md:40,44`）。上限是否被突破（即是否为 4 次）在本轮证据中不可判别，而"超过 3 次请求"恰是任务书写明的**停止条件**（`00-task.md:70`）。

其余部分（构建、保留数据安装、临时打包改动还原、证据脱敏边界）经我独立核对**成立**，其中临时 suffix 还原是我本次唯一能自算的正面结论（见第二节 P1）。

---

## 一、我实际执行的核对动作与输出

严格遵守 `00-task.md:47` 的 reviewer 可读白名单，**未**使用 Glob/Grep/目录遍历触达白名单外路径。

| # | 动作 | 结果 |
| --- | --- | --- |
| A1 | 读取 `00-task.md`（全文 75 行） | 完成 |
| A2 | 读取 `10-execution.md`（全文 70 行，按"待验证主张"读） | 完成 |
| A3 | 读取 `05-routing.md`（24 行）、`06-review-commands.json`（29 行）、`18-review-probes.md`（33 行） | 完成 |
| A4 | 读取 `app/build.gradle` 第 70–109 行 | 第 91 行为 `applicationIdSuffix '.release'`，**临时 `.releaseS` 已还原**（P1）；另见第 92–98 行本身就有 `.releaseS` → `app_name_s` 的分支，佐证 `.releaseS` 是本仓库受支持的变体，执行者选用该后缀与仓库设计一致 |
| A5 | 冻结命令 `temporary-packaging-changes-reverted`、`whitespace-and-conflict-check` | **NOT RUN（本会话未提供 shell 执行工具）**。`18-review-probes.md:11-33` 记录的是 runner 侧执行结果（两条均 PASS，exit 0；`git diff --exit-code -- app/build.gradle app/src/main/AndroidManifest.xml` 输出为空）。我**不能**把它当作自己算出的结论，只作为 runner 生成的独立证据引用 |
| A6 | 会话起始 git 快照 | 分支 `codex/manga-import`、HEAD `17f8e29d8`、`?? .agent-protocol/tasks/ai-stage-e-device/` → 与 `00-task.md:31`、`18-review-probes.md:3` 的冻结 HEAD 一致，且与 `10-execution.md:63`"该目录未提交、仍为未跟踪"一致 |

**因无 shell 而无法自算的项**（本节声明，不自行扩大范围）：APK 存在性与 SHA-256、adb 安装结果与 `dumpsys` 输出、`git status` 全量差异、设备内设置项现值。

## 二、经我核对成立的主张（含交叉验证）

- **P1 临时打包改动已还原（独立自算）**：`app/build.gradle:91` = `applicationIdSuffix '.release'`；且 runner 侧 `git diff --exit-code`（覆盖 `app/build.gradle` + `AndroidManifest.xml`）exit 0、输出为空（`18-review-probes.md:11-19`）。`00-task.md:50-51` 的两处临时改动均满足"构建后还原"。
- **P2 未卸载 / 未清数据（证据自洽）**：`10-execution.md:28` 的 `firstInstallTime=2026-09-06 20:31:38` 本身即可证明自 09-06 起未发生卸载重装（若卸载重装，该值必为当日）。虽只贴了安装后的值，"与安装前一致"的推断仍成立——但该推断依赖该行确系真机输出，我无法复算。
- **P3 版本一致性交叉验证**：`dumpsys` 的 `versionName=3.26.091915`（`10-execution.md:27`）与 APK 文件名 `legado_app_3.26.091915.apk`（第 31 行）一致，`lastUpdateTime=2026-09-19 15:51:51`（第 28 行）与安装动作同日 → 与"本次构建产物确被安装"自洽。
- **P4 分块上限临时调整确实当时生效（交叉验证）**：样本 1"发送 3412 字符 · 请求 1 块"（`10-execution.md:39`）。若分块上限仍为 600，3412 字符必然产生多块 → 反推当时上限确为 6000，与 `00-task.md:23` 的补充授权记录自洽。
- **P5 证据脱敏（就当前版本而言）**：我逐行读过 `00/05/06/10/14/18` 六个文件，**未发现**凭据、Token、服务地址、正文、响应体、异常 message、**书名/章节名/模型名**。样本已改为"样本 1/2/3"（`10-execution.md:10`）。
- **P6 未提交/未推送（就本地仓库而言）**：HEAD 与冻结值一致 + 任务目录为未跟踪（A6）→ 报告所述"未进入任何提交或版本历史"与工作树状态一致。
- **P7 冻结探针本身有效**：`06-review-commands.json` 两条命令均为只读 git 命令，无写操作、无网络、无参数注入面；`18-review-probes.md:6-8` 声明"真实工作树前后状态 stable"。

## 三、发现（按严重程度）

### F1 — 阻塞（严重度高）：核心目标"锚点路径是否生效"零证据，却被第七节并入"字面要求已达成"

- 位置：`10-execution.md:3, 50, 67`；目标见 `00-task.md:7-10`，完成条件见 `00-task.md:63`。
- 事实：`recordLog` 关闭 → 无 `ai-ok` / `ai-editkind` / `ai-fail code=… anchor=… chunk=N` 任何固定字段；三例结论取自 UI 状态文案与 `ai_stats` 计数（`10-execution.md:39-41`）。而 `00-task.md:9-10` 明确要求"**只从固定日志字段读取结论**"。
- 具体失败场景：若锚点修复实际无效，生产配置（分块 600）下仍可能出现真机此前观测到的 `ANCHOR ambiguous + changed` → "AI 失败，正在显示原文"；本轮 3 次成功会被下游读者（含 D2 发布判定）误读为"修复已被真机证明"。执行者虽在 `10-execution.md:50` 明确否认了这一点，但 `:67` 的总结句未携带该限定词，两处强度不一致。
- 建议：D1 结论固定表述为"未复现，未证明"；发布判定维持 NOT READY；重跑需**新的外发授权**并先开启 `recordLog`、当场导出固定字段（与 `10-execution.md:68` 的建议一致）。

### F2 — 阻塞（严重度高）：≤3 次请求上限未被证实，且停止条件可能已被触发

- 位置：`10-execution.md:35-44`；上限 `00-task.md:22`、完成条件 `00-task.md:63`、停止条件 `00-task.md:70`。
- 事实：样本 1、3 各读到 1 块；**样本 2 未当场读取计数**。"已确证 3 块、无法排除样本 2 为 2 块"（`:44`）。
- 具体失败场景：样本 2 若送出 2 块 → 真实外发 4 次 > 授权 3 次，属超范围外发；且执行者当时无法察觉，因而**在已满足停止条件的情况下继续执行了样本 3**（`10-execution.md:41`）。
- 建议：改为"每样本前后各读一次 `ai_stats` 取差值"并在证据中逐样本留数；读不到计数即停止、不下结论、不推进下一个样本。当前状态应在验收记录中显式写为"上限合规性不可判定"。

### F3 — 中：分块上限 6000→600 的"已还原"无独立证据，且属设备侧持久状态

- 位置：`10-execution.md:9`；要求见 `00-task.md:23`、清理要求见 `00-task.md:54`。
- 事实：该项是 App 内设置（不在 git、不在冻结命令覆盖范围），报告只有自述"已还原为 600 并复核"，未按 `00-task.md:23` 要求把**前后值**作为证据记录。
- 具体失败场景：若实际未还原，用户此后所有处理都按 6000 字符分块，出网内容粒度与成本被静默改变，同时违反"收尾前必须清理"。
- 建议：在设置对话框内截图（脱敏）记录前后值，或由 acceptor 在设备上现场复核该项现值；我无法从工作树侧复核（NOT RUN，见第五节）。

### F4 — 中：测试配置非生产配置，结论不可迁移到发布配置

- 位置：`10-execution.md:9, 43, 67`；缺口 4 见 `10-execution.md:53`；授权见 `00-task.md:23`。
- 事实："未复现 `ANCHOR ambiguous + changed`"是在**临时把分块上限改为 6000** 的条件下取得的；生产/在架配置为 600。`MAX_ANCHOR_CONTEXT = 32` 与 6000 的组合未经校准（执行者已自述）。
- 具体失败场景：D2 发布判定若引用本轮"零失败"，等于把 6000 分块下的行为外推到 600 分块，而锚点歧义恰恰与文本范围/上下文窗口相关。
- 建议：把"结论仅在该临时配置下成立"写进结论句本身，而非只留在缺口列表；D2 必须回到 600 分块重测。

### F5 — 中：完成条件 5 的"`git status` 无新增业务改动"未被冻结命令覆盖

- 位置：`06-review-commands.json:5-27`（仅两条命令）；`18-review-probes.md:11-33`；条件见 `00-task.md:65`。
- 事实：冻结命令只检查 `app/build.gradle` 与 `AndroidManifest.xml`，外加 `git diff --check`（仅空白/冲突标记，其 stderr 只列了 3 个文件的 LF 警告）。非目标明确禁止改动"业务源码、测试、Prompt 或缓存版本"（`00-task.md:14`）。
- 具体失败场景：若真机测试期间顺手改了缓存版本号或 Prompt，两条冻结命令都不会报出，`git diff --check` 也只会静默通过。
- 建议：冻结命令清单加入 `git status --porcelain`（或 `git diff --stat`），使该声称可被独立核验。**注意：这是命令集覆盖不足，不等于执行者越界**——我没有发现越界证据。

### F6 — 中低：样本 2 的结论与用量都无证据，却在表格中与已取证行同列同强

- 位置：`10-execution.md:40`。
- 具体失败场景：表格读者会把三行当作同等证据强度，把"≥3 块"读成"3 块"，进而与 F2 叠加形成"上限合规"的错误印象。
- 建议：把该行标为 `NOT RUN / 未取证`，或另设"证据等级"列。

### F7 — 低："无需修改"被误置于"缓存命中"路径的达成叙事

- 位置：`10-execution.md:39, 51, 67`；目标原文见 `00-task.md:10`。
- 事实：样本 1 发送了 3412 字符 → **发生了真实请求**，属模型判定"无需修改"，与"无需修改缓存命中（不再发请求）"是两件不同的事。执行者在缺口 2 已如实否认后者，但 `:67` 的总结句据此前推"字面要求已达成"。
- 具体失败场景：D2 若按"缓存命中已验证"设计发布矩阵，会遗漏对缓存路径的实际覆盖。
- 建议：措辞上区分二者；缓存命中需要"重开同一样本不再产生请求"的对照（已列 NOT RUN，正确）。

### F8 — 低（治理）：两次授权扩围的记录均由被审方自己书写，无法独立核验

- 位置：`00-task.md:18-23`（"用户明确授权"与 controller 补充授权）、`05-routing.md:15-23`（第三个样本的追加授权，"当时未记入路由文件，现已补记"）。
- 事实：本任务 controller = executor = dsh，授权叙述由同一被审方书写；真实外发数由样本数 3 超过 `00-task.md:21` 的"1–2 个极短章"反映出来。
- 具体失败场景：若用户实际未授权"分块上限改 6000"或第三个样本，则存在两次未经授权的行为；我在白名单内**无法核验用户原话**（NOT RUN）。
- 建议：由 acceptor（或用户本人）确认这两条授权记录与真实一致；后续任务把授权变更写成独立留痕（含用户原话时间戳），而不是仅由 controller 记入任务书。

## 四、按复审角度逐项的"未发现"结论

- **越界**：在被审方真正动过的两个受控文件上**未发现越界**（P1，`app/build.gradle:91` 已还原 + 冻结 diff 为空）。是否触碰敏感路径（`keystore*`、`look.ps1`、真实书源/正文/用户缓存）**无法判定**——这些路径不在我的可读白名单内，我未读取也未探测，见第五节。**唯一边界违规是执行者自述的初版报告写入书名/章节名/模型名**（`10-execution.md:60-64`）：就当前工作树版本而言该内容已不存在（P5），且 HEAD 未变、任务目录未跟踪，与"未进入提交/远端"自洽（P6）。残余风险：我无法核验被删版本的残留副本是否存在于其它未列白名单的证据文件、编辑器备份或 logcat 转储中。
- **安全（注入/穿越/XSS/SQLi/权限放大）**：**不适用且未发现**。本任务未修改任何代码（`00-task.md:14` 明确禁止），冻结命令仅两条只读 git 命令，无参数拼接、无路径输入、无网络（`00-task.md:41`）。凭据/地址外泄：在可读证据文件中**未发现**（P5）。权限相关的真实风险是外发范围与请求计数，已归入 F2/F8。
- **简化（不必要的抽象/兼容垫片/死代码）**：**未发现，且本轮不可评**——本任务不产生代码改动；锚点修复的源码（`AiOutputValidator.kt` 等）**不在本任务 reviewer 白名单内**，按 `14-reviewer-prompt.md:12` 的约束我未读取。
- **证据缺口**：F1、F2、F3、F5、F6；此外 `10-execution.md:67` 引用了一句"计划书 D1 字面要求（'至少取得一章 Ready 和一章无需修改'）"，该"计划书"**不在我的可读白名单内**，该引文**无法追溯核验**（NOT RUN）。
- **反例**：见 F1（生产 600 分块下歧义未复现不等于已消解）、F2（样本 2 若 2 块即超上限）、F3（设置未还原会静默改变用户后续行为）、F4（6000 分块结论外推到 600）、F5（改 Prompt/缓存版本不会被任何冻结命令抓到）。这五条都给出了具体触发条件，不是推测性罗列。

## 五、我没检查到的范围（NOT RUN，诚实声明）

- **冻结命令未由我执行**：本会话无 shell 工具，两条冻结命令我**未运行**；`18-review-probes.md` 的 PASS 是 runner 侧结果，属"引用"而非"自算"。
- **真机侧全部不可复核**：APK 存在性/大小/SHA-256、`adb install -r` 结果、`dumpsys` 输出、App 内 `ai_stats` 与"分块上限"现值、`recordLog` 开关状态——我无设备访问、且相关路径不在白名单 → 全部 `NOT RUN`。
- **全量工作树差异**：未被冻结命令覆盖的文件是否有改动 → `NOT RUN`（F5）。特别是 `00-task.md:65` 的"`git status` 无新增业务改动"这一完成条件，我**只能确认被覆盖的两个文件干净**，不能确认整棵树。
- **敏感路径与未列白名单路径**：未读取、未用 Glob/Grep 间接探测 → 相关禁令的遵守情况 `NOT RUN`。
- **上一任务 `ai-stage-e-anchor/30-acceptance.md`、锚点修复源码、`PROJECT.md`、`CURRENT.md`、`docs/ai-tasks`**：`00-task.md:47` 未列入 reviewer 白名单（`PROJECT.md` 仅在白名单内才可读，本任务未列入）→ 一律**未读**，因此**未**对锚点修复代码本身做任何正确性复审。F1 的"修复是否有效"我**没有**独立判断，只判断"本轮无证据"。
- **任务目录内其它文件（如任何未列白名单的证据文件）是否残留初版书名/章节名/模型名**：未枚举、未读取 → `NOT RUN`（仅就 `00/05/06/10/14/18` 六个文件确认干净）。
- **D2 发布矩阵、缓存命中对照、提交/推送/合并/发布**：与 `10-execution.md:55-58` 一致，`NOT RUN`（未授权）。

## 六、与执行报告不一致之处

未发现陈述与工作树冲突的项。可复核的交叉点（`app/build.gradle:91` 已还原、HEAD 未变、任务目录未跟踪、版本号与安装时间自洽、6000 上限确实当时生效）均与报告一致。**唯一的不一致是报告内部强度不一致**：`10-execution.md:3`/`:50` 判"核心目标未达成"，而 `:67` 判"字面要求已达成"——见 F1、F7，建议统一为"部分完成：计划书字面样本数达成，但锚点路径与请求上限两项不可判"。
