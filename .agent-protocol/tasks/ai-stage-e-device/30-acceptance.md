# ai-stage-e-device — 最终验收

状态：`provisional`

- **acceptor 运行实例**：人类启动的 DSH 会话（与 controller/executor 同一实例）。独立复审为两个不同的 claude 会话：round 1 `a4978895-5aa0-45e4-a93e-5cbd256cd556`、round 2 `e28ff921-262a-424c-8eff-ee5c8f3ec332`，均非本实例。
- **mode / assurance / route_revision**：dsh-claude / standard / 1
- **实际 diff**：**业务代码零改动**。临时打包改动（`app/build.gradle` 第 91 行 suffix、`AndroidManifest.xml` 第 28/541/542 行业务权限名）已还原；冻结命令 `git diff --exit-code -- app/build.gradle app/src/main/AndroidManifest.xml` 退出码 0、输出为空（探针独立复核 PASS）。
- **执行证据核对**：`10-execution.md`、`18-review-probes.md`（两条冻结命令 PASS，工作树 `stable`）、`05-routing.md`（授权追加已补记）。
- **独立复测**：acceptor 亲自执行的部分——构建 `BUILD SUCCESSFUL in 7m 28s`；`adb install -r` → `Success`；设备侧 `dumpsys` 复核 `versionName=3.26.091915`、`firstInstallTime=2026-09-06 20:31:38`（未卸载、未清数据）；APK 19,965,316 字节 / SHA-256 `CB1FE920…5CC6`；三个样本结果与 `ai_stats` 计数当场抄录。
- **可见行为**：样本 1 = **AI 检查完成，无需修改**（请求 1 块 · 3412 字符）；样本 2 = **AI 正文已就绪**（计数未当场读取）；样本 3 = **AI 正文已就绪**（请求 1 块 · 3470 字符）。**零失败**，未出现「AI 失败，正在显示原文」。
- **NOT RUN**：D2 发布矩阵、缓存命中对照、`recordLog` 固定字段取证、样本 2 的请求计数、提交/推送/合并/发布。

## 对 `10-execution.md` 的限定（**本节口径优先，属"收窄表述"**）

复审 F1/F7 指出执行报告内部强度不一致：第 3 行与缺口 1 判"核心目标未达成"，而第七节判"字面要求已达成"。本验收**统一并收窄**为：

> **部分完成**：计划书的**字面样本数**达成（≥1 Ready + ≥1 无需修改，零失败），但**锚点路径是否生效**与**请求上限合规性**两项均**不可判**。第七节"字面要求已达成"的表述以本行为准，且**不得**被读作"锚点修复已获真机证明"。

## 复审 findings 逐项处置

**round 1**
- **F1（阻塞｜核心目标无证据）**→ **接受判定**，不改写事实：本任务目标 3（"只从固定日志字段判断 `ANCHOR ambiguous + changed` 是否已被消解"）**未达成**——`recordLog` 关闭，无任何 `ai-ok`/`ai-editkind`/`ai-fail` 固定字段。结论固定为"**未复现，未证明**"。转后续任务 1。
- **F2（阻塞｜证据边界违规）**→ **已修正**：初版把书名、章节名、模型名写进证据文件，违反任务书；现已全部删除（样本改编号）。**暴露面为零**：`HEAD=17f8e29d8` 未变、`tasks/` 仍为未跟踪，未进入任何提交或远端。复审第二轮 P5/P6 独立确认当前版本干净、未提交未推送。
- **F3（中｜授权扩围未留痕）**→ **已补记**于 `05-routing.md`（样本 3 的人类当场追加 + controller 的分块上限补充授权）。
- **F4（中｜设备侧仅自述）**→ **已补原始输出**（`dumpsys` 节选、APK 大小与 SHA-256）。仍无法由 reviewer 独立复算（其无 shell 与设备访问），故保留"executor 自述 + 原文已附"的强度标注。
- **F5（低｜状态字不自洽）**→ **已改**为"部分完成"。

**round 2**
- **F1（阻塞｜核心目标零证据、结论句缺限定）**→ 接受；已在本文限定节收窄（`10-execution.md` 冻结于复审快照，**按协议不得在复审后改动**，故更正落在本文件）。
- **F2（阻塞｜≤3 次请求不可证、停止条件可能已被触发）**→ **接受判定**：**上限合规性不可判定**。已确证 3 块（样本 1、3 各 1 块），样本 2 未当场读取 → 不排除 4 块。后续任务须"每样本前后各读一次 `ai_stats` 取差值"，读不到即停止。
- **F3（中｜分块还原无独立证据）**→ **acceptor 现场复核**：还原后立即在设置对话框读到 `分块 600`、`超时 120`、`本章：请求 0 块`（同一次会话内 dump 抄录）。**未做二次复核**，理由：再次进入阅读页可能触发未经授权的新外发（开章自动处理）。该值现为 `600` 属自述+当场抄录强度，**未获第三方复算**。
- **F4（中｜测试配置非生产配置）**→ **接受**：本任务结论**仅在临时分块 6000 下成立**，**不得外推**到在架配置 600；D2 必须在 600 下重测。
- **F5（中｜`git status` 未纳入冻结命令）**→ **接受为命令集覆盖不足**（非越界）：acceptor 已亲自复核业务树仅有锚点修复任务遗留的 6 个未提交文件，本任务未新增业务改动。转后续任务 2（把 `git status --porcelain` 加入冻结命令模板）。
- **F6（中低｜样本 2 与已取证行同列同强）**→ **接受**：样本 2 的用量标 `未取证`，其结论强度低于样本 1/3。
- **F7（低｜"无需修改"≠"缓存命中"）**→ **接受**：样本 1 **发生了真实请求**（3412 字符），属模型判定无需修改；**缓存命中**（重开不再产生请求）**未验证**，已列 NOT RUN。
- **F8（低｜授权叙述由被审方自书）**→ **如实登记**：本任务 controller=executor=acceptor 同为 dsh，授权叙述确实由被审方书写，reviewer 无法独立核验用户原话。可核对的事实是：①用户当场追加样本 3 的原话存在于会话记录；②**分块上限 600→6000 是 controller 在本任务书内自行补充授权的决定，并非用户明确指令**（其在"不改目的地/模型/正文范围、不超请求上限"的信封内，且已还原）。用户如不认可该两条授权记录，可在后续任务中推翻。

## 边界与越界披露
- **写白名单**：本任务唯一的越界是初版证据文件写入书名/章节名/模型名（F2），已修正且确认无对外暴露。
- **数据边界**：未读取凭据/`keystore*`/`look.ps1`/真实书源与正文/用户缓存；未把服务地址、密钥、正文、响应体写入任何证据（含掩码处理）。
- **冻结命令未由 reviewer 执行**（其无 shell），其 PASS 为 runner 侧独立运行结果。

## 最终判定

`provisional`

- **理由**：任务按授权范围**实际执行完毕且可复核部分全部成立**（构建、保留数据安装、临时改动还原、证据脱敏、零失败、请求数落在已确证的 3 块内）；但**核心目标（锚点路径是否生效）无证据**、**≤3 次请求的合规性不可判定**，独立复审两轮均判"不能按通过验收"。故不给 `accepted`，按协议以 `provisional` 收口并转后续任务；**发布判定维持 review / NOT READY**。
- **未达成/不可判项**：①锚点路径是否被走到（未证明）；②请求上限合规性（不可判）；③缓存命中对照（NOT RUN）；④结论仅成立于临时分块 6000（F4）。
- **唯一下一步**：后续任务 1 —— 在**新的外发授权**下，先开启 `recordLog`，用"能触发重复锚点编辑"的样本跑 1–2 次，逐样本前后读 `ai_stats` 取差值，并导出 `ai-editkind`/`ai-fail` 固定字段；据此把状态从 `provisional` 提升到 `review`/`accepted`，再进入 D2。
- **提交 / 推送 / 发布状态**：**未提交、未推送、未合并、未发布**。

## 后续任务（按优先级）
1. **D1 补测（需新授权）**：开启 `recordLog` → 锚点样本 1–2 次 → 导出固定字段；同时按 F4 在**生产分块 600** 下复测。
2. **协议模板改进（本项目外）**：把 `git status --porcelain` 纳入默认冻结命令建议；授权变更要求独立留痕（含用户原话与时间）。
3. **回到 `ai-stage-e-anchor` 的遗留项**：窄化 `start/end` 交叉校验以闭合 F-C；修正 `AiProtocol.kt` 第 38 行 KDoc；测试加固（超限反例可判别、`contextAfter` 超限、锚点路径补充平面正例、Prompt 的 32 由常量插值）。
4. **D2 发布矩阵**：在 600 分块下按计划书 §8 D2 全量执行后，才可进入《发布推送流程书》§4。
