# 构建优化预处理：状态盘点与任务清单（2026-08-14）

> 目的：为「构建优化书」的 P0~P3 落地做预处理——确认现状基线、核对每个改动点的精确落点、列出前置障碍与建议执行顺序。
> 依据：《构建优化书》、复盘总结（2026-08-12/13）、代码与 Git 实际状态、`build.log`、CI 工作流原文。
> 状态：**预处理完成，未做任何代码改动**。

## 1. 状态快照

| 项 | 状态 |
| --- | --- |
| 当前分支 | `codex/manga-import` @ `6ae99cf64`（ahead origin 11） |
| 工作区 | 干净（`git status --short` 为空） |
| Gradle | wrapper 8.14.4；AGP 8.13.2；Kotlin 2.3.10；JDK 17 工具链 |
| 最近构建日志 | `build.log`（2026-08-11 00:28）为**历史失败记录**：transform 缓存损坏 `Could not deserialize analysis`（配置阶段 classpath 解析失败）；当前 `.gradle_home/` 已删除，需实测重建基线 |
| APK 输出 | `app/build/outputs/apk/app/{debug,release}` 均存在（variant 名 `app`，输出路径 `apk/app/…`） |
| 单测 | 复盘称 61 个全绿（`testAppDebugUnitTest`），待实测复核 |
| CI | `test.yml` 只 `assemble`，不跑单测/lint；build 步骤 `continue-on-error: true` |
| 沙箱构建限制 | 实测 `gradlew --version` 失败：workspace-write 拒绝写 `D:\gradle_home`（wrapper 锁 `.lck`）→ 跑 gradle 需提升权限或改 GRADLE_USER_HOME 到 workspace 内 |

## 2. 优化项落点核对（计划书 §4 → 精确位置）

### 4.1 配置缓存（P1，价值最高）

| 落点 | 位置 | 现状 |
| --- | --- | --- |
| 版本号 `new Date()` 在配置阶段 | `app/build.gradle:17-24`（`def now = new Date()` / `version` / `buildVersionCode`） | 阻塞配置缓存 + 构建不可复现 |
| 配置缓存开关 | `gradle.properties:33`（`org.gradle.unsafe.configuration-cache=false`） | 关闭中，注释即说明原因 |
| CI 版本注入 | `test.yml:194`（`sed "/def version/c def version = …"`） | 方案 A 落成后此 `sed` 需同步删除 |
| 灰度开关 | `gradle.properties` 新增 `configuration-cache=true` + `configuration-cache.problems=warn` | 需验证 `app/download.gradle` 自定义 `Download`/`downloadCronet` 兼容性 |

### 4.2 并行 + 本地缓存（P1）

| 落点 | 位置 | 现状 |
| --- | --- | --- |
| `org.gradle.parallel=true` | `gradle.properties:15`（被注释） | 打开即可 |
| `org.gradle.caching=true` | `gradle.properties` 无此项（本地未开；CI 用 `--build-cache`） | 新增 |

### 4.3 Kotlin classpath snapshot（P1）

| 落点 | 位置 | 现状 |
| --- | --- | --- |
| `kotlin.incremental.useClasspathSnapshot=true` | `gradle.properties:24`（被注释） | 打开即可 |

### 4.4 死二进制与死配置清理（P0）

| 落点 | 位置 | 现状 |
| --- | --- | --- |
| 死 rhino jar | `modules/rhino/lib/rhino-1.7.14.jar`（已跟踪） | 引用已注释（`modules/rhino/build.gradle:38`），实际用 `api libs.mozilla.rhino`=1.8.1（`:39`）→ 可删 |
| Firebase 依赖残留 | `gradle/libs.versions.toml:79`（`firebaseBom`）、`:100-102`（`firebase-bom/analytics/perf`）、`:228`（`google-services` 插件） | 均为死配置（`app/build.gradle` 无实引，仅注释） |
| google-services 插件别名 | 根 `build.gradle:30`（`alias libs.plugins.google.services apply false`） | 可删 |
| `google-services.json` | `app/google-services.json`（已跟踪） | Firebase 已移除（提交 `94eb65b2a`）→ 可删 |
| 注释掉的旧配置块 | 各 build 文件大量 `//` 注释块 | 清理（保留「为何锁定版本」注释） |

### 4.5 Cronet 自管（P2）——**已修正计划书假设**

> **修正**：计划书 D8/§4.5 写「4 ABI `.so` 入库」**不成立**。核实：`app/.gitignore` 含 `/so`，`git log --all -- 'app/so'` 无记录，磁盘无 `app/so` 目录 → **`.so` 从未入库**。

| 落点 | 位置 | 现状 |
| --- | --- | --- |
| 入库二进制 | `app/cronetlib/*.jar`（5 个，共约 1.8MB，已跟踪） | 体积可接受；可评估转远程依赖，但非必须 |
| `.so` 下载 task | `app/download.gradle:46-78`（`downloadARM64/ARMv7/X86_64/X86`） | 生成 `assets/cronet.json` 的 md5 清单（`.so` 本身不入库，运行时从 CDN 下载）；**保留，不做改动**（见 §3.3） |
| 更新流程 | `cronet.yml`（每周 `app:downloadCronet`，`add-paths` 精确匹配 `*cronet*jar`） | 流程较规范，保留 |

### 4.6 模块拆分（P3）

| 落点 | 位置 | 现状 |
| --- | --- | --- |
| 巨石模块 | `:app` 873 个 Kotlin 文件 | 先抽 `:core`（utils/model/help），再抽 `:feature:manga/audio/video`，参照 `:modules:book` 范例 |

### 4.7 CI 优化（P0）

| 落点 | 位置 | 现状 |
| --- | --- | --- |
| 缺单测/lint | `test.yml` build job（`:145-251`）只 `assemble` | 加 `testAppDebugUnitTest lintAppDebug` |
| 构建失败被吞 | `test.yml:184`（`continue-on-error: true`） | 移除；仅保留上传 missing_rules 的 `ignore` |
| 版本注入 sed | `test.yml:194` | 方案 A 落成后删除，改 `-PVERSION/-PVERSION_CODE` |

### 4.8 仓库卫生（P2）

| 落点 | 位置 | 现状 |
| --- | --- | --- |
| 个人代理写进工作区 | `gradle.properties`（skip-worktree 标记 `S`） | 移到 `~/.gradle/gradle.properties`（用户级），仓库级只留通用配置 |
| 阿里云镜像 | `settings.gradle:3-19`（skip-worktree `S`） | 保留，加注释说明 |

## 3. 前置障碍（动手前需先解决/确认）

### 3.1 构建基线需实测重建（最高优先）

- `build.log` 是 2026-08-11 的**失败**记录：`Could not deserialize analysis`（Gradle transform 缓存损坏），发生在**配置阶段**（root classpath 解析 `fus-statistics-gradle-plugin`/`kotlin-gradle-plugin-api` 等）；
- 当前 `.gradle_home/` 已删除（第三期预处理已清理并 gitignore），复盘（08-12/13）称 `assembleDebug` 通过，但**本机最新基线未实测**；
- **动手前第一步**：`.\gradlew.bat :app:compileAppDebugKotlin :app:testAppDebugUnitTest` 确认绿，并留 `--profile` 基线数据；若再遇 `Could not deserialize analysis`，删 `~/.gradle/caches`（或 `GRADLE_USER_HOME`）重建；
- **沙箱障碍（已实测）**：workspace-write 下 `gradlew --version` 因无法写 `D:\gradle_home` 的 wrapper 锁文件（`.lck`）而失败 → 重建基线需（a）提升沙箱权限，或（b）把 `GRADLE_USER_HOME` 指向 workspace 内的 `.gradle_home`（已 gitignore，但依赖需重新下载）。

### 3.2 CI `assemble` task 名大小写错误（已确认 bug）

- **variant 名已由构建产物确认为 `appRelease`**：`app/build/intermediates/{merged_manifest,dex,javac,merged_native_libs,lint_vital_output}/` 下均有 `appRelease` 子目录；APK 文件 `legado_app_3.26.081316.apk` 佐证；
- AGP 规则：assemble task 名 = `assemble` + capitalize(variant) = **`assembleAppRelease`**；
- `test.yml:197` 写 `./gradlew assemble${{ env.product }}release`，`product="app"` → 实际命令 **`assembleapprelease`**（全小写）→ 与 `assembleAppRelease` 大小写不一致，**确认为 bug**；被 `continue-on-error: true` 掩盖，仅靠「Check Build production」兜底；
- **处理（P0）**：将该行改为 `assembleAppRelease`（或 `assemble${product.capitalize()}release`）。

### 3.3 Cronet native `.so` 实际来源（已查明）

- **`.so` 是动态链接库**（`libcronet.<version>.so`），但**不打进 APK、不入 git**；
- 运行机制是**运行时分发**：`CronetLoader.loadLibrary`（`lib/cronet/CronetLoader.kt:118-169`）先 `System.loadLibrary` 兜底，失败后按 `assets/cronet.json` 的 md5 从 `https://storage.googleapis.com/chromium-cronet/...` 下载对应 ABI 的 `.so` 到应用私有目录（`getDir("cronet")`），校验 md5 后 `System.load` 加载；
- `cronet_impl_native_java.jar` **不含** `.so`（`tar -tf` 已核实，仅 class 文件）；
- 因此 `download.gradle:46-78` 的 4 个 so 下载 task **不是死代码**：它们是「更新 Cronet 版本时下载 `.so` → 算 md5 → 写 `assets/cronet.json`」的清单生成器（`.so` 被 `/so` 忽略，只有 md5 入库）；**结论：保留，不做改动**。

### 3.4 配置缓存兼容性

- `app/download.gradle` 的自定义 `Download` task 与 `downloadCronet` 的 `doLast` 是否兼容配置缓存，需灰度验证；不兼容则标 `notCompatibleWithConfigurationCache()`。

## 4. 建议执行顺序

1. **重建构建基线**（§3.1）：`compileAppDebugKotlin` + `testAppDebugUnitTest` + `--profile` 基线；
2. **P0 死二进制/死配置清理**（§4.4）：删死 rhino jar、`google-services.json`、catalog firebase 条目、google-services 插件别名（每个删除前 `grep -r` 确认无引用）；
3. **P0 CI 修复**（§4.7 + §3.2）：加单测/lint 门禁、去 `continue-on-error`、修正 `assemble` task 名大小写、删除 `sed` 版本注入；
4. **P1 构建提速**（§4.1~4.3）：版本号外部化 → 配置缓存灰度 → 并行/缓存/classpath snapshot；
5. **P2**（§4.8）：代理移用户级（Cronet 维持现状，无需改动）；
6. **P3**（§4.6）：模块拆分，每步 `assembleDebug` 全绿再继续。

> 每项独立分支、可单独回退；P0 完成后即可看到「CI 有单测门禁 + 仓库瘦身」的可见收益。

## 5. 工作区卫生清单（待决策）

| 文件/目录 | 状态 | 建议 |
| --- | --- | --- |
| `build.log` | 未跟踪，历史失败日志 | 已 gitignore；删除或保留作参考 |
| `modules/rhino/lib/rhino-1.7.14.jar` | 已跟踪，死二进制 | 删除（§4.4） |
| `app/google-services.json` | 已跟踪，Firebase 已移除 | 删除（§4.4） |
| `app/cronetlib/*.jar`（5 个） | 已跟踪，约 1.8MB | 保留（自管 Cronet，体积可接受） |
| `app/so/` | 已被 `/so` gitignore，磁盘不存在 | 无需处理（计划书假设已修正） |
| 代理/镜像配置 | skip-worktree（`S`） | P2 移用户级 `~/.gradle/gradle.properties` |

## 6. 验证基线备忘

- 正确测试任务名：`gradlew.bat :app:testAppDebugUnitTest`（`testDebugUnitTest` 是空任务，别用）；
- 编译任务：`:app:compileAppDebugKotlin`；lint：`:app:lintAppDebug`；
- 打包：`:app:assembleDebug`；APK 文件名带时间戳，安装必须动态取最新文件；
- CI 的 assemble task 名需为 `assembleAppRelease`（大小写敏感，见 §3.2）；
- Gradle 需写 `GRADLE_USER_HOME`（本机 `D:\gradle_home`），wrapper 锁文件需写权限。

## 7. 待确认项（核实后收敛，仅剩 2 项需拍板）

**已核实、无需再确认：**

1. ~~Cronet `.so` 来源~~ → 运行时从 Google CDN 下载的动态库，不打进 APK；4 个 so task 是 md5 清单生成器，保留。
2. ~~CI task 名~~ → **确认是 bug**：`assembleapprelease`（全小写）≠ `assembleAppRelease`（variant 名 `appRelease` 已由 `build/intermediates` 实锤），P0 直接改为 `assembleAppRelease`。
3. ~~P0 清理范围~~ → **四项均零引用**（`grep` 核实：死 rhino jar 唯一命中是注释行；firebase/google-services 在 `*.kt`、`*.xml`、`*.gradle` 中无活动引用），可安全删除，无需逐个确认。

**仍需拍板：**

4. 版本号方案：**A（`-PVERSION`/`-PVERSION_CODE` 外部注入，推荐）** 还是 B（时间戳移任务内）？——A 才能彻底消除配置阶段非确定性、解锁配置缓存；B 保留时间戳但需额外处理 provider 的配置缓存兼容性。
5. 是否授权我执行 P0，以及**沙箱权限**怎么给：跑 gradle 需写 `D:\gradle_home`（当前 workspace-write 已实测被拒），要么提升到 `danger-full-access`，要么把 `GRADLE_USER_HOME` 改到 workspace 内（`.gradle_home`，需重新下载依赖）。
