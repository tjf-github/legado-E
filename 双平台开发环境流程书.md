# 双平台开发环境流程书（Windows + WSL/Linux）

> 状态：已完成搭建并验证（2026-08-17）。本文档是环境事实 + 注意事项的备份，供 Windows 侧与 WSL 内 agent 共读。
> 一句话版本：Windows 在 `D:\vsproject\legado-E`，WSL 在 `~/legado-E`，同一 Git 远程 `tjf-github/legado-E`，双向 `push`/`pull` 同步，两套构建环境互不依赖。

## 1. 环境事实（两侧）

| 项 | Windows 侧 | WSL/Linux 侧 |
|---|---|---|
| 项目路径 | `D:\vsproject\legado-E` | `~/legado-E`（WSL2，Ubuntu 22.04 系） |
| JDK | 17（`JavaLanguageVersion.of(17)`，AGP 8.13.2） | 17.0.19（`openjdk-17-jdk`，`/usr/lib/jvm/java-17-openjdk-amd64`） |
| Gradle | wrapper 8.14.4 | wrapper 8.14.4（发行版镜像下载，见 §4） |
| Android SDK | Windows SDK | `~/android-sdk`（`platforms;android-36` + `build-tools;35.0.0` + `platform-tools`），环境变量 `ANDROID_HOME`/`ANDROID_SDK_ROOT` 已写入 `~/.bashrc` |
| 内存 | — | 7.6Gi（WSL 上限=Windows 物理内存的 50%），swap 由 `.wslconfig` 提供（`swap=8GB`） |
| Gradle 堆 | `-Xmx6g`（仓库默认） | `-Xmx3g`（本地改动，见 §2） |

- flavor 是 `app`（`flavorDimensions=['mode']`），所以构建任务名是 **`:app:assembleAppDebug`**（`assembleDebug` 等价，因为只有一个 flavor）。AGENTS.md 里编译/单测门槛同理是 `:app:compileAppDebugKotlin` / `:app:testAppDebugUnitTest`。
- `compileSdk = 36`、`targetSdk 36`、`minSdk 21`；`CronetVersion=128.0.6613.40`。
- 依赖仓库：阿里云镜像优先 + `google()` + `jitpack` + `mavenCentral`（见 `settings.gradle`）；cronet 的 5 个 jar 与 `assets/cronet.json` 已入库，clone 不缺文件。

## 2. WSL 侧本地私有改动（勿提交、勿同步到 Windows）

以下改动仅存在于 WSL 的 `~/legado-E`，用 `git update-index --skip-worktree gradle.properties` 标记，`git status`/`git add -A`/`git pull` 都不会被它们干扰：

- `gradle.properties`：`org.gradle.jvmargs` 里 `-Xmx6g` → `-Xmx3g`（7.6Gi 内存的稳妥值；Windows 侧保持 6g 不变）。
- （可选）若仍 OOM：追加 `echo "kotlin.daemon.jvmargs=-Xmx1536m" >> gradle.properties`。

恢复默认：`git update-index --no-skip-worktree gradle.properties && git checkout -f gradle.properties`。

## 3. 构建命令（WSL 侧）

```bash
cd ~/legado-E
./gradlew :app:assembleAppDebug
find app/build/outputs/apk -name "*.apk" -type f   # 产物 app/build/outputs/apk/app/debug/legado_app_*.apk
```

- 首次构建下载依赖 10~30 分钟属正常；失败重跑同一条命令即可（Gradle 续传）。
- 版本号未传 `VERSION` 参数时用时间戳兜底（如 `3.26.081710`），每次构建会变，正常。
- Debug 构建**不需要**签名文件（`hasSigning=false` 时自动跳过 signingConfig）。

## 4. 网络与下载踩坑记录

- **GitHub 直连极慢**（几十 KB/s）：不要 `git clone` 远程。WSL 能访问 Windows 盘时，直接**本地 clone**：`git clone /mnt/d/vsproject/legado-E ~/legado-E`（只拷 `.git` + 受跟踪文件，远快于全量 `cp`）。clone 后需 `git remote set-url origin https://github.com/tjf-github/legado-E.git` 并 `git config core.autocrlf false`。
- **全量 `cp -r /mnt/d/... ~/legado-E` 会卡死**：WSL 读写 `/mnt/d` 走 9P 协议，几 GB 构建垃圾（`.gradle`/`build/`/`app/build/` 等）小文件海量时极慢，不要用。
- **Gradle 发行版下载慢**：腾讯云镜像 `https://mirrors.cloud.tencent.com/gradle/gradle-8.14.4-bin.zip`（备选华为云 `https://mirrors.huaweicloud.com/gradle/...`）。下载后放进 `~/.gradle/wrapper/dists/gradle-8.14.4-bin/<哈希目录>/`，gradlew 自动跳过下载直接解压。
- maven 依赖走阿里云镜像，一般不需要额外处理。

## 5. 签名与安全（两侧重中之重）

- `keystore.properties`、`release.keystore`、`keystore-base64.txt`、`build.log` 被 `.gitignore` 忽略，**严禁 `git add -f` 提交、严禁打印密码**。
- WSL 的 `~/legado-E` 是 cp/clone 自 Windows 的完整拷贝，签名文件可能也在——保持原样勿动即可（被忽略，不进 Git）。
- Linux 打 Release 需手动复制 `keystore.properties` + `release.keystore` 到 `~/legado-E/`；或者走 CI（推 `main` 触发 GitHub Actions，签名在仓库 Secret 里，本地无需文件）。
- 任何 `git clean` 之前先备份这些本地专属文件。

## 6. 双端同步约定（日常开发）

```bash
# Windows 改完 → WSL 拉取
cd D:\vsproject\legado-E && git add -A && git commit -m "feat: 描述" && git push
cd ~/legado-E && git pull

# WSL 改完 → Windows 拉取（反向同理）
cd ~/legado-E && git add -A && git commit -m "feat: 描述" && git push
cd D:\vsproject\legado-E && git pull
```

- 开发都在 `codex/manga-import`；`main` 是发布分支（push 触发 CI 构建 beta，见 AGENTS.md）。
- Windows 侧推送注意：HTTPS 推送在沙箱下可能报 `schannel SEC_E_NO_CREDENTIALS`——需提权（danger-full-access），仍失败加 `-c http.sslBackend=openssl`；正常信息走 stderr，看到 `branch -> branch` 即成功，别只看 exit code。

## 7. WSL 环境备忘

- WSL2 内存上限 = Windows 物理内存 50%（本机 16GB → 7.6Gi）；扩容靠 Windows 侧 `C:\Users\huixu\.wslconfig` 的 `swap=8GB` + `wsl --shutdown` 重启生效，不要试图超过物理内存。
- 项目必须放 Linux 文件系统（`~/legado-E`），不要在 `/mnt/d` 里直接跑 gradle（9P IO 慢、易出权限/符号链接问题）。
- 检查清单（换机器/重装后）：`java -version`=17 → `./gradlew --version`=8.14.4 → `sdkmanager --list_installed` 含 android-36 → `./gradlew :app:assembleAppDebug` 出 APK。
