# 第五期计划书：售后与收尾桶（开放，不一定是终点）

> 状态：**预留（2026-08-13）**，作为售后/收尾的承接目录；后续手测反馈、使用中问题、新需求均可归入。
> 机制：第五期不是最后一期。新任务可随时新开对话/线程，本计划书仅做汇总与承接；完成一项即勾选一项。

## 1. 定位

- 第四期 4.1~4.5 已全部实施（代码侧），但**真机/模拟器手测尚未执行**；
- 四期交付中有少量“部分完成”项（见 §2），需按手测体感决定是否补全；
- 手测/使用中发现的 bug 与改进建议进入 **售后修复池**（§3），按“复现 → 定位 → 修复 → 验证”闭环处理；
- 产品候选功能（§4）不承诺排期，需要时新开任务。

## 2. 四期遗留（部分完成/未做）

| 项 | 现状 | 建议 |
| --- | --- | --- |
| 4.4.6 大目录分批扫描 | 仅进度提示（系列模式 x/y） | 若手测慢：批量查重（`bookDao.has` 合并一次查询）+ 分批扫描；目录章节懒加载一般不需要（漫画单书章节少） |
| 4.4.2 作者/系列元数据 | 封面识别已做；系列沿用父目录名；作者未推导 | 作者保持手动编辑；如需要再补“文件夹名分隔符推导”，不智能清洗 |
| 真机回归 | androidTest 2 用例（事务回滚、书架分类）+ 手测清单未执行 | 用户执行；代码侧提供测试与清单 |
| modules/web 治理 | `.gitattributes` 已固定生成文件 LF；`tsconfig` 已回退 | 上游同步纪律：web 模块改动尽量随上游，不在本分支积压无关改动 |
| 4.4.6 目录页章节懒加载 | 未做 | 默认不需要；大目录书出现卡顿再评估 |

## 3. 售后修复池（手测反馈驱动）

- 转本地：导出仅含已缓存章节的预期管理、导出→导入全流程；
- 相册导入：权限拒绝/同意、单书单章阅读、相册重命名后 bookUrl 稳定性；
- 图片间距档位：0/小/中/大生效、旧 `imageNoGap=false` 数据映射；
- 统一显示设置面板：方向切换、灰度/墨水屏互斥、阈值、面板与菜单状态同步；
- 扫描进度/封面/混合结构建议角色的实际观感；
- 一/二/三期回归：拆分（超集格式）、合并撤销、事务化、书源书籍常驻。

### 3.1 拆分设计改进（2026-08-13 评审，按优先级）

> 背景：拆分整体设计（插入语义、标题保留原文、firstTitle 补入、编号规范化、事务化）经评审与手测确认正确；
> 以下为薄弱点与建议，按优先级排，可独立立项。

1. **正文空行保真**（数据正确性，建议先做）：`ChapterSplitter.split` 用 `filter { it.isNotBlank() }` 丢弃了全部空行，
   拆分后的单元正文段落结构被压缩；应只跳过空行做标题判断，正文原样保留空行；
2. **字数统计刷新**（低成本高感知，手测实锤）：拆分/合并/增删后目录仍显示旧字数；
   应在操作后主动刷新 wordCount；
3. **命中来源标注**（可排查性）：拆分预览为每个单元标注“命中规则类型”（第N章/Chapter N/包裹符），
   减少“为什么没拆开/为什么拆了这行”的困惑；
4. **撤销拆分（轻量）**（与合并不对称的补足）：拆分前备份原章完整正文，提供“撤销上次拆分”；
   优先级低于 1~3（长按触发 + 预览确认，误操作概率低）；
5. **外部变更提示**：源文件外部修改触发重解析时会清覆盖文件、静默丢弃本地拆分/编辑；
   检测到该书有本地编辑时应先提示再清理；
6. 低优先：文件写入与 DB 事务不同步（失败回滚已写覆盖文件）；同编号重复单元预览弱提示；
   超长章节拆分进度；原文编号跳号提示。

## 4. 候选功能（需要时新开任务）

- 书架分类“其它”折叠（第四期 §4.6 产品建议，已记录设计，未立项）；
- 合并撤销栈 / 合并标题开关（四期暂缓）；
- 目录批量改名/重排（拖拽排序）；
- 目录规则可视化编辑（拆分/合并规则界面化）；
- 漫画阅读设置全局模板；
- 在线书转本地后的批量合并/整理；
- GSON 序列化进一步加固（如需覆盖其他 java.time 字段，已在工具层留好适配器）；
- 批量查重优化落地（§2 建议项，待手测确认）。

## 5. 收尾检查清单（本桶完成定义）

- [ ] 真机回归通过（androidTest + 一~四期手测清单）；
- [ ] 手测反馈问题全部闭环；
- [ ] 四期遗留“部分完成”项已按体感决策（补全或明确不补）；
- [ ] 工作区卫生收尾（无遗留未跟踪文件、`modules/web` 无无关改动）；
- [ ] 复盘总结更新至最新。
