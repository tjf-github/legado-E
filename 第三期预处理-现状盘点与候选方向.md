# 第三期预处理：现状盘点与候选方向

> 日期：2026-08-12 · 状态：预处理完成，**未开展第三期功能开发**
> 依据：第一期计划书（目录章节增删）、第二期计划书（本地漫画导入）＋仓库代码核查

## 0. 阶段划分决策（2026-08-12 用户确认）

- **第三期 = 方向 A（批量拆分粘连章节）＋ 方向 C（中文数字标题重写）＋ 方向 F（工程质量补齐）**；
- **第四期 = 方向 B（多选合并）＋ 方向 D（在线书转本地引导）＋ 方向 E（漫画侧增强）**；
- 对第三期影响最大的问题（构建基线、事务化/回滚、核心测试）给予最高优先级，先消除再进入功能开发；
- 具体优先级与执行顺序见 §4，第三期计划书按此起草。

## 1. 一、二期落地核对（代码事实）

### 1.1 第一期：目录「新增/删减章节」

| 模块 | 代码位置 | 落地情况 |
| --- | --- | --- |
| DAO 方法 | `data/dao/BookChapterDao.kt`（`delChapter`/`upIndexTitle`）、`data/dao/BookmarkDao.kt`（`shiftChapter`/`delByChapterIndex`） | 已实现（提交 1725e4215） |
| 核心逻辑 | `ui/book/toc/TocViewModel.kt`（`insertChapter`/`deleteChapter`/`renameChapterFile`） | 已实现（提交 b14a42b37） |
| 标题重写工具 | `help/book/ChapterNumberUtils.kt`（`rewriteTitle`/`nextNumberTitle`） | 已实现，支持“第N章/回/节”与 `Chapter N` |
| 单元测试 | `test/.../ChapterNumberUtilsTest.kt`（6 组用例） | 已实现 |
| 目录 UI | `ChapterListAdapter.onChapterMenu`、`ChapterListFragment` 长按菜单＋新增/删除对话框 | 已实现（提交 33f3bca28、db9f3ff16） |

### 1.2 第二期：本地漫画导入

| 模块 | 代码位置 | 落地情况 |
| --- | --- | --- |
| 扫描器 | `model/localBook/MangaFolderScanner.kt`（三层识别、自然排序、整本/系列模式）＋ `MangaFolderScannerTest.kt` | 已实现（提交 9026b68ca） |
| 导入入口 | `BaseBookshelfFragment` 菜单 → `ui/book/import/manga/ImportMangaActivity` | 已实现 |
| 建书与预览 | `ImportMangaViewModel`（扫描、预览、改名、行级“整本/章节”切换、按 URI 查重、分组创建）＋ `ImportMangaAdapter` | 已实现 |
| 正文供给 | `model/localBook/LocalBook.kt` 的 `getChapterList`/`getContent` 新增 `isImage && isLocal` 分支 | 已实现（提交 807dd804c） |
| 漫画阅读器接入 | `utils/ContextExtensions.kt`：`book.isImage && showMangaUi → ReadMangaActivity`（已放开本地限制） | 已实现（提交 6d1e6c459） |
| 竖排无间隙开关 | `data/entities/Book.kt`（`imageNoGap` 配置）、`ReadMangaActivity` 菜单、`MangaAdapter`/`TextChapterLayout` 间距 | 已实现（提交 96a058178） |
| 书架分类 | `data/dao/BookDao.kt`：书源书籍、本地小说（local 非 image）、本地漫画（local 且 image）查询 | 已实现（提交 407f0a5df、96a058178） |

结论：两份计划书所述的“首版实现”均已落地在 `codex/manga-import` 分支上；工作区中的改动仅涉及 `modules/web` 三个生成文件与若干未跟踪文件，与一、二期功能无直接关系。

## 2. 计划书遗留事项（未完成 / 未验证）

1. **真机回归未完成**：一期 §8 测试清单、二期 §8.1 真机清单均未勾选；
2. **大目录懒加载/分页未实现**：`MangaFolderScanner` 全量 `list()` 扫描，上千文件夹/图片场景有卡顿风险；
3. **自动化测试覆盖不足**：`BookDao` 分类查询、`Book.config`（含 `imageNoGap` 等）序列化、`TocViewModel` 插入/删除主流程均无单测；
4. **一期插入/删除未事务化**：计划书 5.1 推荐事务或“内存备份＋回滚”，当前实现为顺序执行、无备份回滚；第三期若叠加批量操作，该缺口会放大；
5. **构建环境待恢复**：`build.log` 最近一次 `BUILD FAILED`（Gradle transform 缓存反序列化失败，`Could not deserialize analysis`）；`.gradle_home/` 已被清理并加入 `.gitignore`，开工前需重建干净编译基线；
6. **工作区卫生**：`build.log`、`watch-build.ps1`、`make_test_book.py`、`test_book.txt`、`测试漫画素材/` 未跟踪；`modules/web` 三个生成文件有本地改动。

## 3. 第三期候选方向

### 方向 A：批量拆分粘连章节（建议主攻）

一期计划书 §12 预留第一项，且一期明确“一期逻辑是它的基础”。拆分本质＝按标题正则定位切分点，把一章内容拆成多章，再批量复用一期的“插入＋级联重排＋改名＋书签/进度同步”。

**可直接复用的代码**：

| 能力 | 复用点 |
| --- | --- |
| 标题编号重写 | `ChapterNumberUtils.rewriteTitle` / `nextNumberTitle` |
| 级联重排/改名/书签同步 | `TocViewModel.insertChapter` 主体（建议抽取公共函数） |
| 正文读写 | `BookHelp.getContent`（覆盖文件优先）/ `saveText` / `delContent`，`TextFile` 的 `start/end` 偏移 |

**需新增**：拆分规则（标题正则/编号模式）、拆分预览确认、批量执行与异常回滚、阅读进度同步。

**前置条件**：一期的插入流程事务化（或内存备份回滚）、长正文拆分性能验证。

### 方向 B：多选合并冗余章节

复用一期的删除/重排机制，内容拼接后写入新覆盖文件。需要新增：目录多选模式、合并确认对话框、书签归属策略。

### 方向 C：中文数字标题重写

`ChapterNumberUtils` 扩展“第一百章/壹佰章”解析与重写（+1/-1），补单测；可独立成期，也可并入方向 A（拆分后中文数字标题重排）。

### 方向 D：在线书“转本地”操作引导

一期边界中已明确“导出文件 → 导入本地”的路径，本期可做成产品引导/一键操作，改动小、依赖少。

### 方向 E：漫画侧增强（第二期 §11 评估项 + §13 产品建议）

1. 单本相册导入（MediaStore 扁平结构）；
2. 书/章节元数据自动识别（封面、作者）；
3. 混合结构自动化识别（替代预览行级手动切换）；
4. 统一“漫画阅读设置”面板（竖排/横排、无间隙、缩放整合）；
5. 图片间距大小可调（无间隙开关的扩展）；
6. 大目录懒加载/分页（遗留项，独立可做）。

### 方向 F：工程质量补齐（可独立成期，或作为第三期前菜）

- 补齐单测（`BookDao` 分类查询、`Book.config` 序列化、`TocViewModel` 流程）；
- 一期 insert/delete 事务化与回滚；
- 构建脚本封装（固定 `ANDROID_PREFS_ROOT` 与 debug 密钥；`watch-build.ps1` 已有雏形）；
- 工作区清理与未跟踪文件收编。

## 4. 开工前置条件（无论第三期选哪个方向）

按对第三期的影响程度排序（**高优先级先做，做完再进入功能开发**）：

1. **干净构建基线**（已完成 ✅）：清理 Gradle 缓存后 `.\gradlew.bat :app:compileAppDebugKotlin`（实际任务名，早期写法的 `compileLegadoDebugKotlin` 已废弃）与现有单测通过；
2. **一期插入/删除事务化＋失败回滚**（A 的强依赖）：当前 DAO 均为纯接口、无 `@Transaction`，批量操作失败会留下半完成状态；
3. **核心测试补齐**：中文数字标题重写、批量拆分纯逻辑（可 JVM 单测）；`BookDao` 分类查询、`Book.config` 序列化、DAO 流程测试可入 `androidTest`（基建已有）；
4. **真机回归一/二期**（用户侧执行，不阻塞编码、阻塞验收）：至少勾完二期 §8.1 清单；
5. **工作区卫生**：顺手清理/收编未跟踪文件，成本低。

> 大目录懒加载/分页属于方向 E（第四期），不进入第三期阻塞项。

## 5. 待用户确认

1. 第三期主方向：A / B / C / D / E / F，单选或组合；
2. 是否将“构建基线、事务化、补测试”作为第三期开工前置（推荐）；
3. 拆分规则形态（方向 A）：固定正则（沿用 `ChapterNumberUtils` 模式）还是用户可配置。
