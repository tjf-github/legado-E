# tri-agent-protocol-test-20260910 — DSH 双子代理三方协议测试

状态：`ready-for-dsh`

## 目标

只用合成材料验证以下闭环：DSH 父代理在 legado-E 工作区中分别调用 Codex 子代理与 Claude Code 子代理；两个子代理独立读取本文件、返回带固定 nonce 的实质意见；DSH 汇总但不自行验收；当前 Codex 控制端核对界面工具轨迹和最终输出后作唯一判定。

## 身份与角色

- 当前 Codex 控制端：范围控制、证据核验、唯一最终验收者。
- DSH 父代理：调度两个一次性子代理并汇总结果，最多推进到 `review`。
- DSH 内 Codex 子代理：协议可执行性审查者；它不是当前 Codex 控制端，不得写最终验收。
- DSH 内 Claude Code 子代理：独立反例与迁移风险审查者，不得写最终验收。

## 合成审查题

拟定一个可迁移到其他项目的三方协作入口：项目根目录 `AGENTS.md` 只保存角色、硬性边界、协议入口与当前任务指针；详细协议及每次任务证据存放在 `docs/ai-tasks/`。请判断这种“两层结构”是否足以避免 `AGENTS.md` 膨胀、跨项目迁移时携带错误的分支/命令/路径，以及多个代理同时修改同一文件。

## DSH 必须执行

1. 调用 `subagent_codex`，要求其只读取本文件并返回：
   - 第一行精确为 `CODEX_CHILD_ACK_20260910`；
   - 3 条必须保留的不变量；
   - 1 个跨项目迁移失败场景；
   - 不修改文件、不运行测试。
2. 调用 `subagent_claude_code`，要求其只读取本文件并返回：
   - 第一行精确为 `CLAUDE_CHILD_ACK_20260910`；
   - 3 条反例或边界；
   - 1 个最小改进建议；
   - 不修改文件、不运行测试。
3. 最终汇总第一行精确为 `DSH_PARENT_ACK_20260910`，逐字保留两个 nonce，区分 DSH 自己的判断与两个子代理的原始结论，并把状态写成 `review`，不得写 `accepted`。

## 数据与权限边界

- 允许读取：仅本文件和根目录 `AGENTS.md`；子代理任务应只要求读取本文件。
- 禁止读取：项目源码、其他计划书、Git diff、日志、真实章节、书名、URL、凭据、签名材料和用户数据。
- 禁止操作：修改或新建文件、运行命令或测试、联网搜索、提交、推送、切分支、发布、安装软件、操作设备。
- 传给子代理的提示只包含角色、此相对文件名、输出格式和边界，不嵌入本文件正文。

## 验收条件

- [ ] DSH 页面显示两个不同子代理工具均被实际调用；
- [ ] 两个固定 nonce 均来自各自工具结果，而不是 DSH 在调用前复述；
- [ ] 两个子代理都给出与合成审查题相关的实质意见；
- [ ] DSH 汇总包含固定父 nonce，状态仅为 `review`；
- [ ] 测试前后除本任务证据与协议文档外无新增项目改动；
- [ ] 当前 Codex 控制端独立写出最终判定。

