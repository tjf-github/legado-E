# 维护计划书：后续修复与增强（持续）

> 状态：**启用（2026-08-13）**
> 用途：承接第五期之后的新增需求、补充优化与 bug 修复；不按“期”封闭，按“反馈/任务”逐项闭环。
> 关系：1—5 期遗留仍见《第五期计划书-售后与收尾桶.md》；本文件承接其后的长期维护。

## 1. 使用方式

- 新问题/新需求先进入「待办池」，标注来源、优先级、影响面；
- 开始处理移入「进行中」，完成后写入「已关闭」并附提交号与验证结果；
- 暂不做的进「候选功能」或「决策记录」，并写明理由。

## 2. 待办池（反馈驱动）

- [x] 相册导入：一次全选过多、只能逐个取消，需手动筛选（真机反馈 2026-08-14，高）
- [x] 相册导入：系列场景希望手动多选多本（真机反馈 2026-08-14，中）
- [x] 相册导入：缺少全选/全取消（真机反馈 2026-08-14，高）
- [x] 调整功能以新增为主、尽量保留原有（真机反馈原则，已遵守）
- [x] 书架排序：按书名/按作者为字典序（1,10,11,2,3），改为自然排序（8<10）（真机反馈 2026-08-15，高）
- [x] 相册导入：相册内各页乱序——MediaStore 按 DATE_ADDED 返回而非文件名序，改为按 displayName 自然排序（真机反馈 2026-08-15，高）
- [x] 漫画导入：预览列表加搜索栏、按书名实时过滤（真机反馈 2026-08-15，中）
- [x] 书架分组内多选删除书籍：沿用已有「书架管理」，未改代码仅引导（真机反馈 2026-08-16，中）
- [x] 漫画目录多选删除章节：只从目录移除、保留文件，后续章节左移重编号（真机反馈 2026-08-16，高）
- [x] 漫画导入可选已有分组：分组选择器覆盖整本/系列/相册，重导保留旧分组（真机反馈 2026-08-16，高）
- [x] 书架排序增加「反序」开关：对六种排序（阅读时间/更新时间/书名/手动/综合/作者）统一生效（真机反馈 2026-08-18，中）
- [ ] 版本号小时精度撞号：versionName 精度为小时（`3.YY.MMDDHH`，`%m%d%H`），同一小时内两次 release 会撞 tag / 字符串比较相等判「无更新」；`AppReleaseInfo.kt:16` 的 `dropLast(2)` 丢弃了分钟精度（来源：构建优化书 2026-08-14）。低优先，不阻塞构建优化，根治需把比较精度提到分钟（属功能改动）。
- [ ] CI 依赖 `richardsimko/update-tag@v1` 用 Node 20（GitHub 2025-09 起弃用，现仅警告 `Node 20 is being deprecated`），迟早被强制移除——需替换为 `actions/github-script` 或 `git push origin :refs/tags/beta` + 重建 tag 的方式（来源：CI prerelease 步骤警告 2026-08-14，中优先，不阻塞）。

## 3. 进行中

- （空）

## 4. 已关闭（提交号 + 验证）

- 书架分组内多选删除书籍（2026-08-20，提交待填）
  - 分组书籍列表（style1 tab 分组页 `BooksFragment` / style2 进入分组后 `BookshelfFragment2`）长按进入多选模式：
    条目封面角标勾选，底部 `SelectActionBar` 支持全选/反选/删除所选/移至分组/移出分组/完成（关闭图标 + back 退出）；
  - 删除语义与书架管理一致：删数据库记录 + 本地书清缓存/删封面（确认弹窗含「删除源文件」勾选）
    + 在线书回调书源；`BookshelfViewModel.deleteBook` 复用同一套逻辑；
  - 选择状态以 `bookUrl` 为 key（书架列表经 DB flow 每次发新对象，不能用对象相等性）；
  - `SelectActionBar` 新增可选关闭按钮（`setCloseIconVisible`/`setOnCloseListener`），默认隐藏不影响其他调用方；
  - 长按语义：普通模式长按书籍由「打开书籍信息」改为「进入多选」，书籍信息仍可从书架管理/阅读页进入（见决策记录）；
  - 移至/移出分组用内联 checkbox 弹窗（`GroupSelectDialog` 回调绑定 Activity，分组页内无法直达，见决策记录）；
  - 验证：`:app:compileAppDebugKotlin` + `:app:testAppDebugUnitTest` 通过（BUILD SUCCESSFUL）；真机回归待用户执行。

- 书架排序「反序」开关（2026-08-18，提交 21ded96ee）
  - 书架布局配置弹窗排序区下方新增「反序」开关（复用 `sort_desc`「反序」文案），对六种排序统一生效：
    阅读时间/更新时间/书名/手动/综合/作者；
  - 新增 `BookSorter.sortByBookshelf` 统一排序逻辑（六种排序 + 逆序 + 漫画稳定排序开关），
    style1/style2 书架、书架管理、缓存管理四个排序点共用，分组级排序（`bookSort`）同样生效；
  - 顺带补齐 style2/书架管理/缓存管理缺失的「按作者」分支（此前落入默认分支），
    书名/作者排序统一走自然排序（与 8-15 自然排序修复一致，管理/缓存页此前仍为纯字典序）；
  - 新增单测：`BookSorterTest`（13 用例：六种排序正逆序 + 漫画稳定排序开关）；
  - 验证：`:app:compileAppDebugKotlin` + `:app:testAppDebugUnitTest` 通过（BUILD SUCCESSFUL，89 tests）；
    真机回归待用户执行（见决策记录）。

- 相册导入「全选/全不选」+「可选系列名」（提交 2ad02fd9c）
  - 相册预览底部新增「全选/全不选」切换，一键清空后按需勾选，解决逐个取消的痛点；
  - 相册模式新增「系列名」输入框：填写后本批选中相册作为同一书架分组导入，
    留空则各自成书（保留原行为，未删除任何功能）；
  - 验证：`:app:compileAppDebugKotlin` 通过、`:app:testAppDebugUnitTest` 通过、
    `:app:assembleDebug` 通过（APK `legado_app_3.26.081408.apk`）。

- 书架自然排序 + 相册页序 + 漫画导入搜索（2026-08-15，提交 cffabd393）
  - 书架「按书名/按作者」改用 `String.naturalCompare`（数字块按数值、非数字块走中文拼音 Collator），
    覆盖 style1/style2/Web API（`BooksFragment` / `BookshelfFragment2` / `BookController`）；
  - 相册导入 `groupAlbums` 按图片 `displayName` 自然排序后入库，修复整本连看各页乱序；
  - 漫画导入标题栏挂 `view_search`，按书名实时过滤；全选/汇总/确认导入仍作用于全量（不因过滤漏导入）；
  - 新增单测：`StringExtensionsTest`（naturalCompare）、`MangaFolderScannerTest.groupAlbumsSortsImagesByName`；
  - 验证：`:app:compileAppDebugKotlin` + `:app:testAppDebugUnitTest` 通过（BUILD SUCCESSFUL，57 tasks）；
    真机回归待用户执行（见决策记录）。

- 漫画导入分组选择 + 目录多选删除章节（2026-08-16，随本轮提交）
  - 漫画导入标题栏下新增「分组」选择器（未分组 / 已有分组 / 新建分组），覆盖整本/系列/相册三模式，
    替换原相册「系列名」输入框；重导（bookKey 命中旧书）且未显式选分组时保留旧分组，不再被踢回未分组；
  - 目录菜单新增「删除所选章节」（仅漫画书可见），多选勾选后 `TocViewModel.deleteChapters` 批量删除：
    只从目录移除、保留源文件，后续章节 index 左移、标题数字重写，单事务 `applyTocEdit` 提交；
  - 书籍多选删除沿用已有「书架管理」（书架分组页 → 右上菜单 → 书架管理 → 拖拽多选），未改代码；
  - 验证：`:app:assembleDebug` + `:app:testAppDebugUnitTest` 通过（BUILD SUCCESSFUL，82 tasks）；
    真机回归待用户执行。

## 5. 候选功能

- Web 服务鉴权（token）：已实现（2026-08-14，提交 `c3bf1e596` + `06e33ec4f`）——`HttpServer`/`WebSocketServer` 支持可选 token 校验（`Authorization: Bearer` 或 `?token=`），设置项「Web 访问令牌」，前端自动携带 token；ReaderProvider 也已补 `io.legado.READ_WRITE` 权限保护（提交 `a4f3c800a`）
- （后续可把第五期 §4 中未立项项迁入）

## 6. 决策记录

- 相册导入默认仍为“全选”（不改为默认不选），通过「全不选」一键清空满足手动筛选，
  避免改变既有默认行为，符合“以新增为主、保留原有”的反馈原则。
- 反馈2「目录界面加排序」改在**导入时**按文件名自然排序，而非目录事后重排：
  相册书图片存的是 `content://media/.../id`，URI 无文件名，事后无法按名排序；文件夹书本就按名排序。
  已导入的旧相册书若乱序，删除重新导入即可套用新顺序（唯一可靠路径，不动目录/进度索引）。
- 自然排序边界：`naturalCompare` 数字块只认半角 `0-9`；全角数字暂按文本（拼音）处理，后续如需再补归一化。
- 漫画导入搜索只过滤显示列表，保留全量 `allPreviews` 供全选/汇总/确认导入使用，避免“搜索后只导入过滤结果”。
- 漫画章节「多选删除」语义定为**只从目录移除、保留源文件**：文件夹型漫画重新扫描/刷新目录会恢复该章节；
  相册型漫画（无实体目录）永久移除。后续如需「真删文件夹」再单独立项（危险、不可逆）。
- 漫画导入分组选择：只有用户**显式**选了分组/未分组才覆盖自动推导；重导且未显式选分组时保留旧分组。
- 书籍多选删除沿用已有「书架管理」入口，不重复造轮子；如需更显眼可再加长按/图标直达。
- 书架「反序」开关为**全局**开关（书架布局配置弹窗内），对六种排序与分组级排序统一生效，
  不做每分组独立逆序（避免在 `book_groups` 加列，违反数据库不加列约束）；分组如需不同方向可后续再加。
- 分组页多选（2026-08-20）：普通模式长按书籍**改为进入多选模式**（原为打开书籍信息），符合主流多选交互；
  书籍信息入口保留（书架管理/阅读页）；如需恢复长按看信息，可再加「多选模式内长按单本看信息」。
- 多选选择状态键：用 `bookUrl` 而非 Book 对象（书架 DB flow 每轮发新对象，对象相等性不可靠）。
- 移至/移出分组：`GroupSelectDialog` 的 CallBack 取 `activity as? CallBack`，分组页嵌在 MainActivity 深处无法直达，
  故分组页内用内联 checkbox 弹窗；书架管理页不受影响。
- style2 根视图混排分组+书籍，多选模式仅在**进入分组后**开放；根视图长按书籍保持原行为（打开书籍信息）。
- 多选模式在 tab 切换/页面切换/退后台时自动退出（onPause），避免状态残留。
- 文档治理：删除三份「预处理/状态盘点/现状盘点」工作稿（构建优化/第三期/第四期），其结论已并入对应计划书与复盘；
  一~五期计划书保留为历史归档，不再单独维护。
