# 日志系统优化计划书

> 状态：**P0/P1/P2 accepted（2026-09-08）；代码、自动化门禁与 P2 真机验收完成，待按发布流程处理提交范围**
> 定位：面向 legado-E 的日志能力整改，目标同时覆盖「排障便利」与「可维护性」，不做功能语义变更，不新增数据库表/列。
> 前置结论：日志功能已被埋点/异常/监控多处使用，且与「记录日志」「保存日志」「崩溃日志」入口联动；任何改动必须先保证：编译 + 全量单测通过、导入/阅读路径零侵入、recordLog 开关语义不回退。

## 1. 计划目的

当前日志能力存在「三套并行、两处割裂」的问题，导致排障时要在三层里来回找，维护时不清楚该走哪个 API。本计划书用于：

- 汇总日志系统现状（模块、数据流、开关语义）；
- 识别影响排障、健壮性、性能、可维护性的具体问题；
- 给出分阶段、低风险、可验证的整改方案与排序；
- 划清边界：本次只做日志层整改，不改业务行为、不改数据库、不改导入/阅读路径语义。

本文件是**方案与排期**，不代表全部立即执行；是否启动、先做哪些，按阶段评审决定。

## 2. 现状梳理

### 2.1 模块清单

| 文件 | 角色 | 说明 |
| --- | --- | --- |
| `AppLog.kt` | 内存日志门面 | 环形缓冲最近 100 条 `Triple<Long, String, Throwable?>`；`put` 写文件，`putNotSave` 仅内存，`putDebug` 受 recordLog 门控 |
| `LogUtils.kt` | 文件日志 | 基于 `java.util.logging.Logger("Legado")` + 自定义 `AsyncFileHandler`，写 `externalCacheDir/logs/appLog-<date>.txt`，7 天清理，级别受 recordLog 门控 |
| `AsyncFileHandler.kt` | 异步落盘 | 重写 `publish`，改为提交到 `globalExecutor`（单线程）执行 |
| `CrashHandler.kt` | 崩溃记录 | 未捕获异常 → `crash-<time>.log`（externalCache/crash + 备份路径）；OOM 时可堆转储；部分异常被 `shouldAbsorb` 吸收 |
| `AppFreezeMonitor.kt` | 冻屏监控 | 仅 recordLog 时注册；检测系统冻结、屏亮/灭 |
| `DispatchersMonitor.kt` | 调度器监控 | 仅 recordLog 时监控 IO/Default/Main 超时 |
| `AppLogDialog.kt` | 日志查看 UI | 只读内存环形缓冲，可清空 |
| `CrashLogsDialog.kt` | 崩溃日志 UI | 读 externalCache/crash + backupPath 的 crash 文件 |
| `AboutFragment.kt` | 导出入口 | saveLog() 会打包 logs/crash/logcat 到备份目录 |

### 2.2 数据流（现状）

```
[业务代码] -> AppLog.put(msg, e, toast)
              |- toastOnUi(msg)            （toast=true 时）
              |- LogUtils.d("AppLog", msg + 可选 stack)   -> AsyncFileHandler -> logs/appLog-*.txt
              |- mLogs.add(0, ...)          （内存环形，供 AppLogDialog）
              \- DEBUG: Log.e(stack[3], msg, e)            （仅 DEBUG）

[业务代码] -> LogUtils.d(tag, msg)          （绕开 AppLog 内存，直接落盘；共 48 处）
                  |
                  \- AsyncFileHandler -> logs/appLog-*.txt

[崩溃]     -> CrashHandler -> crash-*.log（externalCache + backupPath）
```

### 2.3 开关语义

- `recordLog`（PreferKey.recordLog）：总开关。控制 fileHandler 级别（INFO/OFF）、`putDebug`、两个 Monitor、UrlUtil 响应头、Glide 日志等。分散在 `LogUtils` / `AppLog` / `AppFreezeMonitor` / `DispatchersMonitor` / `UrlUtil` / `Glide` / `LiveEventBus` 多处各自判断。
- `recordHeapDump`：只控制 OOM 时的堆转储。

## 3. 问题诊断

### 3.1 排障不便（用户/开发者视角）

1. **AppLogDialog 只显示内存最近 100 条，与落盘日志脱节。**
   - 在 App 内「查看日志」看到的是本进程内存缓冲（且不含直接走 `LogUtils.d` 的条目，如 DeviceInfo、Monitor、LiveEventBus），一旦进程重启即丢失；
   - 真正用于排障、可通过「保存日志」导出的文件日志，在 App 内无任何查看入口；排障要么靠记 toast，要么导出后另开工具看文件。
   - 结果：内存视图与文件视图是**两套不一致**的快照，无法在故障现场直接看到完整落盘内容。

2. **日志与 toast 耦合，内容被用户文案污染。**
   - `AppLog.put(msg, e, true)` 用**同一条字符串**既当 toast 又当日志。大量调用传的是 `getString(R.string.xxx)` 本地化用户文案或简短「出错」文案，丢失技术细节；
   - 文件日志里因此混入大量面向用户的提示语，真正可定位的信息（异常类、cause 链、上下文 URL/文件名/书本名）反而只在 throwable 里，或被截断。

3. **无级别、无标签、无结构化。**
   - 全部是自由字符串；`LogUtils.e` 定义了却 **0 处使用**，连致命路径都走 `LogUtils.d`（`Level.INFO`），没有 ERROR/WARN 区分；
   - 没有统一的 tag/模块/品类标注（WebDAV、TTS、导入、备份……均为散落字符串）；无法按流程 grep 一条连贯链路，也无法按级别过滤噪音。

4. **`${it.localizedMessage}` 可能为 null -> 日志里出现字面 `null`；`message==null` 时整条被丢弃。**
   - `AppLog.put` 对 null 消息直接 return，部分错误信息被完全丢弃；`localizedMessage` 为 null 的异常会打出 "null"。

5. **`AppLog.put` 在 DEBUG 下每次捕获调用栈 + `Log.e`。**
   - `Thread.currentThread().stackTrace[3]` 既慢又不稳（见 3.2）。

### 3.2 健壮性（真实风险）

1. **`AppLog.logs` 取值未加锁。** `put`/`clear` 是 `@Synchronized`，但 `get() = mLogs.toList()` 不是；在多线程（Service、协程）并发 `add(0, ...)` 时 `toList()` 可能抛 `ConcurrentModificationException`。

2. **`stackTrace[3]` 越界风险。** `Thread.currentThread().stackTrace[3]` 假定至少 4 帧；调用栈过浅时抛 `ArrayIndexOutOfBoundsException`，且每次调用都付出捕获完整栈的成本。

3. **文件按「进程启动日」固定，跨天不滚动。** `createFileHandler` 只在 `init` 时以当天日期建一次 `appLog-<date>.txt`，之后不再按天更换；进程跨零点后仍写起始日文件，长驻进程下日志日期会「错位」，7 天清理的过期判断也会因文件更新时间而失真。

4. **`saveCrashInfo2File` 备份路径写失败被静默吞掉。** `catch (_: Exception) {}`；一旦备份路径失败，崩溃日志仅存于 `externalCacheDir`（可被系统清理），用户无从察觉，也没有 UI 提示。

### 3.3 性能

1. **eager 拼接在关闭时仍分配。** `LogUtils.d(tag, msg)`（非懒加载）无论开没开都执行 `"$tag $msg"` 拼接并构造 `LogRecord`；`AppLog.put` 无条件调用它。懒加载版本存在但热路径未迁移。
2. **单线程 `globalExecutor` 承载日志落盘 + 清理 + 各业务复用。** `AsyncFileHandler.publish` 与日志清理都提交到同一个单线程池；慢存储或瞬时大量日志会导致排队、时序乱，且与业务任务共用同一线程，极端情况相互影响。
3. **内存缓冲持有完整 Throwable。** 100 条 × 完整栈，可能明显大于预期；但作为上限尚可接受，需评估是否对大栈做截断。

### 3.4 可维护性

1. **日志 API 分散、命名不清。** `AppLog.put` / `putNotSave` / `putDebug` / `LogUtils.d` / `LogUtils.e`，开发者很难判断该用哪个，也导致 304 处 `AppLog.put` 与 48 处 `LogUtils.d` 之间无一致约定。
2. **无 tag 常量集中管理。** `"AppLog"`、`"App"`、`"AppFreezeMonitor"`、`"[LiveEventBus]"`、`"DispatchersMonitor"` 等散落各处。
3. **`LogUtils` 职责过多。** 文件句柄创建、级别、格式化、设备信息、7 天清理混在一个 object，且依赖 JUL `FileHandler`（平台相关、难单测）。
4. **`recordLog` 门控分散在 6+ 处**，新增日志点容易漏、容易和 DEBUG 行为混在一起。

## 4. 优化目标

按「排障便利 -> 健壮性 -> 可维护性 -> 性能」的优先级排序，且**每一步都不改业务语义**。

- T1（排障）：让 App 内日志查看器能读到**落盘日志**，支持按级别/标签/关键词过滤；日志与 toast 解耦；补充级别与标签。
- T2（健壮性）：修掉 3 个真实风险（logs 加锁、stack[3] 越界、崩溃日志静默失败）；处理跨天滚动。
- T3（内容质量）：系统化梳理 300+ `AppLog.put` 调用点，规范消息 & 标签，null 兜底。
- T4（可维护性）：统一日志门面 + 集中 tag + 集中门控。
- T5（性能）：热路径懒加载、评估缓冲截断与日志线程隔离。

## 5. 分阶段方案

> 每阶段独立可评审、可回退、可验证；阶段间不互相依赖。建议顺序：P0（安全护栏）-> P1（门面）-> P2（查看器）-> P3（内容质量）-> P4（性能）。

### P0 安全护栏（低风险、先修正确性）

- **logs 取值加锁**：给 `logs` getter 用 `@Synchronized` 或改为在锁内 `mLogs.toList()`；考虑换成 `CopyOnWriteArrayList`。
- **stackTrace[3] 越界**：捕获完整数组，取长度 ≥4 才用下标 3，否则用 `this::class.java.name` 兜底。
- **崩溃日志静默失败**：`saveCrashInfo2File` 的备份路径分支失败时至少 `LogUtils.e` / `AppLog.put`，并避免吞掉异常。
- **跨天滚动（可选）**：`createFileHandler` 改为按天生成句柄，或定时检查日期更换；若评估成本高可先只做「记录真实写入日期」。
- **验证**：`:app:compileAppDebugKotlin` + `:app:testAppDebugUnitTest` 通过；手动触发若干 `AppLog.put` 无并发异常。

### P1 统一日志门面（可维护性核心）

- 将 `AppLog` 提升为**唯一对外门面**，新增结构化方法：
  ```
  AppLog.i(tag, msg) / AppLog.w(tag, msg) / AppLog.e(tag, msg, e)
  AppLog.i(tag) { lazy msg }            // 懒加载
  AppLog.toast(msg)                      // 仅 toast，不写日志（解耦）
  ```
- `put`/`putDebug`/`putNotSave` 保留为兼容包装（deprecated），逐渐替换调用点；行为不变，仅新增入口。
- 新增 `LogTag` 常量对象（如 `TAG_APP`、`TAG_TTS`、`TAG_WEBDAV`、`TAG_IMPORT`、`TAG_MANGA`……），消除散落字符串。
- 集中门控：把 `recordLog` 判断收敛到门面一个点，`UrlUtil`/`Glide`/`LiveEventBus` 等外部日志只保留各自开关。
- **验证**：编译 + 全量单测；抽查 `AppLog` 相关 UI 无回归。

### P2 日志查看器重做（排障最大收益）

- 让 `AppLogDialog` 支持切换「内存视图 / 落盘文件视图」，落盘视图读取 `externalCacheDir/logs/appLog-*.txt`（倒序取末 N 行），并按级别/标签/关键词过滤。
- 保留「清空」，但明确：清空只清内存缓冲，不删文件（历史存证不丢）；文件在「保存日志」或 7 天清理时收回。
- 增加一键「复制当前筛选结果」/「导出全部日志」入口，方便直接回传给开发者。
- 不改数据库、不新增权限，纯读文件；兼容日志开启/关闭两种状态（关闭时提示先开启 recordLog）。
- **验证**：编译 + 单测；手动查看器过滤、复制、导出各流程。

### P3 内容质量（系统性梳理 300+ 调用点）

- 制定日志书写规范（写入仓库文档，供后续所有 agent 遵守）：
  - 前置「模块/操作」标签；首行给**可定位上下文**（书名/URL/文件名/书名源）；异常必须带 throwable；`localizedMessage` 用 `?.let` 兜底，禁止输出字面 `null`；
  - 用户可见提示走 `toast()`，与日志解耦；日志里写技术细节而非用户文案；
  - 高噪音路径（音频焦点、来电、屏态）降为 WARN/仅 debug 或独立节点，便于按级别过滤。
- 按模块分批改造（WebDAV/备份恢复、TTS/朗读、导入、漫画、书架/目录、网络/JS 源），每批独立提交并跑单测。
- **验证**：逐模块手动核对日志与 toast 行为一致；全量单测通过。

### P4 性能与监控（按需）

- 热路径迁移至懒加载（已有 `inline d(tag, lazyMsg)`）。
- 评估：内存缓冲大栈截断（如保留前 k 帧/前 N 字符）；日志线程与业务线程隔离（独立单线程日志池，不复用 `globalExecutor`）。
- 可选：`fileHandler` 增加大小滚转（`setLimit`/多个文件），避免单日单文件无限增长。
- **验证**：无功能回归；日志写入不再与业务任务共享线程。

## 6. 不做什么（边界 & 风险）

- **不新增数据库表/列**（日志在文件与内存，不入 Room）。
- **不改导入/阅读路径语义**；日志门面整改只动日志调用，不动业务执行逻辑。
- **不改变 recordLog 语义**：关闭时不写文件、不注册 Monitor、不上送日志；保持现状「默认关闭」。
- **不引入第三方崩溃/指标 SDK**（保持零网络依赖与隐私边界，除非用户明确立项）。
- **不承诺 100% 覆盖改造 300+ 调用点**：P3 分模块进行，未改造点保持原 API 兼容（不破坏编译），不阻塞其他阶段。

## 7. 验收标准（针对 P2，最可量化）

- 查看器能打开落盘日志并正确显示最近 N 条、含时间与标签；
- 按级别/标签/关键词过滤结果正确；复制/导出内容与查看一致；
- 内存视图与文件视图在「开启 recordLog」下可以自由切换，互不丢失；
- 关闭 recordLog 时给出引导（先开启）；开启后 Monitor 正常注册、文件正常写入；
- 编译与全量单测（`:app:testAppDebugUnitTest`）通过；`lint` 无新增告警。

## 8. 决策记录 / 待确认项

| 项 | 问题 | 需要决定 |
| --- | --- | --- |
| 查看器范围 | 是否允许读取备份目录（doc）里的日志 | 默认只读 externalCacheDir，备份目录是否纳入 |
| 日志级别粒度 | 是否新增独立 `logLevel` 偏好（按级过滤） | 涉及新增偏好项，需确认不破坏现有 recordLog 语义 |
| 崩溃日志 UI | 崩溃日志是否并入统一查看器 | 可做可不做，看是否影响现有 CrashLogsDialog |
| 音频焦点等噪音 | 降级为 WARN/仅 debug 是否影响用户可见提示 | 需确认这些行为是否仍会被用户看到 |

## 9. 参考定位

- 计划目的、边界、状态写法对齐 `优化预处理计划书.md` / `维护计划书-后续修复与增强.md`。
- 涉及文件集中在 `app/src/main/java/io/legado/app/{constant,utils,help,ui/about}`。

## 10. P0 执行记录（2026-09-07）

- 已完成：`AppLog.logs` getter 加 `synchronized`，与 `put` / `putNotSave` / `clear` 使用同一对象锁，避免并发复制 `mLogs`。
- 已完成：两处 DEBUG 调用栈 tag 改为安全取第 4 帧；浅栈时回退到 `AppLog` 类名，不再存在数组越界。
- 已完成：崩溃日志写入备份路径失败时通过 `LogUtils.e` 记录异常类型与消息，不再静默吞掉。
- 已新增：`AppLogSafetyTest`，锁定 getter 同步修饰符、正常调用栈取值和浅栈回退行为。
- 已验证：`:app:compileAppDebugKotlin` 与全量 `:app:testAppDebugUnitTest` 通过。
- 待真机：当前 `adb devices` 无连接设备，尚未执行 App 内多线程 `AppLog.put` 现场触发。
- 本轮不做：跨天滚动在 P0 定义中为可选项，留待独立设计与测试，避免扩大本轮正确性修复范围。

## 11. P1 执行记录（2026-09-07）

- 已完成：新增 `AppLog.i/w/e`、懒加载 `i(tag) { ... }` 与仅提示不记录的 `AppLog.toast`。
- 已完成：新增 `AppLogLevel` / `AppLogEntry`，内存缓冲保留级别、tag、消息和 throwable；旧 `logs` 三元组视图继续供现有查看器使用。
- 已完成：新增集中 `LogTag`，覆盖 App、崩溃、设备、LiveEventBus、监控、网络、JS、TTS、WebDAV、导入、漫画、书架、目录和 AI。
- 已完成：`LogUtils` 收为 app 模块内部落盘后端，不再直接读取 `AppConfig.recordLog`；初始化和开关更新统一由 `AppLog` 传入状态。
- 已完成：文件格式显式输出稳定的 `[INFO]` / `[WARN]` / `[ERROR]`，为 P2 文件筛选提供可解析级别。
- 已完成：应用启动、两处 recordLog 设置监听、LiveEventBus 与崩溃备份失败路径接入新门面。
- 已兼容：`put` / `putNotSave` / `putDebug` 保留原签名和行为并标记 deprecated；`put` 在 recordLog 关闭时仍保留内存与 toast 行为，但不落盘。
- 已新增：`AppLogFacadeTest` 与 `LogFormatTest`，验证级别映射、结构化字段、集中 tag 约束和落盘格式；P0 测试继续保留。
- 已验证：`:app:compileAppDebugKotlin` 与全量 `:app:testAppDebugUnitTest` 通过，共 229 个测试、0 失败。
- 迁移债务：仍有 15 个 app 内部文件直接调用 `LogUtils.d/e`；后端已不可作为模块外 API，这些内部调用按 P3 模块批次迁移，避免本阶段批量改业务文件。
- 待真机：当前无可用 ADB 设备，尚未核对日志开关、内存条目和文件级别的可见行为。

## 12. P2 执行记录（2026-09-08）

- 已完成：`AppLogDialog` 改为独立日志查看器，支持「内存 / 文件」来源切换；文件视图在 IO 线程读取 `externalCacheDir/logs/appLog-*.txt`，按文件新旧和条目时间倒序展示。
- 已完成：文件读取限制为单文件末尾最多 2,000 行 / 1 MiB、总计最多 500 条，避免在 UI 中一次加载完整大日志；异步读取增加 generation 校验，切换来源后迟到结果不会覆盖当前视图。
- 已完成：兼容 P1 的 `[INFO] / [WARN] / [ERROR]` 新格式与旧格式，堆栈续行归并到所属条目；支持级别、tag、关键词组合过滤。
- 已完成：「复制当前筛选结果」「导出全部日志」「清空内存日志」入口；导出复用关于页的统一 `LogExport`，清空只影响内存缓冲，不删除历史文件。
- 已完成：关闭 `recordLog` 时仍可查看已有文件，并提示开启「记录日志」以采集新内容；未新增数据库、权限或网络依赖。
- 已新增：`AppLogViewDataTest` 4 项，覆盖新旧格式解析、堆栈续行、组合过滤、尾部读取上限与复制文本内容。
- 已验证：最终候选执行 `:app:compileAppDebugKotlin :app:testAppDebugUnitTest :app:lintAppDebug :app:assembleAppRelease`，`BUILD SUCCESSFUL in 10m 59s`；共 36 个测试套件、233 项测试、0 failures / errors / skipped，P2 新代码无 lint 告警，报告仅保留既有 `item_app_log.xml` overdraw 提示。
- 已产物：`app/build/outputs/apk/app/release/legado_app_3.26.090808.apk`（19,963,884 bytes，SHA-256 `E1E01DAC4DACD9228E41BEC979F0AC0F599DEF4169069EF7BB22C6E9792D915C`）。该本地产物只证明可构建，不等同于真机验收或 GitHub Release。
- 真机验收：目标设备 `10CE5P1M1Z001P9`，共存包 `io.legado.app.tjf.releaseS` / `3.26.090809` 以 `install -r` 成功安装，未卸载、未清数据、未覆盖正式包 `io.legado.app.releaseA`。测试 APK 为 `app/build/reports/log-p2/legado_log_p2_releaseS.apk`，SHA-256 `43E2314100CDD7D27C643BF54A575E980FA44C282EC6256926C9D6AC36502316`；临时 `.releaseS` 与权限名构建改动已精确恢复。
- 真机通过：查看器正常打开；内存 / 文件来源可自由切换；开启 `recordLog` 后生成 `appLog-26-09-08_09-15-25.787.txt` 并在文件视图显示当天 `[INFO]` / tag / 消息；级别、tag、关键词组合筛选的正向命中和无结果反例正确。
- 真机通过：将仅含 `LifecycleHelp` 的受控筛选结果复制后粘贴回输入框，得到 1,473 字符且格式正确；执行“清空内存日志”后内存条目为 0，文件视图仍保留 7 条可见记录。测试结束后 `recordLog` 已恢复为原关闭状态。
- 真机通过：经用户明确授权，将该共存包现有 logs/crash/logcat 导出到用户设置的 `/storage/emulated/0/Download/yuedubackup/logs.zip`；文件于 2026-09-08 09:40 生成，大小 26,734 bytes，归档清单含 12 个日志相关文件与 `logcat.txt`，未上传网络。
- 发布判定：P0/P1/P2 验收完成，可进入提交与发布审计；但当前工作树同时包含 AI 正文净化阶段 E 的 `review / NOT READY` 未提交改动，发布前必须明确并隔离提交范围，不得把日志验收等同于整个工作树可发布。
- 提交记录：日志 P0/P1/P2 代码与测试已独立提交为 `46b326680 feat(logging): complete structured log viewer`；共享文件中的 AI 未验收 hunks 未进入该提交。
- 后续范围：P3（300+ 调用点内容质量迁移）与 P4（性能/监控）仍是独立候选，不作为 P2 收尾或本次发布的隐含范围。
