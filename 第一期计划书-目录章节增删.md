# 第一期计划书：目录「新增/删减章节」功能

> 开发状态：**已完成首版实现**（数据层 → 核心逻辑 → UI，编译与单元测试通过），待真机回归（见第 8 节测试计划）。

## 1. 背景与目标

### 背景

- 部分本地 TXT 书籍目录不完整、章节粘连或冗余，用户需要手动调整章节结构。
- 粘连类问题根源在书源/抓取层（一个 URL 对应多章），规则层无法根治，需要阅读器侧提供章节级编辑能力（拆分为第二期）。
- 现有能力盘点：
  - 正文编辑（改标题、改正文）已有，见 `ContentEditDialog`；
  - 目录重排已有先例（“反转目录”全量重排 `index`）；
  - 但**没有**“插入/删除章节并级联重排”的能力。

### 目标（第一期）

在目录页为**本地 TXT 书籍**提供两个操作：

1. **在此章后新增章节**：如在第 100 章后新增，自动命名为“第 101 章”，后续章节序号与标题数字自动 +1；
2. **删除此章节**：后续章节序号与标题数字自动 −1。

操作后同步维护：章节正文覆盖文件、书签、阅读进度与书籍元数据。

## 2. 范围

### 本期包含

- 本地 TXT 书籍（`Book.isLocalTxt`）目录页的新增/删减章节；
- 后续章节的 `index` 级联重排与标题数字重写；
- 正文覆盖文件改名、书签同步、阅读进度与 `Book` 元数据同步；
- 对应 UI（长按菜单 + 对话框）与文案。

### 本期不包含（后续期）

- 按标题正则**批量拆分**粘连章节（一期逻辑是它的基础）；
- 多选**合并**冗余章节；
- 中文数字标题（“第一百章”）重写；
- EPUB / MOBI / UMD / PDF 的支持；
- 在线书籍（离线缓存态）的章节编辑。

## 3. 现状与依据

| 关注点 | 现状 | 位置 |
| --- | --- | --- |
| 章节序号 | `BookChapter.index` 为排序字段，`(bookUrl, index)` 唯一索引，主键 `(url, bookUrl)` | `data/entities/BookChapter.kt` |
| 重排先例 | `reverseToc`：全量取出 → 重设 index → `insert(REPLACE)` | `ui/book/toc/TocViewModel.kt:55` |
| 本地 TXT 正文 | 默认按原文件字节偏移 `start/end` 读取；**覆盖文件优先**（`downloadDir/.../%05d-标题MD5.nb`） | `model/localBook/TextFile.kt`、`help/book/BookHelp.kt` |
| 覆盖文件命名 | `String.format("%05d-%s.nb", index, titleMD5)` | `BookChapter.getFileName()` |
| 覆盖文件读写 | `BookHelp.saveText / delContent / getContent` 已具备 | `help/book/BookHelp.kt:176 / 426` |
| 书签 | 按 `chapterIndex + chapterName` 定位章节 | `data/entities/Bookmark.kt:19` |
| 阅读器同步 | `ReadBook.onChapterListUpdated(book)` 刷新章节数与当前章 | `model/ReadBook.kt:976` |
| 目录 UI | 目录项长按目前仅 toast 标题 | `ui/book/toc/ChapterListAdapter.kt:183` |
| 正文编辑先例 | 阅读页正文编辑对话框可改标题与正文并保存覆盖文件 | `ui/book/read/ContentEditDialog.kt` |

## 4. 功能设计

### 4.1 交互流程

**入口**：目录页长按章节条目 → 弹出菜单：

- 在此章后新增章节
- 删除此章节

入口仅对本地 TXT 书籍显示（复用 `isLocalTxt` 判断，与目录菜单 `menu_group_text` 一致）。

**新增对话框**：

- 标题输入框：默认按选中章节的标题模式自动生成（选中“第100章 xxx” → 预填“第101章”）；
- 正文输入框（多行，可留空）；
- 提示文案：正文为空将写入占位内容，可后续在阅读页“正文编辑”中修改；
- 确认后执行，成功后目录自动刷新并 toast 结果。

**删除对话框**：

- 展示章节标题与警示（已编辑正文将被删除，后续章节序号将自动前移，不可恢复）；
- 确认后执行，成功后目录自动刷新。

### 4.2 新增章节（insert）

入参：`book`、锚点章节 A（`index = i`）、标题 `title`、正文 `content`。

1. 构造新章节：`index = i + 1`，`url` 使用本地唯一标识生成（本地 TXT 不依赖 url 取正文），`start/end = null`，`isVolume = false`；
2. **从后往前**（避免唯一索引中间态冲突）将 `index >= i + 1` 的章节 `index + 1`，每章同步：
   - 标题数字重写 +1（见 4.4）；
   - 正文覆盖文件旧名 → 新名（`index` 与 `titleMD5` 均可能变化）；
   - 书签 `chapterIndex` 与 `chapterName` 同步（见 4.6）；
3. 写入新章节的正文覆盖文件（`content` 为空则写占位文本，保证不落入文件偏移读取路径）；
4. 更新 `Book`：`totalChapterNum + 1`；若 `durChapterIndex > i` 则 `+1`（保持当前阅读内容不变），同步 `durChapterTitle`；
5. 调用 `ReadBook.onChapterListUpdated(book)` 通知阅读器。

### 4.3 删除章节（delete）

入参：`book`、目标章节 D（`index = i`）。

1. `BookHelp.delContent` 清理 D 的正文覆盖文件；
2. 删除 D 的书签（`chapterIndex == i`）；
3. **从前往后**将 `index > i` 的章节 `index - 1`，每章同步：
   - 标题数字重写 −1；
   - 正文覆盖文件改名；
   - 书签同步；
4. 更新 `Book`：`totalChapterNum - 1`；`durChapterIndex > i` 则 `-1`；`durChapterIndex == i` 则指向原下一章（新 `index = i`）且 `durChapterPos` 归零；
5. 调用 `ReadBook.onChapterListUpdated(book)`。

### 4.4 标题数字重写规则

首版支持的匹配模式：

- `第(\d+)章`
- `第(\d+)回`
- `第(\d+)节`
- `Chapter\s*(\d+)`（忽略大小写）

规则：

- 命中模式 → 仅重写数字段，前后缀与副标题保留（“第100章 风雨” → “第101章 风雨”）；
- 未命中 → 只重排 `index`，**不改标题**（不误伤“序章”“番外”等自定义标题）；
- 数字边界：正常整数增减（999 → 1000）；
- 首版不做设置项，默认开启；中文数字重写留待二期。

### 4.5 正文覆盖文件改名

- 对每个受影响章节按 `getFileName()` 计算旧名/新名并 `renameTo`；
- 改名失败：仅日志告警，**不阻断数据库操作**；后果是该章回退读取原文件正文（本地 TXT 场景不致命，数据仍在）。

### 4.6 书签同步

- 新增：受影响章节书签 `chapterIndex + 1`，`chapterName` 同步为新的章节标题；
- 删除：受影响章节书签 `chapterIndex - 1`；被删章节的书签直接删除；
- 同步条件：`bookName + bookAuthor` 匹配当前书，`chapterIndex` 匹配旧序号。

## 5. 技术方案

### 5.1 数据层改动

`BookChapterDao` 新增：

```kotlin
@Query("delete from chapters where bookUrl = :bookUrl and url = :url")
fun delChapter(bookUrl: String, url: String)

@Query("update chapters set `index` = :index, title = :title where bookUrl = :bookUrl and url = :url")
fun upIndexTitle(bookUrl: String, url: String, index: Int, title: String)
```

`BookmarkDao` 新增：

```kotlin
@Query("update bookmarks set chapterIndex = :newIndex, chapterName = :newTitle " +
        "where bookName = :bookName and bookAuthor = :bookAuthor and chapterIndex = :oldIndex")
fun shiftChapter(bookName: String, bookAuthor: String, oldIndex: Int, newIndex: Int, newTitle: String)

@Query("delete from bookmarks where bookName = :bookName and bookAuthor = :bookAuthor and chapterIndex = :index")
fun delByChapterIndex(bookName: String, bookAuthor: String, index: Int)
```

事务性：优先将新增/删除主流程封装为 Room `@Transaction`（DAO 改造为 abstract class 或在 default 方法中聚合）；若改造代价大，则退化为 `execute` 单线程顺序执行 + 内存备份（受影响章节列表），失败时逆操作回滚。

### 5.2 业务逻辑

`TocViewModel` 新增：

```kotlin
fun insertChapter(book: Book, anchor: BookChapter, title: String, content: String)
fun deleteChapter(book: Book, chapter: BookChapter)
```

内部实现 4.2 / 4.3 全流程（IO 线程），完成后刷新目录列表。

新增工具：

- `ChapterNumberUtils.rewriteTitle(title: String, delta: Int): String?`（命中返回重写后标题，未命中返回 null）；
- 覆盖文件改名辅助函数（入参 book、chapter、旧 index/title、新 index/title）。

### 5.3 UI 层

- `ChapterListAdapter`：长按回调从 toast 改为 `onChapterMenu(bookChapter)`；
- `ChapterListFragment`：弹出操作菜单（新增/删除），校验 `isLocalTxt`；
- 新增对话框布局：标题输入 + 正文输入（可复用/仿 `DialogContentEditBinding`）；
- 操作完成后：`upChapterList(searchKey)` 刷新 + toast。

### 5.4 预计改动文件

| 文件 | 改动 |
| --- | --- |
| `data/dao/BookChapterDao.kt` | +2 查询方法 |
| `data/dao/BookmarkDao.kt` | +2 查询方法 |
| `ui/book/toc/TocViewModel.kt` | +2 主流程 |
| `ui/book/toc/ChapterListAdapter.kt` | 长按回调 |
| `ui/book/toc/ChapterListFragment.kt` | 菜单与对话框 |
| 新增 `ChapterNumberUtils.kt` | 标题重写工具 |
| 新增对话框布局 / `strings.xml` | 文案与界面 |

## 6. 边界与限制（产品层面明确）

1. 入口仅本地 TXT 显示；在线书（含离线缓存）不提供入口，产品提示“导出文件 → 导入本地”后可用；
2. 修改在“重新解析本地文件 / 重新导入”后会被重置 —— 与现有正文编辑行为一致，帮助文案说明；
3. 标题数字重写只覆盖常见编号模式，无编号章节只重排 `index`；
4. 新增章节的正文需要本地覆盖文件支撑，空正文以占位文本兜底；
5. EPUB 等格式不显示入口。

## 7. 风险与规避

| 风险 | 影响 | 规避 |
| --- | --- | --- |
| 唯一索引冲突 | 操作失败或数据错乱 | 新增时从后往前 +1；DB 变更事务化或顺序执行 + 备份回滚 |
| 标题误重写 | 用户标题被改坏 | 仅对明确编号模式重写；新增对话框预填可见 |
| 覆盖文件改名失败 | 该章正文回退原文件 | 不阻断 DB，日志告警（本地 TXT 数据仍在） |
| 书签错位 | 书签挂到错误章节 | 按 `chapterIndex` 批量同步；删除章节时清除其书签 |
| 阅读进度错位 | 跳章/重读 | 按 4.2/4.3 规则调整 `durChapterIndex/Pos` 并通知阅读器 |
| 空正文章节读取崩溃 | 进入 `start!!` 空指针路径 | 新章节必须存在覆盖文件（占位文本兜底） |

## 7.1 稳定性保障策略

> 目标：**不因新增/修改导致原有功能崩溃**。以下为硬性约束，开发与评审均按此执行。

1. **零侵入原则**：新功能只增加“长按菜单”入口，不改动现有路径的行为（目录点击、反转目录、目录规则、正文编辑、缓存下载等一律保持原样）；若某处必须改动，只允许“新增调用点”，不允许改写原有逻辑。
2. **不迁移数据库**：本期不改表结构、不加列、不改约束，只新增只读/定向查询方法，**无 Room 迁移风险**；数据库变更全部包裹在事务中。
3. **独立失败域**：新逻辑全部在 IO 线程执行并捕获异常，失败仅 toast + 日志，绝不向 UI 抛异常；新功能整体失败时，原有功能不受影响（文件改名失败仅告警，不阻断 DB）。
4. **事务与回滚**：DB 变更事务化；执行前把受影响章节/书签备份到内存，失败时逆操作恢复；重排从后往前进行，避免唯一索引中间态冲突。
5. **删除安全**：删除章节需二次确认；删除前备份元数据用于回滚；正文覆盖文件删除只针对 `getFileName()` 精确命中的文件。
6. **编译门槛**：每一步改动后必须编译通过再继续，未编译验证的代码不入库：

   ```powershell
   .\gradlew.bat :app:compileLegadoDebugKotlin
   ```

7. **小步提交**：按“数据层 → 核心逻辑 → UI”三步分提交，每步可独立 `git revert`，避免大改动集中出问题后难以定位。
8. **回归测试**：第 8 节测试清单在交付前全量执行，重点回归：反转目录、目录规则、正文编辑、离线缓存、阅读进度。

## 8. 测试计划

1. 基本新增：第 100 章后新增 → 新章为 101，后续标题数字 +1、覆盖文件改名后内容一致；
2. 基本删除：删除第 100 章 → 后续 −1；
3. 边界：第 1 章前新增；最后一章后新增；删除最后一章；删除唯一章节（拦截确认）；
4. 无编号标题：只重排不重写；
5. 已编辑正文的章节重排后内容不丢；
6. 书签：插入/删除后书签归属正确，删除章节时其书签被清；
7. 阅读进度：操作后当前阅读内容不跳错；
8. 空正文新增：占位内容可读，阅读页正文编辑可修改；
9. 连续快速操作不产生重复 `index`；
10. 回归：反转目录、目录规则、正文编辑不受影响；
11. 重新解析本地文件后修改重置（预期行为确认）。

## 9. 验收标准

- [ ] 本地 TXT 目录长按出现“在此章后新增 / 删除此章节”；
- [ ] 新增后目录、标题、正文、书签、进度全部正确（测试 1、5、6、7）；
- [ ] 删除后同上（测试 2）；
- [ ] 在线书 / EPUB 等不显示入口；
- [ ] 空正文章节可正常打开与编辑；
- [ ] 回归项通过（测试 10）。

## 10. 工作量与排期

| 阶段 | 内容 | 估时 |
| --- | --- | --- |
| 数据层 | DAO 方法与事务封装 | 0.5 天 |
| 核心逻辑 | 插入/删除/重排/改名/书签/进度同步 | 1 天 |
| UI | 长按菜单 + 对话框 + 刷新 | 0.5 天 |
| 自测与修复 | 边界与联动、回归 | 0.5~1 天 |
| 合计 | | **2.5~3 天** |

> 说明：以上按“由熟悉 Kotlin 的开发人员实施”估算。若由不熟悉 Kotlin 的人边学边做，建议按 **5~7 天** 排期，并把第 6 节编译门槛作为每个步骤之间的强制检查点。

## 11. 待确认项

> 已确认（2026-08-10，均按推荐项执行）：

1. 新增章节正文为空时：**写入占位文本**（不要求必填）；
2. 删除的是当前阅读章节时：**定位到原下一章**（原上一章仅作备用考虑）；
3. 标题数字重写：**首版默认开启，不提供设置开关**；
4. 计划书确认：**按此排期进入开发**（2.5~3 天，数据层 → 核心逻辑 → UI 分步提交）。

## 12. 二期预留（不在本期）

- 按标题正则批量拆分粘连章节（复用本期插入/重排/覆盖文件机制）；
- 多选合并冗余章节；
- 中文数字标题重写；
- 在线书“转本地”操作引导。

## 附录 A：Kotlin 快速适配指南

> 面向熟悉 C/C++、SQL、Linux，但对 Kotlin 陌生的人。只覆盖本项目需要理解的部分。

### A.1 项目结构与构建

- 语言：Kotlin，构建：Gradle；主要业务代码在 `app/src/main/java/io/legado/app/`；
- 编译验证（Windows PowerShell）：

  ```powershell
  .\gradlew.bat :app:compileLegadoDebugKotlin
  ```

- 建议工作流：先精读三处样板代码再动手 ——
  `TocViewModel.reverseToc`（重排先例）、`ContentEditDialog`（编辑先例）、`TextFile.kt`（本地 TXT 解析）。

### A.2 本项目高频 Kotlin 概念对照

| Kotlin 概念 | 类比 C++ / 备注 | 本项目示例 |
| --- | --- | --- |
| `class` / `data class` | 类似 C++ struct，字段即属性，自带 getter/setter | `BookChapter.kt` |
| 扩展函数 `fun Book.isLocalTxt` | 类似给已有类追加自由函数 | `BookExtensions.kt` |
| 空安全 `?` `?:` `!!` | 可空类型/兜底/强制非空；**新代码尽量避免 `!!`** | 全项目常见 |
| 协程 `lifecycleScope.launch { }`、`withContext(IO) { }` | 轻量异步，替代裸线程/回调 | `ChapterListFragment.kt` |
| `execute { }.onSuccess { }.onError { }` | 项目封装的后台任务 + 结果回调，新逻辑统一走它 | `TocViewModel.kt` |
| Room `@Dao` 接口 + `@Query("SQL")` | 注解式 SQL，方法即 SQL 语句 | `BookChapterDao.kt` |
| `LiveData` + `observe` | 数据变化自动刷新 UI，类似“观察者模式” | `TocViewModel.bookData` |
| viewBinding（`XxxBinding.inflate`） | XML 布局自动生成的类型安全绑定，替代 findViewById | 各 Activity/Fragment |
| `Array`/`List` 与 `forEachIndexed` | 集合遍历；与 C++ vector/range-for 类似 | `reverseToc` |

### A.3 新代码编写约定（贴合本项目风格）

1. 数据库操作放 IO 线程（`execute` 或 `withContext(IO)`）；
2. 所有可能抛异常的新逻辑用 `runCatching` / `onError` 兜底，不静默失败（toast + `AppLog.put`）；
3. 字符串格式化用 `String.format` / 模板 `"${...}"`，与现有代码一致；
4. 方法命名沿用项目习惯：`upXxx`（刷新）、`getXxx`（查询）、`saveXxx`（持久化）；
5. 改动后立即编译，编译通过再写下一个步骤。

## 13. 后期复盘与状态（2026-08-12）

### 已落地

- 首版（DAO、核心逻辑、UI、单测）完成；
- 三期演进：插入/删除数据库操作**事务化**（`BookChapterDao.applyTocEdit` @Transaction，提交 a66316548）；**中文数字标题重写**（`ChineseNumberUtils`，提交 c8da1ff00）；**批量拆分粘连章节**（`ChapterSplitter` + `splitChapter`，提交 73964bece / a4e32a6bb 及两轮修复）。

### 本期预留项去向

- 按标题正则批量拆分 → **三期已实现**；
- 中文数字标题重写 → **三期已实现**；
- 多选合并冗余章节 → **四期**（复用三期抽取的 `batchReindex`/`applyTocEdit` 机制）；
- 在线书“转本地”操作引导 → **四期**。

### 遗留 / 标记

- 新增章节空白默认标题 off-by-one（`anchor.index + 2`）：三期仅记录不修改（对应第三期计划书 4.0.1），**四期评估**是否修复（需先确认预期编号约定，属旧功能行为变更）；
- 真机回归未执行。
