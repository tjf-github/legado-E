# 构建优化书：Gradle 构建性能与工程化（方案稿）

> 状态：**计划书草稿（2026-08-14），待确认后分阶段执行，未做任何代码改动**
> 定位：针对构建速度、可复现性、CI 回归、仓库卫生的优化方案；不涉及功能开发
> 前置：项目评估已完成（见《项目评估报告》）；本文件承接评估中「构建/工程化」相关短板
> 原则：每项独立分支落地、可单独回退；优化前后以 `--profile` 数据对比，避免凭感觉优化

## 1. 背景与目标

### 背景

- 项目为 873 个 Kotlin 文件的 Android 单体应用（`:app`）＋ 3 个子模块（`:modules:book`、`:modules:rhino`、`:modules:web`）；
- 当前 `org.gradle.unsafe.configuration-cache=false`，且版本号在**配置阶段**用 `new Date()` 计算，配置缓存被关闭、构建不可复现；
- CI（`.github/workflows/test.yml`）只做 `assemble`，不跑 `testAppDebugUnitTest`/`lint`，61 个 JVM 单测无自动化回归保护；
- 仓库存在死二进制（`modules/rhino/lib/rhino-1.7.14.jar`）、已移除但未清理的 Firebase 配置等卫生问题。

### 目标

1. **构建提速**：开启配置缓存、并行、本地构建缓存、Kotlin classpath snapshot；
2. **可复现**：版本号外部化，消灭配置阶段非确定性；
3. **CI 回归**：单测/lint 接入门禁，构建失败即时可见；
4. **仓库瘦身**：清理死二进制与死配置；
5. **量化**：每项优化前后有 `--profile`/`--scan` 数据对比。

## 2. 现状诊断（已核实）

| # | 现状 | 影响 |
| --- | --- | --- |
| D1 | `org.gradle.unsafe.configuration-cache=false`，注释「enable cache can not up app version」 | 配置缓存关闭，每次构建重跑配置阶段 |
| D2 | 版本号 `def now = new Date()` 在**配置阶段**计算（`app/build.gradle` 顶部） | 配置非确定性 → 阻配置缓存、构建不可复现、本地/CI 版本漂移 |
| D3 | `org.gradle.parallel=true` 被注释 | 多模块无法并行 |
| D4 | 本地 `org.gradle.caching=true` 未开（仅 CI 用 `--build-cache`） | 本地不享跨会话构建缓存 |
| D5 | `kotlin.incremental.useClasspathSnapshot=true` 被注释 | classpath ABI 兼容变更易触发全量重编译 |
| D6 | 873 个 Kotlin 文件集中在 `:app` 单模块 | 增量编译粒度大、编译内存峰值高 |
| D7 | `modules/rhino/lib/rhino-1.7.14.jar` 已死（`build.gradle` 改用 `api libs.mozilla.rhino`=1.8.1，旧 jar 行被注释） | 无用二进制仍被跟踪，repo 体积与混淆 |
| D8 | Cronet 自管：5 个 jar 入库（约 1.8MB）；`.so` 为动态库，**运行时按需下载**（`CronetLoader` 从 Google CDN 下载，md5 见 `assets/cronet.json`） | 体积可接受；维持现状即可 |
| D9 | `test.yml` 只 `assemble`，不跑单测/lint/仪器测试 | 61 单测无自动化回归保护 |
| D10 | `test.yml` build 步骤 `continue-on-error: true` | 构建失败被吞，仅靠「Check production」补救 |
| D11 | Firebase 已从依赖移除（提交 `94eb65b2a`），但 `app/google-services.json`、catalog 的 firebase 条目、google-services 插件别名仍在 | 死配置残留 |
| D12 | 个人代理/镜像靠 skip-worktree 隔离（`git ls-files -v` 显示 `S`） | 有效，但个人代理写进工作区有误提交风险 |

## 3. 优化目标（KPI，需先测基线校准）

| 指标 | 现状 | 目标 |
| --- | --- | --- |
| 配置阶段耗时 | 未测（配置缓存关闭，每次全量） | 开启后 < 2s |
| 无改动增量编译 | 未测 | < 10s |
| 干净构建 `assembleDebug` | 未测 | 减少 30–50% |
| CI 回归 | 无单测 | `testAppDebugUnitTest` 强制通过 |
| 仓库体积 | 含死 rhino jar（`.so` 未入库，运行时下载） | 删除死二进制与死配置 |
| 可复现性 | 时间戳版本号 | 版本号由外部注入，可复现 |

> **第 0 步必须先做：建立基线。**
> ```bash
> ./gradlew clean assembleDebug --profile --console=plain   # 干净构建基线
> ./gradlew assembleDebug --profile                          # 无改动增量基线
> ```
> 报告位于 `build/reports/profile/`；有条件加 `--scan` 上传 Develocity 做阶段级耗时分析。

## 4. 优化项清单

### 4.1 开启配置缓存（Configuration Cache）—— 价值最高，需先解 D2

**根因**：`app/build.gradle` 在配置阶段用 `new Date()` 算版本号，配置缓存会「冻结」首次算出的版本，导致版本不再更新（即 `gradle.properties` 中注释的原因）。

**方案 A（推荐）—— 版本号外部化，确定性**：

```groovy
// app/build.gradle 顶部，替换 new Date() 计算
val versionName = (project.findProperty("VERSION") as String?) ?: "3.0.0-dev"
val versionCode = (project.findProperty("VERSION_CODE") as String?)?.toInt() ?: 1
```

CI 改为传参注入（替换现有 `sed` 方案，更干净）：

```bash
./gradlew assembleAppRelease -PVERSION="$VERSION" -PVERSION_CODE="$CODE"
```

`test.yml` 里现有的 `sed "/def version/c ..."` 同步删除。

**方案 B（保留自动时间戳，但移到任务内）**：把版本计算放进 `provider`/自定义任务，使其在**执行阶段**求值，配置阶段只留静态占位。

**灰度开关**：

```properties
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn   # 先告警不失败
```

需逐个验证 `app/download.gradle` 的自定义 `Download` task、`downloadCronet` 的 `doLast` 是否兼容；不兼容的 task 临时标 `notCompatibleWithConfigurationCache()`。

**预期收益**：配置阶段 20–80% 耗时下降；无改动构建几乎秒开。这是本项目单点收益最大的一项。

### 4.2 打开并行 + 本地构建缓存

```properties
org.gradle.parallel=true
org.gradle.caching=true
```

- 并行对当前单主模块收益有限，但与 §4.6 模块拆分**配套**后收益放大；
- 本地缓存打开后，跨会话复用 R8/编译产物，第二次构建显著变快。

### 4.3 Kotlin 增量优化

> 已核实（2026-08-14）：`kotlin.incremental.useClasspathSnapshot` 在 Kotlin 2.3.10 **已废弃**——Kotlin 2.x 默认启用 classpath snapshot，无需显式设置；显式设置反而产生 deprecation 警告。本项**无需改动**。

### 4.4 死二进制与死配置清理（低风险、立即见效）

| 项 | 动作 |
| --- | --- |
| `modules/rhino/lib/rhino-1.7.14.jar` | **删除**（已被 Maven `org.mozilla:rhino:1.8.1` 替代，引用已注释） |
| `app/google-services.json` | **删除**（Firebase 已移除，见 D11） |
| `gradle/libs.versions.toml` 的 `firebase-bom/firebase-analytics/firebase-perf` | 删除 |
| 根 `build.gradle` 的 `alias libs.plugins.google.services apply false` | 删除 |
| 各 build 文件中大量被注释的旧配置块 | 清理（保留「为何锁定版本」注释，那是有效文档） |

**预期收益**：repo 体积下降、消除「哪个 rhino 在用」的混淆、缩短依赖解析。

### 4.5 Cronet 自管方式（已查明，维持现状，无需改动）

Cronet 自管（含 `DnsHttpsSvcb` 支持）是有意为之，**不建议贸然迁 Maven**（`org.chromium.net:cronet-embedded` 需验证特性与 proguard）。

> 预处理二次修正（2026-08-14）：`.so` 从未入库，且进一步查明它是**运行时按需下载的动态库**——`CronetLoader` 按 `assets/cronet.json` 的 md5 从 Google CDN 下载到应用私有目录后 `System.load` 加载；`download.gradle` 的 4 个 so task 是「下载 `.so` → 算 md5 → 写 `cronet.json`」的清单生成器，非死代码。

**结论：Cronet 本项无需改动**（5 个 jar 入库约 1.8MB + `cronet.json` md5 清单 + 运行时下载 `.so`）。可选评估：

1. jar 转远程依赖（可选，非必须）：5 个 jar 体积可控，暂保留；
2. `cronet.yml` 的 `add-paths` 已精确匹配 `*cronet*jar`，流程较规范，保留。

### 4.6 模块拆分（中长期，收益最大但风险最高）

**现状**：`:app` 是巨石（873 文件），`ui/book/manga`、`ui/book/read`、`ui/book/audio`、`ui/book/video` 天然可拆。

**渐进路线**（先叶子、后核心，每次一个独立 PR）：

1. 先抽纯逻辑 `:core`/`:base`（utils、model、help），无 UI 依赖，最安全；
2. 再抽 `:feature:manga`、`:feature:audio`、`:feature:video` 等 feature 库（参照现有 `:modules:book` 范例）；
3. `:app` 只留壳与装配。

**收益**：并行编译、增量编译单元缩小、内存峰值下降、`org.gradle.parallel` 生效。
**风险**：Resource/Manifest/命名空间移动、循环依赖。**必须先抽 `:core` 打破依赖再拆 feature**，切勿一次性大爆炸拆分。

### 4.7 CI 优化（D9/D10）

1. **加测试门禁**（最关键）：
   ```bash
   ./gradlew testAppDebugUnitTest lintDebug assembleAppRelease
   ```
2. **移除 build 步骤的 `continue-on-error: true`**（保留上传 missing_rules 的 `ignore`），让失败即时可见；
3. 拆分 job：`build` 与 `test` 分开，失败定位更快、可并行；
4. `gradle/actions/setup-gradle@v5` 已启用远程缓存，保留；建议加 `dependency-graph: generate-and-submit` 做依赖可视化；
5. 加 Develocity/`--scan` 输出，CI 每次构建留构建耗时报告。

### 4.8 仓库卫生（D12）

- 个人代理 `127.0.0.1:7897` 移到**用户级** `~/.gradle/gradle.properties`，仓库级只保留通用配置，消除误提交风险；
- `settings.gradle` 阿里云镜像：**维持现状**——HEAD 已预留镜像注释且标注「不要提交修改」，本地经 skip-worktree 启用镜像，符合上游约定，无需改动。

## 5. 分阶段路线图

| 阶段 | 内容 | 风险 | 预期收益 |
| --- | --- | --- | --- |
| **P0（立即）** | 建基线（`--scan`/`--profile`）；§4.4 死二进制清理；§4.7 CI 加单测门禁、去 `continue-on-error` | 极低 | 立竿见影，且建立可量化基线 |
| **P1（短期）** | §4.1 版本号外部化 + 配置缓存灰度；§4.2 并行/缓存；§4.3 classpath snapshot | 中（配置缓存需灰度验证） | 配置阶段大幅提速 |
| **P2（中期）** | §4.8 仓库卫生（Cronet 维持现状） | 中 | 体积下降 |
| **P3（长期）** | §4.6 模块拆分 | 高（分 PR 渐进） | 增量编译与并行收益最大化 |

## 6. 验收标准

- [ ] 每项独立分支落地，可单独回退；
- [ ] 优化后 `./gradlew assembleDebug` 与 `testAppDebugUnitTest`（61 单测）全绿；
- [ ] CI 出现单测 job，且失败能阻断合并；
- [ ] 配置缓存开启后，无改动构建配置阶段 < 2s（以基线对比为准）；
- [ ] `git ls-files` 不再含死 rhino jar / `google-services.json`。

## 7. 风险与回退

| 风险 | 影响 | 规避 |
| --- | --- | --- |
| 配置缓存与某插件不兼容 | 构建失败 | 保持 `configuration-cache.problems=warn` 灰度；单独任务标 `notCompatibleWithConfigurationCache()`；紧急时 `--no-configuration-cache` |
| 版本号改外部注入后 CI 更新检测/发布版本错位 | 发布错误版本 | 先核对 `test.yml` 的 `sed` 与版本输出逻辑，同步改 `-P` 注入；灰度一次 release 验证 |
| 模块拆分引发资源/循环依赖 | 编译失败 | 先抽 `:core`，每步 `assembleDebug` 全绿再继续；保留 `git revert` 单 PR 粒度 |
| 死二进制删除后仍有隐式引用 | 编译失败 | 删除前 `grep -r` 确认无引用（rhino jar 引用已注释，Firebase 依赖已移除） |

## 8. 待确认项

1. 版本号方案：选 **A（外部注入，可复现，推荐）** 还是 **B（保留时间戳，移到任务内）**？
2. ~~Cronet `.so` 出库~~（已查明：`.so` 运行时下载、未入库，维持现状即可，无需决策）
3. CI 单测门禁：`testAppDebugUnitTest` 直接加进 `test.yml` 主链路，还是先独立 job 灰度？
4. 模块拆分：是否立项 P3？还是只做 P0~P2（构建提速已覆盖大部分痛点）？
5. 优化顺序：按 P0 → P1 → P2 → P3 推进，还是先做单点收益最大的 §4.1？

## 9. 决策记录

- **P0 已执行完成（2026-08-14，改动未提交）**：
  1. 基线重建：`compileAppDebugKotlin + testAppDebugUnitTest` → BUILD SUCCESSFUL 4m2s（61 单测绿）；确认沙箱需 `danger-full-access` 写 `D:\gradle_home`（复用 1.5GB 缓存，提权方案优于临时缓存）；
  2. 死配置清理：删 `modules/rhino/lib/rhino-1.7.14.jar`、`app/google-services.json`；catalog 删 firebase 3 库 + `firebaseBom` 版本 + `google-services` 插件；根 build.gradle 删 `google.services` 别名；
  3. 版本号外部化：`app/build.gradle` 改 `findProperty("VERSION"/"VERSION_CODE")` + 时间戳兜底（首版 `as String?` 语法报错，改 `?.toString()` 后通过）；`test.yml` set-ver 加 versionCode、build 命令改 `assembleAppRelease` + `-PVERSION/-PVERSION_CODE` 注入 + `testAppDebugUnitTest lintAppDebug` 门禁 + 去 `continue-on-error`；
  4. 验证：`compileAppDebugKotlin + testAppDebugUnitTest + assembleDebug` → BUILD SUCCESSFUL 8m；APK `legado_app_3.26.081414.apk`（时间戳兜底生效，格式 `3.YY.MMDDHH` 不变）。
- **版本号方案**：选 A（`-P` 外部注入），时间戳格式不变、应用内更新机制无感。
- **沙箱方案**：选提权（`danger-full-access` 复用 `D:\gradle_home`），而非临时缓存。

- **P1 已执行完成（2026-08-14，提交 `47319a4f8`）**：
  1. `gradle.properties`：开启 `org.gradle.parallel=true`、`org.gradle.caching=true`、`org.gradle.configuration-cache=true` + `problems=warn`；
  2. `kotlin.incremental.useClasspathSnapshot` 已核实为 Kotlin 2.x 废弃属性（默认启用），未设置（§4.3 已更新）；
  3. 代理迁移到用户级 `~/.gradle/gradle.properties`（不在 git），`gradle.properties` 解除 skip-worktree 恢复 HEAD 基线；
  4. 验证：配置缓存存储 + 复用均正常；`compileAppDebugKotlin + testAppDebugUnitTest` 复用配置缓存后 **1s**（基线 4m2s），配置阶段几乎消失。

- **P2 结论（2026-08-14，无需代码改动）**：Cronet 维持现状（`.so` 运行时下载，4 个 so task 是 md5 清单生成器）；代理已迁用户级；`settings.gradle` 镜像维持 skip-worktree（上游「不要提交修改」约定）。P2 无待提交改动。
