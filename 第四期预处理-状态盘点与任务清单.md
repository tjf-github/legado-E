# 第四期预处理：状态盘点与任务清单（2026-08-13）

> 目的：为第四期 4.3~4.5 开发做预处理——确认现状基线、核对代码落点、列出前置障碍与建议执行顺序。
> 依据：第四期计划书、复盘总结（2026-08-12）、代码与 Git 实际状态、JVM 单测实测。

## 1. 状态快照

| 项 | 状态 |
| --- | --- |
| 当前分支 | `codex/manga-import` @ `659344b01`（日终复盘提交） |
| 4.1 三期收尾补丁 | 已完成（`584c1036b`）：规则超集、首标题规范化、书源书籍常驻、默认标题 off-by-one |
| 4.2 多选合并 | 已完成（`cb586c085` + `0732fca5c` 续章编号前移 + `309acb666` 标题边界可拆分撤销） |
| JVM 单测 | **47 个全绿**（`testAppDebugUnitTest`，2026-08-13 09:27 实测；复盘记录的 48 以实际为准） |
| androidTest | 2 个编译就绪（`TocEditTransactionTest`、`BookDaoClassificationTest`），待真机 |
| 工作区 | 3 个 `modules/web` 生成文件有改动；6 项未跟踪（见 §5） |
| 构建环境 | `GRADLE_USER_HOME=D:\gradle_home`（wrapper 锁文件需写权限）；SDK 位于 `D:\gradle_home\android` |

## 2. 未完成工作分解（4.3 / 4.4 / 4.5）

### 4.3 在线书“转本地”操作引导（D）

**现状**：导出链路已存在（`CacheActivity` + `ExportBookService` + `getExportFileName`，支持 txt/epub）；本地导入已存在（`ImportBookActivity` → `LocalBook.importBook`）。缺的是在线书的“转本地”入口与引导。

**落点**：

- 入口：`ui/book/info/BookInfoActivity.kt`（`res/menu/book_info.xml` 新增菜单项，仅在线书显示）或目录页 `TocActivity`；
- 引导流程：确认对话框 → 选导出目录（复用 `HandleFileContract`）→ `ExportBookService` 导出 → 提示导入本地；
- 不改变在线阅读逻辑；首版=引导 + 一键导出。

**开发时需确认**：`ExportBookService` 是否只导出已缓存章节，是否需要先触发全文缓存（决定引导文案）。

### 4.4 漫画侧增强（E）

**4.4.1 单本相册导入（MediaStore）**

- 新入口：`ui/main/bookshelf/BaseBookshelfFragment.kt:105`（`menu_import_manga` 旁）；
- 权限：`AndroidManifest.xml` 目前**没有** `READ_MEDIA_IMAGES`（API 33+），需新增并按版本分支申请；旧版本沿用存储权限；
- 复用：相册扁平结构 → 单书单章（`MangaFolderScanner` 建书逻辑 + `ImportMangaViewModel.importBook` 可复用，相册每个相册=书、图片=整本连看）。

**4.4.2 元数据自动识别**

- 落点：`model/localBook/MangaFolderScanner.kt` 的 `scanBook`；
- 封面：命名含 cover/封面 的图片，否则取首图；作者/系列：从文件夹名/父目录名推导（不智能清洗）；
- 入库时写入 `Book` 的 `coverUrl` / `author` 字段（`ImportMangaViewModel.importBook`）。

**4.4.3 混合结构自动化识别**

- `scanBook` 统计“直接含图 + 含子文件夹”的混合节点，`MangaBookPreview` 增加建议角色；`canToggleWhole`/行级切换已兜底（`MangaPreview.kt`）。

**4.4.4 统一漫画阅读设置面板**

- 落点：`ui/book/manga/ReadMangaActivity.kt` 菜单（`menu_image_no_gap`、`menu_disable_manga_page_anim`、`menu_gray_manga` 等）；整合为面板，字段结构不变。

**4.4.5 图片间距可调（与无间隙开关合并）**

- 落点：`data/entities/Book.kt`（`ReadConfig.imageNoGap: Boolean?` 兼容映射 0/小/中/大）、`Book.getImageNoGap/setImageNoGap`、`ReadMangaActivity.kt:615-621`（现为布尔开关）、`ReadManga` 渲染处；
- 原则：`imageNoGap` 字段保留兼容，UI 不再暴露独立开关。

**4.4.6 大目录懒加载/分页**

- 落点：`MangaFolderScanner.scan/scanChapters`（当前全量扫描）、`ImportMangaActivity`（`scanningLiveData` 已有，补进度明细）、目录页章节懒加载；
- 建议先做此项（性能底座），再做相册导入。

### 4.5 工程补齐

- **构建脚本封装**：`watch-build.ps1` 已有雏形（可收编）；固定 `GRADLE_USER_HOME=D:\gradle_home`、SDK 路径；APK 文件名带时间戳，安装脚本动态取文件（复盘踩坑记录）；
- **`ReadConfig` 序列化单测**：`Book.ReadConfig`（`@Parcelize` data class）+ `BookDao` TypeConverter 已用 GSON；补 round-trip 单测（含 `imageNoGap`、null 字段）；
- **工作区卫生**：见 §5，待用户决策。

## 3. 前置障碍（建议先修）

### 3.1 覆盖文件残留问题（高优先，代码已定位）

- 现象：`BookHelp.getContent`（`help/book/BookHelp.kt:421`）**覆盖文件优先**；旧编辑/拆分/合并写入的 `.nb` 在重新导入后顶替新正文，导致内容错乱、拆分失灵（复盘 §9 已记录，模拟器复现）；
- 代码确认：`LocalBook.importBook`（`model/localBook/LocalBook.kt:260-281`）重导时只 `delByBook`，**未调 `BookHelp.clearCache(book)`**；对比 `ImportMangaViewModel.importBook` 重导时已清缓存；
- 建议：小补丁在重导路径加 `BookHelp.clearCache(book)`（与漫画导入对齐），配单测/实测；应在 4.3 与后续手测之前完成，避免误判新功能。

### 3.2 真机回归

- androidTest 2 个用例 + 一/二/三期手测清单由用户并行执行，代码侧不动。

### 3.3 `modules/web` 生成文件归属

- `auto-imports.d.ts`、`components.d.ts`、`tsconfig.app.json`（移除 baseUrl）共 3 个本地改动；疑似构建生成/本地工具改动，需确认是回退还是保留（4.5 工作区卫生一并处理）。

## 4. 建议执行顺序

1. **修覆盖文件残留**（小补丁，防后续手测误判；可与 4.5 工作区卫生并行）；
2. **4.3 转本地引导**（0.5~1 天，复用现有导出/导入）；
3. **4.5 工程补齐**（构建脚本收编、ReadConfig 单测、工作区卫生）；
4. **4.4 漫画增强**：按 4.4.6（性能）→ 4.4.1（相册导入）→ 4.4.2/4.4.3（元数据/混合结构）→ 4.4.4/4.4.5（设置面板/间距）排期；
5. 每步保持“编译 + 单测 + 打包”门槛，小步提交；真机回归并行由用户执行。

## 5. 工作区卫生清单（待决策）

| 文件/目录 | 类型 | 建议 |
| --- | --- | --- |
| `build.log` | 未跟踪，构建日志 | 加 `.gitignore` 或删除 |
| `watch-build.ps1` | 未跟踪，工具脚本 | 收编为构建工具（4.5 封装后保留） |
| `make_test_book.py` | 未跟踪，测试素材生成器 | 收编到测试工具目录或忽略 |
| `test_book.txt` / `test_book_split.txt` | 未跟踪，测试素材 | 同上 |
| `测试漫画素材/` | 未跟踪，漫画测试素材 | 同上 |
| `modules/web` 3 个文件 | 已跟踪有改动 | 确认生成文件归属：回退或提交（含 4.5 决定） |

## 6. 验证基线备忘

- 正确测试任务名：`gradlew.bat :app:testAppDebugUnitTest`（`testDebugUnitTest` 是空任务，别用）；
- 编译任务：`compileAppDebugKotlin`（计划书中的 `compileLegadoDebugKotlin` 不存在）；
- 打包：`assembleDebug`；APK 文件名带时间戳，安装必须动态取文件；
- Gradle 需要写 `D:\gradle_home`（wrapper 锁文件），沙箱内需提升权限。

